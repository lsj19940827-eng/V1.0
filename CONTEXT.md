# 当前进度

- 当前正在做什么：修正倒虹吸平面转弯半径优先级，确保 Excel/水面线显式填写的中间 IP 圆弧半径默认优先于 `n×D`。
- 上次停在哪个位置：已完成模型字段、提取传递、倒虹吸窗口确认、结果回写保护、PRD 和前端 SRS 更新，正在做回归测试。
- 近期关键决定和原因：
- “Excel 已填写”以水面线表格“转弯半径”列非空为准；`0` 也算显式填写，但只有大于 0 的圆弧半径会进入覆盖确认提示。
- 修改 `n` 或直接修改 `R` 后，在 Enter/失焦时提示确认；确认后才覆盖 Excel 半径，取消则继续使用 Excel 原始值参与计算。
- 回写水面线表格时，未确认覆盖不改写已有显式半径的中间 IP 行，确认覆盖后才允许写回新半径。
