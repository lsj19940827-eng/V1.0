# 当前进度

- 当前正在做什么：修复表3里特殊节点在“倒虹吸/有压管道回写后再次执行计算”时转角被二次清零的问题，并把相关旁路一起收口。
- 上次停在哪个位置：已经完成 `calculator.py` 共享保护逻辑、`panel.py` 两条刷新入口收口和回归测试补齐，当前停在文档同步与最终复核。
- 近期关键决定和原因：
- 特殊节点非零转角保护不再只放在“表3回写后的静默刷新”，而是上收到 `WaterProfileCalculator.calculate_all()` 共享使用；这样点击“执行计算”时不会再绕开保护链。
- 表3的几何重算入口 `_recalculate_geometry_impl()` 先清掉表格里遗留的 `进/出` 标记，再按当前坐标和转弯半径重算；原因是这条链路本来就要算“纯几何”，不能再吃旧状态把特殊节点提前清零。
- 保护范围只覆盖真实特殊节点；渐变段、自动插入连接段和原本就应为 `0` 的普通行继续保持 `0`，避免修复过头。
