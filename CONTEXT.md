# 当前进度

- 当前正在做什么：准备发布 `V1.3.4`，补丁最低支持版本收紧为 `V1.3.0`。
- 上次停在哪个位置：水锤 `v0` 修正已在 `master`，本次只补齐发版和补丁范围口径。
- 近期关键决定和原因：
- `V1.3.0` 及以上版本继续优先使用补丁包，更早版本不再提供补丁，改走完整安装包更新。
- `version.json.min_version` 保持 `1.0.0`，避免旧用户失去完整包更新入口。
- 本地自动保存、快捷方式和临时参考资料不进入 `V1.3.4` 发版，只在发布前后用 stash 暂存与恢复。
