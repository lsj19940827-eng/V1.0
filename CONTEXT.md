# 当前进度

- 当前正在做什么：正在把旧工作树里的连续承压整线改动合并进 `master`，并准备在验证通过后删除旧工作树和旧分支。
- 上次停在哪个位置：旧工作树里的改动已经单独验证通过，主干合并时出现文档和提取层冲突，正在按最终口径收口。
- 近期关键决定和原因：
- `xx渠` 不再一律排除整线；只有在末端或跨流量段形成连续承压线时，才补 `route_key / route_display_name / route_ip_points`，非连续场景继续只显示当前分组。
- `WaterProfilePanel` 里 route 分段组装继续保留类级 helper 修复；这样点击“开始计算”不会再报 `group` 参数缺失。
- 整线只作为底层管理口径；压力管道特性表、统计摘要和结果回写继续按原有分段和流量段表达，避免把既有导出口径一起改掉。
