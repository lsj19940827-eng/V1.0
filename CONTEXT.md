# 当前进度

- 当前正在做什么：收口压力管道特性表与连续承压链正式回写口径，把两个 `codex` worktree 的有效改动并入 `master`。
- 上次停在哪个位置：已合并 `pipe-fix`，完成当前表格快照取数、`支管 / 支渠` 主长度与隧洞统计收口，并复核出 `支渠 = 10356.253`、`支管 = 13153.743`；当前正在合并 `chain-fix` 的连续承压逐行正式计损与总损失同步。
- 近期关键决定和原因：
- `支渠` 只统计夹在 `有压管道 / 定向钻 / 顶管` 中间的隧洞；前置隧洞和末尾隧洞都不进入主长度或隧洞栏，避免同一张表出现两套长度口径。
- `支管` 主长度继续按整线口径；在有压边界长度基础上，前置无压隧洞及其与首段有压结构之间的空档都要补回；即使中间夹着渐变段或其他非统计节点，也不能把这段补长打断。
- 压力管道特性表统一优先读取当前导出节点快照；正式 DXF 导出不再优先吃过期 `calculated_nodes`，避免“调试值正确、正式导出回退旧值”。
- 连续承压逐行回写后的正式损失统一复用 `pressure_pipe_window_override`；列38展示值、总损失、累计总损失和水位递推必须来自同一份结果，避免出现“只显示、不累计”的分裂。
- DXF 导出主长度统一以 `panel.py` 汇总结果为准；旧缓存只做兜底，不能再把已过滤或已补回的长度改回旧值。
