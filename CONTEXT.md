# 当前进度

- 当前正在做什么：准备正式发版 `1.3.1`，已整理面向用户的更新说明，并完成本次版本相关回归验证。
- 上次停在哪个位置：`master` 工作区已确认干净，目标版本从 `1.3.0` 升到 `1.3.1`，下一步就是执行 `tools/release.py` 完成正式发布。
- 近期关键决定和原因：
- `1.3.1` 的更新说明统一改成用户能直接看懂的自然语言，不直接照搬开发提交说明；原因是这次发布面向最终用户，不是面向开发记录。
- 发版前的关键测试改为“每个测试文件单独进程运行，并把临时目录落到项目内”；原因是这样能避开 Qt 全局状态冲突和系统临时目录权限问题，验证结果更可靠。
- 建筑物名称上平面图在进入参数设置弹窗前，统一先检查名称、进 / 出标记和有效坐标点；原因是用户更需要提前知道哪些内容不会出图、以及原因。
