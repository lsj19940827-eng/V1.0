# 当前进度

- 当前正在做什么：已完成倒虹吸面板重排、加大量说明精度统一和相关 PRD 同步，当前停在 `master` 提交前校验与推送。
- 上次停在哪个位置：本地改动已经整理完，用户确认当前 `master` 里的代码可以提交并推送远端。
- 近期关键决定和原因：
- 倒虹吸顶部改成“整宽流量条带 + 紧凑参数区”，并把“水损阈值”移到“拟定流速”右侧；原因是减少右栏堆叠，避免动态内容挤压重叠。
- 加大流量说明改成“界面短提示 + 问号悬停完整说明”，两个问号 tooltip 统一提速到 `200ms`；原因是兼顾紧凑布局和可读性。
- 明渠、渡槽、批量计算和有压管道结果中的加大量公式与百分比显示统一保留更高精度；原因是 `Q加大` 输入模式下需要让换算过程和导出文字保持一致。
- 本轮已补相关回归校验，当前用于提交前确认的 `pytest` 结果为 `28` 通过、`0` 失败。
