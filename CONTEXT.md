# 当前上下文

## 正在做什么

- 修复断面 DXF 参数文字中 `m³/s`、`m²` 上标在 CAD 中显示异常的问题。
- 继续收口明渠多工况断面图：确认并防止恢复旧的简化绘图分支。

## 上次停在哪

- 已确认渡槽、明渠、隧洞、暗涵单工况 DXF 参数块存在同类风险，会把 `m³/s`、`m²` 写成普通 `TEXT`。
- 已确认隧洞多工况对比表已使用 `MTEXT` 安全写法，原本就是正常路径。

## 关键决定

- 含上下标的 DXF 参数文字统一用 `MTEXT` 控制码输出，普通文字仍保持原来的 `TEXT` 输出。
- 倒虹吸、有压管道未发现同款单工况断面参数块可复现路径，暂不做功能性改动。
- 明渠多工况断面图不保留“简单网格”旧分支，只允许调用带尺寸标注的完整绘图逻辑。
- 本次属于 DXF 显示缺陷修复，不更新 README.md 和 ARCHITECTURE.md。
