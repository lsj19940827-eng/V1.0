# 当前进度

- 当前正在做什么：补齐表3第38列锁定逐行承压行的手动录入口，让双击详情弹窗可以直接保存“本行采用值”或恢复自动计算。
- 上次停在哪个位置：连续承压链的逐行正式计损已经接通，但锁定行双击后只有详情说明，没有手动录入口，用户无法在不解锁单元格的情况下改正式承压损失。
- 近期关键决定和原因：
- 连续承压链中的 `有压管道 / 定向钻 / 顶管` 只要进入正式计算，就统一拆成逐行成员；父命名组只保留窗口汇总，避免表3重复计损。
- `HydraulicCalculator` 也要同步写回逐行显示损失，不能只算对总损失和水位，否则表3“倒虹吸/有压管道水头损失”一列会和正式递推脱节。
- 锁定逐行承压行继续保持单元格只读，手动入口统一收在“双击列38详情弹窗底部”；手动值持久化到 `pressure_pipe_window_override.manual_total_head_loss`，并优先驱动列38、总损失、累计损失和后续水位递推。
