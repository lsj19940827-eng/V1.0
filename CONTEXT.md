# 当前进度

- 当前正在做什么：已修复 1.2.9 启动前连续弹出多个 Terminal 窗口的问题；当前停在授权校验回归测试通过、准备重打包和启动冒烟验证。
- 上次停在哪个位置：上一轮停在补齐 `latex2mathml` 运行时数据后，确认 1.2.9 新压缩包已能正常弹出主窗口。
- 近期关键决定和原因：
- 主程序启动前的多 Terminal 弹窗不是 Qt WebEngine 或打包模式变化，而是 `license_checker.py` 在窗口程序里直接调用 `reg / wmic` 取机器码，Windows 会把这些控制台子进程窗口暴露出来。
- 授权校验同一次启动里会重复取机器码候选；现在统一用静默子进程执行系统命令，并在进程内缓存机器码候选，避免启动前重复拉起同类命令窗口。
- 这次修复不改授权文件格式、机器码算法和旧授权兼容逻辑；验证继续按“单测先红后绿 + 解压后直接启动主程序冒烟”执行。
