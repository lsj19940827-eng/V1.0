# 当前进度

- 当前正在做什么：完成“独立叠加水损”复查修复，正在提交并推送 master。
- 上次停在哪个位置：有压管道、倒虹吸、旧结果提示、状态栏文案和相关文档已修复；确认本次提交包含全部未跟踪文件。
- 近期关键决定和原因：
- 平面弯管/折管与纵断面弯管/折管分别计损后相加；原因是用户确认不再生成 3D 复合弯道参与水损。
- 沿程损失只算一次，有纵断面时优先取纵断面实长，否则取平面总长；原因是避免把整套水损重复计算。
- 旧结构段数据统一并入“平面+纵断面（独立叠加）/仅平面（独立计算）/仅纵断面（独立计算）”，不再显示“传统模式”；原因是历史项目也要按新口径解释结果。
- 旧工程里若 `data_mode` 仍是“空间模式/空间合并”历史字符串，保存时原样保留，并在汇总表和详情文本补“旧空间合并结果，请按新口径重新计算”；原因是兼容旧结果但不能把旧口径误当成新正式结果。
- `computed_from_profile_source` 只表示纵断面来源，不再用 `data_mode` 兜底；原因是避免把“结果口径”和“来源字段”混写。
- 通用构件和 `PIPE_TRANSITION` 只贡献 ΔZ2 的局部损失系数 ξ，不再把构件长度加进沿程长度；原因是避免把局部构件重复算成沿程损失。
- 有压管道新增可选 `common_local_loss`，默认 0；原因是通用构件局损要有独立入口，但不能和旧的比例估算法混用。
- `SpatialMerger` 保留为历史兼容和诊断工具，但倒虹吸/有压管道水损主路径不再调用 `merge_and_compute()`，纵断面实长也不再调用其私有长度方法。
