# 当前进度

- 当前正在做什么：正在修“倒虹吸/有压管道结果回写后，表3静默重算把已有非零转角清零”的问题，并已在独立 worktree 里补上回归测试。
- 上次停在哪个位置：已经确认根因在 `WaterProfilePanel._recalculate_silent()` 这条回写后刷新链路，当前实现改成专用刷新口径，避免 `in_out=进/出` 二次覆盖既有转角。
- 近期关键决定和原因：
- 这次不改 `calculate_all()` 主链，避免把“执行计算”的默认行为一并带偏；修复只收口在表3回写后的静默刷新入口。
- 静默刷新会继续重算几何长度、桩号和下游水位，但仅对“表内已存在非零转角的真实节点”临时放开 `进/出` 标记，几何完成后再恢复正式进出口语义；原因是既要保住像“七一 IP0”这类合法非零转角，又不能影响渐变段和普通进出口行的零转角规则。
