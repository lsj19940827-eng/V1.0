# 当前进度

- 当前正在做什么：收口压力管道特性表在 `支管 / 支渠` 下的主长度与隧洞统计口径，补齐 PRD，并准备直接提交到 `master`。
- 上次停在哪个位置：已补齐 `README / ARCHITECTURE / CONTEXT / PRD`，核心口径回归 `test_pressurized_dxf_rules_unit.py` 通过，并用九龙自动保存工程复核出 `支渠 = 10356.253`、`支管 = 13153.743`。
- 近期关键决定和原因：
- `支渠` 只统计夹在 `有压管道 / 定向钻 / 顶管` 中间的隧洞；前置隧洞和末尾隧洞都不进入主长度或隧洞栏，避免同一张表出现两套长度口径。
- `支管` 主长度继续按整线口径；在有压边界长度基础上，前置无压隧洞及其与首段有压结构之间的空档都要补回。
- DXF 导出主长度统一以 `panel.py` 汇总结果为准；旧缓存只做兜底，不能再把已过滤或已补回的长度改回旧值。
