# 当前进度

- 当前正在做什么：正在把“渐变段长度口径统一”和“xx管 整线纵断面 + 子段计算”两组草稿并回 `master`，当前代码已合上，剩余是继续并入最后一条支线并做总校验。
- 上次停在哪个位置：`transition-length-alignment` 与 `xxpipe-full-route` 的草稿都已落成提交，第二条支线的代码改动已并入主干，文档冲突已人工整理。
- 近期关键决定和原因：
  - 普通有压管道/定向钻/顶管的渐变段长度正式计算，统一按相邻渠道设计水深的 `5h/6h` 处理，避免插入阶段和详情弹窗各用一套口径。
  - 渐变段详情继续保留“公式/规范值、规则目标长度、物理上限、最终采用长度”四层信息，让长度被压缩时也能直接看懂原因。
  - xx管 下连续“有压管道/定向钻/顶管/隧洞*”会先识别成整线，再把每个子段自己的起止桩号挂到 group 上，避免整线导入后只能按单段重复维护纵断面。
  - 整线纵断面改按 `route_key` 持久化，空间长度、空间总损失、导出和中心线高程都先找整线数据，再按 `segment_start_mc / segment_end_mc` 裁切子段，减少匿名子段撞键和跨段误算。
