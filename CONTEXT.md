# 当前进度

- 当前正在做什么：正在把“有压管道基础水锤验算”和“表3静默刷新保留既有非零转角”的修复一起收口到 `master`，并完成合并后的验证与推送。
- 上次停在哪个位置：功能分支已经并入本地主干，当前正在整理合并记录、恢复本地 JSON 数据，并准备跑整组合并回归测试。
- 近期关键决定和原因：
- 基础水锤验算只做“直接关阀、全关”这一版，并列展示结果，不写入主水位和主损失递推链，避免把事故校核值混进正常工况计算。
- 水锤输入放在每条有压管道卡片内部，和现有平面 `R / D` 参数放在同一入口；`wall_thickness_m` 与 `water_hammer_basic` 单独持久化到 `PressurePipeManager`，保证弹窗重开可恢复，同时不污染表3正式承压覆盖口径。
- 这次不改 `calculate_all()` 主链，静默刷新只收口在表3回写后的专用刷新入口；原因是要保住像“七一 IP0”这类真实节点已有的非零转角，同时继续让渐变段和普通进出口行保持零转角规则。
