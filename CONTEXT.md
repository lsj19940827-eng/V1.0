# 当前进度

- 当前正在做什么：修复“空名称有压管道紧邻隧洞时，插入渐变段会漏掉隧洞进出口两侧渐变段”的问题。
- 上次停在哪个位置：已在 `推求水面线/core/calculator.py` 补上插段阶段的本地边界识别，并新增对应回归测试。
- 近期关键决定和原因：
  - 不改 `preprocess_nodes` 对空名称有压管道保持 `NORMAL` 的旧规则，避免影响有压管道分组与提取链路。
  - 只在 `_should_insert_open_channel`、`_check_gap_exit_to_gate`、`_check_gap_gate_to_entry` 中把空名称有压管道按当前位置临时识别为进口或出口，范围最小。
  - 继续保留现有渐变段压缩/合并规则，这次只修“边界没识别到”的问题，不扩展到长度策略。
