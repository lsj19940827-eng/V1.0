# 当前进度

- 当前正在做什么：已完成 `xx渠` 连续承压整线支持，正在收尾文档同步和最终核对。
- 上次停在哪个位置：代码和相关测试已经通过，剩余 README / ARCHITECTURE / PRD 的口径更新。
- 近期关键决定和原因：
- `xx渠` 不再一律排除整线；只有在末端或跨流量段形成连续承压线时，才补 `route_key / route_display_name / route_ip_points`。
- 整线只作为底层管理口径；压力管道特性表、统计摘要和结果回写继续按原有分段和流量段表达，避免改表。
- 保留 `panel.py` 里 identity helper 的类级调用修复；这样“开始计算”不会再报 `group` 参数缺失。
