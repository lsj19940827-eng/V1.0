# 当前进度

- 当前正在做什么：已完成“平底圆形”隧洞全链路收口，以及表3第38列“倒虹吸/有压管道水头损失”的双击修复，当前处于合并收尾阶段。
- 上次停在哪个位置：平底圆形链路已经补齐并通过回归；第38列原先在倒虹吸出口行会误弹“没有详情”提示，现在已改成“有详情可看可改、没详情直接编辑”。
- 近期关键决定和原因：
- 平底圆形固定采用“直径 D + 平底宽 B”两输入，程序自动推导总高 `H_total`，并由 `app_渠系计算前端/tunnel/geometry.py` 作为唯一几何真源，避免计算、绘图、DXF 和表3复算口径分叉。
- 表3对 `隧洞-平底圆形` 继续只允许“表1同步 / 共享结果导入”两种来源；双击选择、手输、粘贴和批量改写都拦截，避免手工新建和导入链路出现两套口径。
- 第38列新增独立的人工采用值 `pressure_pipe_loss_override_m`，不覆盖原始计算结果；这样详情弹窗既能保存人工采用值，也能恢复回原计算值，项目重开后仍可还原。
- 第38列双击先判断“有没有可展示详情”；有详情时打开详情弹窗并允许保存采用值 / 恢复计算值，没有详情时直接进入单元格编辑，避免继续弹误导提示。
- 当前验证结果：本次相关回归共 `77` 项通过、`0` 项失败；此前平底圆形专项与相关旧回归共 `92` 项通过、`0` 项失败，落文件冒烟也已完成。
