# 当前进度

- 当前正在做什么：已补充一份面向用户的《xx管有压管道与夹带隧洞使用说明》，把整线卡、隧洞分段卡、DXF 导入、典型场景、常见提示和导出口径整理成操作手册，方便培训和现场使用。
- 上次停在哪个位置：停在 mixed route 主链已经打通，但操作细节分散在代码、PRD 和测试里，开发者和用户都不容易一次看清的位置。本次文档工作未改功能逻辑，沿用上一轮已验证的实现口径。
- 近期关键决定和原因：
  - xx管 mixed route 不再拦截整线；连续承压链继续保留原顺序，隧洞成员继续按原有单行规则参与链计算，避免重写整套链模型。
  - xx管 只导入 1 份整线纵断面 DXF，但这份 DXF 只覆盖非隧洞段；隧洞段按“进口底高 + 坡降 i + 起终桩号”生成，并统一存进 `routes[route_key].profile_segments`。
  - 若整线起点是隧洞，则 DXF 导入锚点固定按“首个非隧洞节点”的实际或回退桩号确定；这样即便该节点本身没填 `station_MC`，也不会误退回整线起点。
  - 当 mixed route 后续变回纯有压整线时，计算结果会显式把 route 级 `profile_segments` 写成空列表，主动清掉旧的隧洞分段缓存，避免导出仍误读历史 mixed 数据。
  - 隧洞分段卡现在直接录入“进口底高 / 坡降 i / 出口底高校核 / 断面类型 / 断面参数”，并同步写进 `PressurePipeManager`；重新打开窗口后仍能恢复，避免每次重填。
  - xx管 图2第 4 行标题继续保留“管中心线高程（米）”，但隧洞段实际出底高程；第 5 行在隧洞段改为断面参数文本，普通有压段仍显示材质和 DN。
  - 用户手册里不再使用 `mixed route`、`profile_segments` 这类内部术语，统一改写成“夹带隧洞的整线”“整线纵断面”“隧洞参数”，降低培训门槛。
