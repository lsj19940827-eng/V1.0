# 当前进度

- 当前正在做什么：已把“Excel 导入后顶部立即采用手工 `Q加大`”主线和倒虹吸 `D` 行两项收尾一起合到 `master`，正在做最后的测试、提交和分支/worktree 清理。
- 上次停在哪个位置：`master` 里已有一组未提交的 Excel/批量计算主线改动，两个 `codex/siphon-*` worktree 里又各自留着“单位不拆行”和“左栏宽度稳定”的未提交修复，尚未统一收口。
- 近期关键决定和原因：
- 保留两个 siphon worktree 的代码、测试和 PRD 改动，但不带 `siphon_autosave.json`、`工况*.siphon.json` 这类运行态文件；原因是这些文件属于现场状态，不应混进主干提交。
- 倒虹吸 `D` 行同时补两层保护：`m³/s` 在斜杠处不再拆行，左侧“管道基础参数”列按最宽态预留保底宽度；原因是既要解决窄宽度下最后一个 `s` 掉行，也要避免 `N=1 / N=2 / 指定管径` 切换时布局抖动。
- 手工加大流量唯一入口继续收口在导入 Excel `导入模板` 页第 1 行右侧列对，从 `I1:J1` 起按“每段一个标签 + 一个值单元格”填写；原因是保持表 3 只读，不在程序里再开第二个输入口。
- 批量计算继续复用现有“按比例计算”内核，只把手工 `Q加大` 反算成 `manual_increase_percent`，并让顶部“设计流量 / 加大流量”同步链路直接消费 batch 页读到的 `_manual_qmax_by_segment`；原因是保证导入后立即显示用户真实输入，同时不新开第二套计算分支。
