# 当前上下文

## 正在做什么

- 执行 `1.3.5` 正式发版，把 `隧洞-圆拱直墙型` 反算弹窗预览修复发布给用户。
- 本次发版纳入当前所有未提交改动，包括自动保存文件的保存时间变化。

## 上次停在哪

- 已完成发版预检，当前版本为 `1.3.4`，目标版本 `1.3.5` 按 `patch` 递增。
- 用户已确认全部未提交改动纳入本次发版。
- 发版前相关隧洞回归已通过：19 个通过，0 个失败。

## 关键决定

- “净空比例”按净空面积比解释；目标比例用于加大流量工况。
- 固定 `H/B` 与 `θ` 后，先在单位断面求目标水深，再用曼宁相似律反推 `B`，最后回到现有水深和输出函数复核。
- 弹窗内临时修改 `Q加大` 或 `θ` 不影响主面板；只有点击“采用到当前工况”才回填 `θ、B、H直`，且保留 3 位小数。
- 校核失败时只提示原因并禁用采用，不自动替用户改参数。
- 断面预览的浅蓝色水域只表达可视化形状；面积和校核仍以内核计算结果为准。
- 更新说明使用朴素用户话术：修复隧洞圆拱直墙型反算弹窗的预览图，水位进入拱部时，浅蓝色水域会贴着真实断面显示，不再画成窄矩形。
