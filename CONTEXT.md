# 当前进度

- 当前正在做什么：正在把“有压管道基础水锤验算”并列接入有压管道弹窗，并补齐测试与文档。
- 上次停在哪个位置：上一阶段先完成了水锤原理与输入项说明，这一阶段开始正式落地到水面线模块。
- 近期关键决定和原因：
- 基础水锤验算只做“直接关阀、全关”这一版，并列展示结果，不写入主水位和主损失递推链，避免把事故校核值混进正常工况计算。
- 水锤输入放在每条有压管道卡片内部，和现有平面 R / D 参数放在同一入口，便于按管段逐个核对。
- `wall_thickness_m` 与 `water_hammer_basic` 单独持久化到 `PressurePipeManager`，不复用 `pressure_pipe_window_override`，这样重开弹窗能恢复输入和结果，同时不污染表3正式承压覆盖口径。
