# 当前进度

- 当前正在做什么：主干已收口“xx管 下方子表拆分”“表3 黄色提示误报收口”和“有压管道按钮保持可点击反馈”，当前停在合并后回归与清理阶段。
- 上次停在哪个位置：`codex/xxpipe-split-subtables` 与 `codex/warn-missing-structure-height` 两个本地分支已完成提交，并按顺序合并回 `master`。
- 近期关键决定和原因：
- 下方 `xx管` 表继续只保留 `有压管道 / 定向钻 / 顶管`；当这些有压段被隧洞分隔成多段连续 span 时，改为“共享表头 + 横向多个独立子表”，避免一整张下表跨空隙连线，也给后续汇总表和 IP 表留出稳定位置。
- 表3 计算完成后的“缺少结构总高”黄色提示统一排除承压类节点；真正缺少 `H_total` 的普通渠道/隧洞仍继续提示，不扩展成承压行 `top_elevation` 自动补算。
- 表1/表3结果失效时，“有压管道水力计算”按钮继续保持可点击，并沿用现有前置校验给出提示，避免按钮直接禁用后没有反馈。
- `data/siphon_autosave.json` 本轮只有自动保存时间戳变化，因此继续排除在提交之外，避免制造无意义冲突。
