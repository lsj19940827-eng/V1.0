# 当前上下文

## 正在做什么

- 正在修正后续发版的 Gitee 上传规则。
- 新规则：完整包不再上传 Gitee；只有补丁包且不超过 100MB 时才上传 Gitee，超过就跳过，不阻断正式发版。

## 上次停在哪

- 已完成 1.3.8 发版；Gitee 官方限制导致 355.8MB 完整包不能上传，82.23MB 补丁包已上传成功。
- 当前正在把这个限制固化进发版脚本、测试和文档，避免以后每次发版再遇到同类失败。

## 关键决定

- GitHub Gist 仍是唯一正式 `version.json` 主入口，GitHub 是完整包下载源，Gitee 只作为 100MB 以内补丁包备用下载源。
- `GITEE_TOKEN` 只从 `.env` 或环境变量读取，不写入代码；正式发版前缺少该令牌就提前停止。
- 客户端补丁下载先尝试现有 GitHub/代理地址，失败或 SHA256 不一致时再尝试 Gitee 镜像；补丁失败回退全量包时使用 GitHub 完整包地址。
- README 面向仓库访客，只保留功能、运行、测试、发版、自动更新和提交边界，不再堆叠内部过程记录。
- 发版脚本创建 Gitee Release 前，必须已把对应 release commit 和 tag 推到 Gitee，避免 Gitee Release 指到旧代码；但 Gitee Release 只上传合格补丁包，不上传完整包。
- `.gitignore` 需要覆盖根目录工程文件、日志、成果目录和授权快捷脚本，防止发版脚本 `git add -A` 误收本地产物。
