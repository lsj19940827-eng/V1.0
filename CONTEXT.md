# 当前上下文

## 正在做什么

- 正在优化表3复核长度的异常提示。
- 新逻辑：复核弯前长度、复核弯后长度、复核总长度显示为负数时，表格数字标红；执行计算后用一次非阻断 InfoBar 汇总提示。

## 上次停在哪

- 已在 `app_渠系计算前端/water_profile/panel.py` 增加复核长度负数判定、红字显示和计算完成提示汇总。
- 已在 `tests/test_water_profile_coord_precision_unit.py` 补充红字、提示汇总、提示数量截断和现有列头公式说明保留测试。

## 关键决定

- 只改界面表3显示和执行计算后的 InfoBar，不改导出、保存数据结构或计算公式。
- 负数按三位小数显示值判断，显示为 `-0.000` 时不标红、不提示。
- InfoBar 最多展示前5处负数，超过时汇总为“等N处”，不打断计算流程。
- 保留现有列头触摸公式说明，不新增列头提示；相关 PRD 已同步，README 和 ARCHITECTURE 暂不需要调整。
