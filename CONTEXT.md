# 当前进度

- 当前正在做什么：修复江家坝支管逐行承压成员中“有效平面转角但弯头损失为 0”的问题。
- 上次停在哪个位置：已补折管损失入口，并让逐行承压成员在转弯半径为 0 或未填写时按折管公式计算；小型单元测试和江家坝真实样例测试已转绿。
- 近期关键决定和原因：
- 平面转角有效且转弯半径 `R>0` 时继续按弯管查表；平面转角有效但 `R<=0` 或未填写时按折管计算，不自动给默认半径；原因是避免引入不透明的工程假设，同时修复江家坝支管 `121` 个有效转角逐行段漏算弯头损失的问题。
- 表3工具栏顺序调整为“插入渐变段 / 倒虹吸水力计算 / 有压管道水力计算 / 执行计算 / 有压管道水锤验算”；原因是水锤需要先完成水力计算并生成表3水位。
- “有压管道水力计算”只负责水损计算、纵断面导入和结果回写；“有压管道水锤验算”只保存并展示水锤结果；原因是避免两个流程混在同一个入口里。
- 水锤按钮按基础条件灰色禁用：表1/表3链路未就绪、未检测到承压结构、有压水力计算未完成或表3水位未生成时都不可点击；单段缺壁厚、启闭时间等细节仍在水锤窗口内提示。
- 水击验算改为线性关闭正水击 + 线性开启负水击；原因是用户指定《水力计算手册》简单管道线性启闭理论。
- 正式水击控制值仍按直接、第一相、末相候选结果比较取得；原因是计算更稳定。
- 图 1-3-3 现在作为对照字段显示，单管卡、整线卡和整线明细可看到对应区名；原因是便于按手册人工复核，但不参与正式控制值计算。
- 整线水击分布验算会自动跳过起终点相同且没有实际长度的锚点行；原因是这类行不是物理管段，不应阻断后续真实管段验算。
- 计算核心的零长度兜底提示改为检查起终点桩号或忽略起点锚点；原因是避免把“成员长度”这类内部术语暴露给用户。
- 水击分布插值采用 `±0.001m` 桩号容差；原因是 DXF 原始终点和界面三位小数终点存在毫米级尾差，不能误判覆盖不足。
- 表3水位为 0 时按“缺少表3水位”处理；原因是 0 表示主链路尚未生成有效压力线，不能拿来做水击压力线。
- 水击结果仍只在弹窗内独立展示和保存，不写入表3第38列、水损、累计损失或主水位递推。
- 水锤窗口内“验算/刷新”使用橙色强调按钮，`控制类型 / 图1-3-3对照` 等长结果改为整行换行；原因是避免验算后窗口内容宽度增加并截断右侧信息。
