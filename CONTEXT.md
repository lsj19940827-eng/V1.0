# 当前进度

- 当前正在做什么：正在收口“特殊建筑进出口不占显示 IP 编号”的实现，代码、测试和文档已同步，当前只剩最终整理。
- 上次停在哪个位置：停在规则已确认，待把显示编号从内部 `ip_number` 中拆开，并同步到主表回读、纵断面、IP 表和 legacy Excel。
- 近期关键决定和原因：
  - 保留内部 `ip_number`，新增显示用 `display_ip_number`；这样不影响现有计算链和专项模块，同时能把用户看到的编号改成连续口径。
  - 隧洞/渡槽/倒虹吸/有压管道/定向钻/顶管的进出口只显示“名称 + 简称 + 进/出”，不占显示 IP；矩形暗涵保持原规则，避免误伤旧表。
  - 主表 IP 单元格同时写入 raw/display 元数据，表格回读优先取元数据；这样“黄角坝隧进”这类没有数字的文本也不会被误判成 `0`。
