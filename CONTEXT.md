# 当前进度

- 当前正在做什么：已把 xx管 夹带隧洞整线能力合并到 `master`，并把纵剖表格第 5 行表头统一为“管材（管径）”，当前正在收口主分支提交与推送。
- 上次停在哪个位置：停在 mixed route 主链、测试和用户手册已经完成，但 `master` 上另有一组“管材（管径/米）”改名改动还没并入的位置；本次把两边口径合成同一版主线。
- 近期关键决定和原因：
  - xx管 mixed route 继续只导入 1 份整线纵断面 DXF，非隧洞段走 DXF，隧洞段按“进口底高 + 坡降 i + 起终桩号”生成，并统一落到 `profile_segments`；这样不用给每段隧洞单独准备 DXF。
  - 隧洞段在图2里继续占用固定 5 行模板：第 4 行标题仍显示“管中心线高程（米）”但实际输出底高程，第 5 行改为断面参数文本；这样既不打乱既有版式，也能把隧洞信息表达清楚。
  - xx管 固定 5 行模板里的第 5 行表头统一改为 `管材（管径）`；因为这一行只表达材质、管径或隧洞断面参数，不再混入长度单位，用户理解更直接。
