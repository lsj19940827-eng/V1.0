# 当前进度

- 当前正在做什么：补齐 `xx管 / xx渠` 整线卡里的基础水锤验算入口。
- 上次停在哪个位置：连续有压段识别、route-only 整线入口、批量填写、保存恢复和表3不回写回归均已完成，正在做最终验证。
- 近期关键决定和原因：
- 水锤按连续 `有压管道 / 顶管 / 定向钻` 合并成验算段；原因是用户真正关心的是连续有压管道类整体，而不是逐行入口。
- `隧洞 / 明渠 / 倒虹吸 / 渐变段 / 自动插入连接段` 不参与水锤段；原因是它们不是本次基础水锤公式的有压管道类对象，隧洞还会切断前后有压段。
- 同段内 `D / 管材 / 流量` 不一致也不拆段，默认推荐最大 `v0` 代表行并允许手改；原因是保持入口数量少，同时让用户能选择保守参数。
- route 级水锤结果保存到 `routes[route_key].water_hammer_segments`，且不写入表3第38列和水位递推；原因是本次只做独立校核。
