# 当前进度

- 当前正在做什么：收口“在线更新卡在校验安装环境”的修复，补旧 `_update_sessions` 残留自动清理、`validate` 细分状态和更明确的失败提示。
- 上次停在哪个位置：已经完成更新器主链路、安装窗口文案和相关单元测试，下一步是跑最终验收命令与本地冒烟，再同步现场处理结论。
- 近期关键决定和原因：
- 根因先按“失败电脑本地状态异常”处理，而不是把问题归因到全量包本身；同样的 `1.2.5 -> 1.2.6` 在另一台电脑上能成功，说明问题更像旧残留、占用或安全软件干预。
- `_update_sessions` 继续视为纯临时目录：安装开始时只清理当前会话之外的旧目录，不碰 `_internal`、主程序文件和用户数据。
- `validate` 继续沿用原有阶段枚举，但现在会细分显示“清理残留 / 检查写入权限 / 统计目录大小 / 解压完整包或补丁包 / 校验补丁适用性”，减少用户把重操作误判成卡死。
- 如果旧残留清理失败，更新器会记录具体路径并返回 `stale_session_cleanup_failed`，窗口直接提示先关闭软件，仍失败再重启电脑。
