# 当前进度

- 当前正在做什么：本轮已把“xx管夹带隧洞按结构分上下两张表”落到 `TXT / DXF / 合并 DXF` 三个导出入口，并同步复核保存重开、表1真源和文档口径。
- 上次停在哪个位置：上一轮已完成“隧洞参数只认表1、弹窗改只读摘要”，但导出仍保留“隧洞留在 xx管 下表里”的旧逻辑，这一步把导出链一起切到新口径。
- 近期关键决定和原因：
- `xx管` 夹带隧洞导出改成按结构拆成上下两张表：上方普通渠道表继续跟随当前 `7/扩展项` 配置，下方 `xx管` 表只保留 `有压管道 / 定向钻 / 顶管`，这样隧洞回到更符合现场理解的普通渠道表达，不再和固定 5 项管表混在一起。
- 隧洞参数的正式来源只有表1手填或 Excel 导入；弹窗只做只读摘要和缺项提示，不再提供单独录入或单独导出一套隧洞参数，避免出现多套真源。
- 圆拱直墙型只认 `B`，不再使用 `H`，原因是这轮只收口现有输入链，不扩展新的断面编辑方案。
- `xx管` 下方专用表不再单独展示无压隧洞底线或断面参数；涉及隧洞的复核与修改，一律回表1或 Excel 处理，避免把展示信息误当施工口径。
- 导出压缩区间按结构类型重新布局，同类型多段会按原桩号顺序接着画，中间不属于本表的区间直接省掉；这样 `TXT / DXF / 合并 DXF` 三个入口保持同一套双表结果。
- 人工冒烟时额外发现“压缩区间起点为 0 时会被误当空值”的问题，已修复并补了回归测试，避免第一段不压缩的隐性偏差。
- 旧项目不承诺迁移保证；低成本兼容只作为内部兜底，不作为对外说明口径。
