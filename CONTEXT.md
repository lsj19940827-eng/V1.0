# 当前进度

- 当前正在做什么：准备发布 1.2.1，已完成 `xx管` 纵断面导出、有压管道导出口径和示例8入口的收尾，并通过本次发布相关回归测试。
- 上次停在哪个位置：停在 `xx管` 导出和有压管道规则已经接通，但 IP 表展示口径、设计流速显示精度、建筑物长度统计以及示例8入口还没有一起整理进正式版本的状态。
- 近期关键决定和原因：
  - `xx管` 的“管中心线高程（米）”单独使用 `xxpipe_centerline_elev_decimals`，默认 2 位；这样用户能单独调这一行精度，不会影响普通纵断面的其它高程项。
  - IP 表只把展示口径统一成“里程”，不改底层数据键；这样预览、DXF 和 Excel 能一起统一，同时不打断已有计算链路。
  - 压力管道特性表里的“设计流速”只改展示精度为两位小数，建筑物摘要长度按“出口里程 - 进口里程”统计；这样导出更稳定，也避免把相邻普通管段误算进建筑物长度。
  - “示例八（江家坝支管）”同步补到批量计算面板和水面线面板；这样两个入口看到的示例保持一致，方便演示和回归测试。
