# 当前进度

- 当前正在做什么：同步本轮“表3 计算后黄色提示误报收口”的文档，主修点是把“缺少结构总高”提示统一改成排除承压类节点。
- 上次停在哪个位置：mixed route 导出已经走 `generated_tunnel/profile_segments` 分段纵断面链路，DXF 本身可正常导出；剩余问题集中在表3计算完成后的黄色提示仍沿用旧判定。
- 近期关键决定和原因：
- 这轮只收口黄色提示的判定口径：旧逻辑只按 `bottom_elevation` 有值且 `top_elevation` 为空触发，没有复用现有承压类节点识别，导致 `xx管`/连续承压边界行误报。
- 用户可见结果统一为：`有压管道 / 定向钻 / 顶管` 等承压类节点不再触发这条黄色提示；真正缺少 `H_total` 的普通渠道/隧洞仍继续提示。
- 明确不新增承压行 `top_elevation` 自动补算，原因是本轮目标是收口提示口径，不扩大成数据回写或补算行为变更。
