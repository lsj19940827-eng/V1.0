# 当前进度

- 当前正在做什么：已完成三清支渠有压管道的纵断面选线修正、支渠连续承压链修正，正在同步文档并做最终收口。
- 上次停在哪个位置：针对“DXF 误取首条多段线”“支渠前置隧洞误入连续承压链”“route 起点被前置隧洞带偏”三类问题，回归测试已经补齐并通过。
- 近期关键决定和原因：
- 纵断面 DXF 改为按候选规则自动优选，不再盲取首条多段线；当前两名候选非常接近时，导入前先弹一次确认，避免把工程坐标辅助线误当纵断面。
- 支渠连续承压链只从首个真正的有压段开始，前置隧洞不再入链；但进入有压段后的后续隧洞仍保留，避免把三清支渠这类整线起点和 route 锚点带偏。
- 支渠 route 上下文跟随收紧后的链覆盖行生成，因此 `route_start_row_index`、导入锚点和整线平面点都会从首个真正有压段起算；`xx管` 原有 mixed route 口径保持不变。
