# 当前进度

- 当前正在做什么：本轮补齐了 `xx渠` 末尾连续承压命名尾段的最后一格漏计问题；当旧工程或静默重算场景里只剩列38显示值时，总损失、累计损失和水位现在也会一起补回，已补齐回归测试和 PRD。
- 上次停在哪个位置：上一轮已经把逐行正式计损主链打通，但命名尾段出口行在“只剩显示层本段损失”的旧数据场景里，仍会出现列39是 `-`、累计值停在上一行。
- 近期关键决定和原因：
- `_pressure_pipe_display_loss` 现在也作为命名尾段出口行的正式递推口径；原因是旧工程恢复后，这一笔值可能是仅剩的真实本段损失。
- 表3列38、总损失、累计损失和水位递推改成共用同一条命名尾段取值逻辑；原因是要彻底消掉“显示层有值、递推层没跟上”的错位。
- 命名整组结果仍只保留在有压管道窗口汇总里；出口行正式参与递推的，始终只能是本行逐段损失，避免整组口径再次挤回表3。
