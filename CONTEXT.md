# 当前进度

- 当前正在做什么：修正 DXF 与相关导出里的有压段 IP 点命名口径，并同步测试与 PRD。
- 上次停在哪个位置：已经确认 `有压管道 / 定向钻 / 顶管` 的进出口名称都走统一命名函数，当前实际会统一显示成 `xxx压进 / xxx压出`。
- 近期关键决定和原因：
- 这次只改展示名称，不改“有压管道同类”的业务归类、计算逻辑和导出分表逻辑，避免一个文案问题扩散成承压规则改动。
- 命名规则统一改成：`xxx有压管道进/出`、`xxx定向钻进/出`、`xxx顶管进/出`；隧洞、倒虹吸、渡槽继续保留原简称，保证老图面风格稳定。
- 主模型和 `cad_tools` 里的兜底显示都要一起改，避免“DXF 一种叫法、提示里另一种叫法”再次分叉。
- 连续承压场景不再只靠旧的 `pipes + 名称兜底`，而是正式增加 `routes / segments` 两层持久化，避免“计算一套、导出一套、提示一套”继续分叉。
- 支渠连续承压成员统一改成 `flow{流量段}-row{行号}` 新身份；旧 `rowsxx` 口径只被动兼容，不再继续生成，避免前缀段导出时再落回旧空记录。
- 连续承压保存改成“按当前整线快照重建并覆盖活动范围”；同一整线范围内残留的旧 `route / segment / pipe` 会一起清掉，避免新旧两套记录同时参与导出。
- xx管 普通导出默认优先拿表3完整节点做查找；若先碰到空壳旧记录，会继续按 `route_key` 回退整线纵断面，不再把已存在的 `405m` 判成未匹配。
- DXF 建筑物名称统一只显示基础名称；软件内展示名继续保留“前缀段 / 前段 / 后段”这类区分，避免再次互相污染。
- 末尾连续承压的判断改成独立规划阶段，统一按整张表 `full_nodes` 判断；这样赛金支渠这类“前面明渠、后面连续承压”的导出入口不再各自打补丁。
- `导出全部DXF`、单独纵断面 DXF、单独 TXT 现在共用同一套尾段分表规则；进入真正承压段后的普通有压、定向钻、顶管、隧洞统一归下方 5 项有压表。
- 本次相关回归已通过，连续承压、xx管、合并 DXF 三类测试已一起覆盖。
