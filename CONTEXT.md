# 当前进度

- 当前正在做什么：已完成表3 4 个主按钮的回滚修正，锁定时恢复为原生灰色禁用态且不可点击。
- 上次停在哪个位置：上一轮停在“灰色但可点”方案已落到主工作区；本轮已回滚代码、同步文档，并跑通相关回归测试。
- 近期关键决定和原因：
- `routes[route_key].longitudinal_nodes` 继续作为 xx管 整线纵断面的事实来源；关闭返回后即使 `pipe / segment` 入口被清掉，重开弹窗也先按 `route_key` 恢复。
- `pipe / segment` 只保留为回退来源；覆盖不完整时继续保留 route 级缓存并提示补导入，不要求重复导入同一份 DXF。
- `1.2.8` 继续按既定版本号走 `patch` 发版；虽然本轮包含新增能力，但为保持版本节奏不额外改成 `minor`。
- `data/siphon_autosave.json` 只改了自动保存时间，不纳入正式发版，避免把现场临时数据带入版本提交。
- 表3工具栏的 `插入渐变段 / 倒虹吸水力计算 / 有压管道水力计算 / 执行计算` 已决定回滚为原生禁用态，原因是必须保留按钮原生内核风格，并避免解锁后残留自定义灰样式。
- `tests/test_water_profile_transition_ready_unit.py` 已在主工作区恢复并更新，只覆盖本轮按钮回滚相关断言；其余已删除测试文件保持现状，不额外恢复。
