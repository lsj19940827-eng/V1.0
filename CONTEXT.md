# 当前进度

- 当前正在做什么：已完成 `1.2.1` 启动失败热修，修复正式包启动时 `utils.pressure_pipe_result_helpers` 导入失败导致主界面不弹出的问题；当前打包、导入校验和启动冒烟都已通过。
- 上次停在哪个位置：停在“新包里已经带上 `pressure_pipe_result_helpers`，但启动仍然同样报错”的位置；后来确认不是再次漏打包，而是打包运行时顶层 `utils` 被同名包抢占。
- 近期关键决定和原因：
  - 打包清单和打包前校验同时保留 `utils.*` 与 `推求水面线.utils.*` 两条导入路径；这样既补上原先漏掉的 helper，也能提前拦住命名空间路径再次漏包。
  - `推求水面线/utils/__init__.py` 现在会在加载时显式接管顶层 `utils` 别名，水面线和倒虹吸相关 UI 入口会先导入这个包；这样正式包运行时即使先碰到别的同名 `utils`，也不会再把项目自带工具包挡住。
  - 本轮只做“热修 + 防摔”，不改主窗口 eager 装配策略；这样先恢复 `1.2.1` 正常打开，避免在热修里再引入更大行为变化。
