# 当前进度

- 当前正在做什么：已完成普通纵断面与 xx管 的导出桩号精度拆分，并把 xx管 进出口外侧相邻 IP 点竖线修正为“保留下半段、截掉上半段”。
- 上次停在哪个位置：停在普通模式桩号默认保留几位小数、xx管 是否继续单独控制，以及相邻 IP 点竖线是否整条删除这几个口径还没完全收口的位置。本次直接相关回归结果为 93 通过、0 失败。
- 近期关键决定和原因：
  - 普通模式新增 `station_decimals`，默认 2 位，并同步影响普通纵断面、IP 表、合并 DXF 里的 IP 表和 bzzh2；这样同类导出桩号口径保持一致，但不改主界面的通用显示。
  - xx管 继续单独使用 `xxpipe_station_decimals` 与 `xxpipe_centerline_elev_decimals`；这样桩号与中心线高程都能各自调精度，不会反向影响普通模式。
  - 命名 `定向钻 / 顶管` 外侧相邻普通 IP 点继续保留下半段竖线，只截掉进入“建筑物名称”行的上半段；这样既保住下半段分隔，也不会把表头区域切碎。
