# 当前进度

- 当前正在做什么：已完成 `xx管` 连续承压链改造，并补齐窗口汇总、正式回写和配置窗口链级展示。
- 上次停在哪个位置：之前匿名普通有压管道已经能单独参与窗口计算，但同一条承压线路仍会被拆成多段，首行有压管道也会因为缺少上一普通行而失败。
- 近期关键决定和原因：
  - `xx管` 渠道级别下，连续出现的 `有压管道 / 定向钻 / 顶管 / 隧洞` 会被组织成一条连续承压链；链只负责连通关系和汇总，不改变各结构原有计算公式。
  - 连续承压链的首个成员如果正好是本流量段起点，就只当锚点使用，不再报“缺少上一普通行”；从后续成员开始按段写回各自行损失。
  - 结果持久化继续按成员保存，链总览只用于窗口展示，避免表格回写时重复计损。
  - 配置窗口改成优先按链展示：同链的有压成员继续保留原卡片，隧洞成员只显示只读说明，不暴露 R / D 输入。
