# 当前进度

- 当前正在做什么：已完成“合并DXF导出在支渠连续承压场景下误走 xx管 冲突校验”的补修，正在收口文档记录。
- 上次停在哪个位置：已确认图2点确定后弹图1，不是用户参数填错，而是“导出全部DXF”这条链路漏掉了承压节点裁切；已补回归测试并修正。
- 近期关键决定和原因：
- `支渠` 连续承压链里，链首单点命名普通有压如果到下一段 `定向钻 / 顶管 / 隧洞` 进口之间存在真实距离，就按“起点前缀段”计入沿程损失；因为这段距离真实存在，不能再当成纯定位点漏算。
- 这笔前缀段损失写回下一段特殊承压建筑的进口行，例如写回“大石包定向钻进口”那一行，而不是写回“苟家湾起点”行；这样表3里的落点和用户实际查看的位置一致。
- 只有当前缀长度无效或拿不到可用长度时，链首成员才回退为“起点锚点”；纯锚点不写回、不计损。
- 连续承压链里的重名成员统一加后缀显示，例如“前缀段 / 起点锚点 / 前段 / 中段 / 后段”；整条链只要还有真实子段失败，就标成“未完成”并隐藏整线总损失。
- “执行计算”前的检查不只跳过纯锚点，还要按实际回写模式识别链内命名特殊成员；像 `隧洞 / 定向钻 / 顶管` 这类已经按 row override 写回的成员，不能再按旧的出口行口径误判成“尚未执行水力计算”。
- 这次补修的真正根因是：有压结果虽然已经写回成功，但 `_recalculate_silent -> calculate_all -> _display_results` 过程中把 `group_mode / storage_key / display_name / target_row_index` 等 row override 元数据洗成了简化版本，后续执行前检查就把已完成结果当成未完成；现在统一保留完整 override 载荷，避免静默重算后误报。
- `支渠` 连续承压导出虽然复用了 `xx管` 的 5 项纵断面模式，但“导出全部DXF”里的纵断面部分也必须先裁成承压 route 节点，再做 `xx管` 结构校验；断面汇总和 IP 表仍可继续使用当前表3快照。否则整张表里的明渠会被误带进 `xx管模式仅允许...` 的冲突校验。
