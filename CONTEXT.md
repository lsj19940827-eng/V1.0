# 当前上下文

## 正在做什么

- 已完成明渠、渡槽、暗涵、隧洞界面断面图底宽 `B` 水平尺寸文字与箭头重叠修复。

## 上次停在哪

- 已将明渠、渡槽、暗涵、隧洞的单工况断面图改为一张图内叠加设计水位和加大水位。
- 共享断面图底座新增像素级回归测试，相关断面图测试已通过；后续继续保持多工况“每个成功工况一个小图”的口径。

## 关键决定

- 隧洞当前断面图风格作为统一参考，但不改变明渠、渡槽、暗涵各自的业务语义。
- 本次只统一界面 matplotlib 断面图：轮廓、水域填充、水位线、尺寸标注、标题、网格和视图范围共用一套底座。
- 只有 1 个成功工况时统一显示单图；设计水位保留水体填充和蓝色实线，加大水位只在同图叠加虚线、文字和 `h加大` 标注。
- 水平尺寸文字使用固定点数偏移放在箭头下方，避免小图里 `B` / `D` 等文字压到尺寸箭头。
- 不改变水力计算内核、输入表单、DXF 导出或 Word 导出规则；渡槽拉杆、倒角等专属信息仍由各自面板叠加。
