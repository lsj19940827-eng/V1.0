# 当前进度

- 当前正在做什么：取消倒虹吸底部“输出详细过程”勾选项，改为默认生成详细过程。
- 上次停在哪个位置：前端面板、批量工况兼容、PRD、前端 SRS 和操作说明已按计划更新，等待完整回归验证。
- 近期关键决定和原因：
- 保留 `show_detail` 历史兼容字段但统一写入/读取为 `True`，原因是旧工况里可能存在 `false`，不能再让它关闭详细过程。
- 采用“核心明细”方案：逐项说明平面转弯、纵断面转弯和通用构件的最终采用系数，并展开 `Σξ_local` 加法式，原因是便于复核且不显著拉长加大工况说明。
- 加大工况沿用设计工况步骤2的 `Σξ_local` 合计值，不重复输出同一组来源说明，原因是局部系数来源不随流量工况改变。
