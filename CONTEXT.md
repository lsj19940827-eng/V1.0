# 当前进度

- 当前正在做什么：已把水锤窗口优化和有压管道结果口径澄清合入 `master`，正在清理临时分支和 worktree。
- 上次停在哪个位置：两个分支的相关单元测试均已通过，合并冲突只出现在阶段记录文件并已整理。
- 近期关键决定和原因：
- 水锤窗口保留默认壁厚 `e=0.06m`、启闭时间 `Ts=300s`，整线分布基础采样步长为 `5m`，并新增“导出Excel / 验算原理”；原因是减少手填、便于复核，同时不自动替用户隐式验算。
- 有压管道公式不变；正式累计唯一来源是 `writeback_enabled=True` 的记录，原因是表3回写、链总损失和累计水损必须使用同一套逐段口径。
- `writeback_enabled=False` 的整组值和锚点记录只进入“参考结果”区，不进入表3、链总损失、顶部正式计数或累计水损；原因是避免江家坝这类同名整组值被误认为重复计入。
- 同名整组参考值与逐段合计可能不同，界面直接提示“计算范围不同，最终以正式计入逐段合计为准”。
