# 当前进度

- 当前正在做什么：实现 `1.3.0+` 用户优先补丁、补丁失败后自动完整包兜底的更新方案。
- 上次停在哪个位置：已完成发版策略、回补多基线、更新会话完整包兜底字段和安装助手自动兜底逻辑，相关文档已同步更新。
- 近期关键决定和原因：
- 补丁不再按固定 64MB 拦截，改为要求补丁明显小于完整包，同时保留删除文件数和总覆盖文件数上限，避免把接近全量的高风险补丁发给用户。
- 回补 patch 改为读取 `.release-snapshots/` 中从 `--min-version 1.3.0` 到目标版本前的全部正式基线，避免只覆盖单个旧版。
- `UpdateSession` 会记录完整包下载地址、SHA256 和大小；补丁安装失败且回滚安全时，`updater.py` 会自动下载完整包继续安装。
- 已新增回归覆盖：超过 64MB 但明显小于完整包的补丁允许发布、回补多基线来源版本、补丁失败自动完整包兜底、回滚失败时不继续兜底。
