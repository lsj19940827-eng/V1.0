# 当前进度

- 当前正在做什么：已把倒虹吸面板 `D` 实时显示、指定管径交互、工况确认态隔离与保存恢复整理进根目录 `master`，固定测试已跑完，正在做独立提交与工作树清理。
- 上次停在哪个位置：在 `master` 合并阶段，`panel.py` 与 PRD 还保留冲突标记，尚未完成最终验证与提交。
- 近期关键决定和原因：
- 倒虹吸保留 `master` 已有的“整宽流量条带 + 紧凑参数区”布局，以及“界面短提示 + 问号悬停完整说明”的加大流量口径；原因是这套布局已经解决窄宽度场景下的拥挤问题。
- `D` 的职责改成“当前输入下的即时结果”，只要 `Q / v / N` 可算就实时显示；确认态继续只服务于提示和计算前校验，避免用户第一次打开就看不到管径结果。
- 指定管径生效后，界面直接显示“采用D + 实际流速”，拟定流速框改为灰色只读；勾选前的拟定流速和确认态另存为 `v_before_override / v_confirmed_before_override`，这样取消指定或重开后仍能恢复。
- 工况确认态改为 `panel.py` 内存缓存按工况独立保存，只在本次运行内有效；重启后全部清空，避免把临时确认误当成长期确认。
- `D` 显示行固定拆成两行：第一行只放 `D设计 / 采用D`，第二行放括号里的理论值、每管Q或实际流速，避免窄界面下挤成一长串。
- 根目录 `main.py` 已完成启动冒烟，程序可正常拉起；固定 pytest 组合命令两边都显示 `12 passed`，但退出码仍保留旧工作树同样存在的 Windows 异常退出现象，因此本次只把它记为既有测试环境尾部问题，不把它归因到这次移植。
