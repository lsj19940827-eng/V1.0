# 当前进度

- 当前正在做什么：按 `patch` 流程发布 `v1.3.3`，纳入倒虹吸水位/渠顶修正、玻璃钢夹砂管水锤 E 修正和纵断面导出轻提示。
- 上次停在哪个位置：发版前状态已确认，相关测试 `121` 通过、`0` 失败，下一步执行 `tools/release.py patch` 正式发版。
- 近期关键决定和原因：
- 用户确认当前 `14` 个未提交改动全部纳入 `v1.3.3`。
- 发布说明使用朴素、面向用户能看懂的语言。
