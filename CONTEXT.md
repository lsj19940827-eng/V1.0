# 当前进度

- 当前正在做什么：`codex/siphon-auto-increase-fix` 已经并入 `master`，并完成主干回归测试、推送前检查和本地数据恢复。
- 上次停在哪个位置：本轮最后收口的是合并冲突中的 `CONTEXT.md`，以及合并后恢复的 `siphon_autosave.json / 工况1.siphon.json` 两份本地数据。
- 近期关键决定和原因：
- 基础水锤验算只做“直接关阀、全关”这一版，并列展示结果，不写入主水位和主损失递推链，避免把事故校核值混进正常工况计算。
- 水锤输入放在每条有压管道卡片内部，和现有平面 `R / D` 参数放在同一入口；`wall_thickness_m` 与 `water_hammer_basic` 单独持久化到 `PressurePipeManager`，保证弹窗重开可恢复，同时不污染表3正式承压覆盖口径。
- 表3回写后的静默刷新继续保持专用刷新口径，不改 `calculate_all()` 主链；这样既能保住真实节点已有的非零转角，也不会影响渐变段和普通进出口行的零转角规则。
