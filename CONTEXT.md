# 当前进度

- 当前正在做什么：正在收口“首次进入面板时加大流量输入两栏同时出现”的回归，已补测试并完成 5 个设计面板的初始化修复，当前停在文档同步与回归确认。
- 上次停在哪个位置：明渠、渡槽、隧洞、暗涵、倒虹吸、有压管道 6 个设计面板的“按比例 / 按Q加大”二选一输入已整体接通，但首屏初始化还漏了一步，导致明渠 / 渡槽 / 隧洞 / 暗涵 / 有压管道第一次打开时会把两栏一起显示。
- 近期关键决定和原因：
- 这次继续沿用各面板现有 `_load_case(...)` 路径做首屏同步，不额外发明新初始化逻辑；原因是切工况、清空和恢复工程本来就走这条路径，风险最小。
- 修复范围收口到明渠、渡槽、隧洞、暗涵、有压管道 5 个面板；倒虹吸不改，因为它启动时已经主动执行过一次可见性同步。
- 只修首屏显示，不改 `inc_mode / inc_q_text / inc_pct` 保存口径，也不改“按比例留空自动查表”和“按Q加大必须大于设计流量 Q”的既有规则。
