# 当前进度

- 当前正在做什么：修正倒虹吸结构段弹窗中弯管 `R/D0` 的管径口径。
- 上次停在哪个位置：代码和文档已改为使用当前实际采用管径，正在跑相关倒虹吸回归测试。
- 近期关键决定和原因：
- 普通模式弯管查表使用 `D设计`，指定管径模式使用 `采用D`，原因是理论管径只用于设计参考，不应参与实际弯管损失查表。
- 多管并联时结构段弹窗按每管流量传入管径上下文，原因是每根管的局部损失应按单管口径计算。
- 旧自动 `xi_calc` 可按当前采用管径重算，手工 `xi_user` 不覆盖，原因是修正历史自动值同时保留用户人工判断。
