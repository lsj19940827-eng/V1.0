# 当前上下文

## 正在做什么

- 正在准备 `1.3.7` 正式发版，目标从 `1.3.6` 做 `patch` 升级。
- 用户已确认采用本轮 7 条更新说明，并选择只纳入代码、文档、测试改动。

## 上次停在哪

- 已完成发版前只读检查：当前分支 `master`，当前版本 `1.3.6`，最新标签 `v1.3.6`。
- 已归纳并确认 `1.3.7` 更新说明，准备隔离临时文件后执行相关测试与发版脚本。

## 关键决定

- 本次发版说明覆盖断面图显示/放大、项目保存恢复、结果导航、多工况上限、批量结果恢复、公式渲染和 Windows 启动稳定性。
- 明显临时或成果文件不纳入发版提交，包括日志、项目样例、成果目录、未跟踪授权启动脚本。
- 正式发版仍使用 `.venv\Scripts\python.exe tools\release.py patch -m "<changelog>"`，不手工绕过发版脚本。
