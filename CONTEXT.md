# 当前进度

- 当前正在做什么：倒虹吸“空白几何 + 默认通用构件”改动已准备合入 master。
- 上次停在哪个位置：已恢复不纳入本次合并的配置/流程/数据删除项，并通过倒虹吸相关回归测试。
- 近期关键决定和原因：
- 普通启动和新建空工况保持空白几何，但结构段表立即显示 7 类通用构件，原因是让用户先看到完整构件清单。
- 新增 `common_defaults_initialized` 标记，原因是旧工况可迁移补齐一次，同时尊重用户之后删除某类默认构件。
- 只有通用构件且没有平面/纵断面几何时不直接计算，原因是避免用户误以为已经具备管身几何。
- 本次合并不删除 `.claude`、`.windsurf` 和默认/自动保存数据文件，原因是避免影响后续发版流程和现有数据。
