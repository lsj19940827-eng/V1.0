# 当前进度

- 当前正在做什么：已修复压力管道特性表在多流量段场景下的主长度口径错位问题，并收口到图2/断面汇总/Excel/DXF同源链路。
- 上次停在哪个位置：问题定位到 `cad_tools.py` 仍优先按原始分组 `segment_start_mc / segment_end_mc` 拼主长度；这会漏掉“普通有压段接命名建筑物前”的短段，也会把跨流量段边界误并进下一段。
- 近期关键决定和原因：
- `xx渠` 不再一律排除整线；只有在末端或跨流量段形成连续承压线时，才补 `route_key / route_display_name / route_ip_points`，非连续场景继续只显示当前分组。
- `WaterProfilePanel` 里 route 分段组装继续保留类级 helper 修复；这样点击“开始计算”不会再报 `group` 参数缺失。
- 整线只作为底层管理口径；压力管道特性表、统计摘要和结果回写继续按原有分段和流量段表达，避免把既有导出口径一起改掉。
- `xx渠` 的压力管道特性表里，隧洞只在同一流量段已经进入“有压类结构（有压管道 / 定向钻 / 顶管）”之后才统计；这样前置隧洞不会误进有压管道特性表，同时 `xx管` 原有整线统计口径不受影响。
- 纵断面 DXF 现在统一先按桩号递增方向规范化，再计算导入偏移；这样即使用户把轴线重画成反方向，`xx管` 和倒虹吸两条导入链路也不会把整线桩号整体算反。
- 压力管道特性表里的“渠首水位 / 渠末水位”现在改成按各流量段自己的首个有效水位和最后一个有效水位输出；没有有效水位的流量段保持空值，不再回退到整线水位。
- 压力管道特性表主列长度现在优先使用流量段连续桩号累计值；分组 `segment_start_mc / segment_end_mc` 只保留给原始分组范围和兜底口径，避免主长度漏段或串段。
- 匿名普通有压段若上游参考点已经跨到别的流量段，就从当前目标行自身起桩；这样不会再把上一流量段尾点到本段首点的边界距离算进本段。
