# 当前上下文

## 正在做什么

- 正在收尾断面图与工况标签布局优化，并把确认有效的代码合入 `master`。
- 本轮只纳入界面展示、单元测试和相关 PRD；运行日志、本地项目文件和虹吸自动保存数据不纳入提交。

## 上次停在哪

- 相关断面图回归测试已通过：明渠、渡槽、暗涵保持居中锚点；隧洞继续使用顶对齐。
- 完整测试套件存在主干已有失败和本机 pytest 捕获异常，本轮用干净 `master` 对照确认这些不是本次改动引入。

## 关键决定

- “每行 3 个”只作为左侧工况标签目标，不用于右侧断面图。
- 右侧断面图继续宽屏默认 2 列、窄窗口 1 列；明渠、渡槽、暗涵保持居中锚点，隧洞专用顶对齐。
- 隧洞 9 工况断面图目标保持 2 列、5 行、2600px 画布高度；标题裁切修复不改变隧洞 520px 专用行高和可读宽度。
- `codex/1` 已是 `master` 历史的一部分，合并提交后可删除本地分支；远端旧分支暂不清理。
