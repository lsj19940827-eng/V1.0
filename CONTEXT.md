# 当前进度

- 当前正在做什么：已把主干和 3 条附属支线的未提交草稿全部保存提交并并回 `master`，附属 worktree 与本地分支也已清理，只保留主干。
- 上次停在哪个位置：最后卡在 `panel.py`、`water_profile_dialogs.py`、`pressure_pipe_extractor.py` 以及项目记录的合并冲突；现已整理完成，相关回归结果为 81 通过、0 失败。
- 近期关键决定和原因：
  - 普通有压管道/定向钻/顶管的渐变段长度正式计算，统一按相邻渠道设计水深的 `5h/6h` 处理，避免插入阶段和详情弹窗各用一套口径。
  - xx管 下连续“有压管道/定向钻/顶管/隧洞*”会先识别成整线，再把每个子段自己的起止桩号挂到 group 上，避免整线导入后只能按单段重复维护纵断面。
  - 整线纵断面改按 `route_key` 持久化，空间长度、空间总损失、导出和中心线高程都先找整线数据，再按 `segment_start_mc / segment_end_mc` 裁切子段，减少匿名子段撞键和跨段误算。
  - `xx管` 渠道级别下，连续出现的 `有压管道 / 定向钻 / 顶管 / 隧洞` 会被组织成一条连续承压链；链总览只用于窗口展示，正式结果仍按成员分别回写，避免重复计损。
  - 本轮已删除全部附属 worktree 和本地分支，只保留 `master`，减少后续误操作。
