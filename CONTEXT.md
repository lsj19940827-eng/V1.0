# 当前进度

- 当前正在做什么：已完成“渠道糙率弹窗时机与候选过滤调整计划”的主线代码、测试和 PRD 同步，当前停在验证通过后的结果整理。
- 上次停在哪个位置：`water_profile/panel.py` 已改成“同步到表3前先确认”，`tests/test_water_profile_coord_precision_unit.py` 与相关回归测试已通过，下一步如有新需求可直接在此基础上继续扩展。
- 近期关键决定和原因：
- 渠道糙率候选白名单收口为 `明渠 / 渡槽 / 隧洞 / 暗涵`；倒虹吸、有压管道、闸类和其他非渠道结构不进入候选，避免非渠道 `n` 干扰顶部统一值。
- 弹窗时机前移到“同步到表3前先确认”；候选一致时不弹窗，候选不一致时才让用户决定本次导入写入什么值。
- 这次统一选择只影响表3和后续水面线计算，不回头改已经完成的表1断面结果。
