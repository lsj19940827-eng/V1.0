# 当前进度

- 当前正在做什么：修正 `支渠` 压力管道特性表在前置/末尾隧洞场景下的长度统计口径，并同步相关文档。
- 上次停在哪个位置：已完成 `panel.py` 摘要逻辑和 `test_pressurized_dxf_rules_unit.py` 回归测试，正在收口 README/ARCHITECTURE/CONTEXT。
- 近期关键决定和原因：
- `支管` 继续沿用原有整线口径，不改前置隧洞统计。
- `支渠` 只统计夹在 `有压管道 / 定向钻 / 顶管` 中间的隧洞；前置隧洞和末尾隧洞都不进入主长度或隧洞栏，避免同一张表出现两套长度口径。
