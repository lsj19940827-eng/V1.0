# 当前进度

- 当前正在做什么：已把 xx管 的“整线几何 + 分段损失”主链接通，整线纵断面改按 `route_key` 管理，计算和导出都会按 `segment_start_mc / segment_end_mc` 裁切子段。
- 上次停在哪个位置：整线卡、route 持久化、子段裁切、匿名段导出 identity 和 route 回写保护都已落地；本次直接相关验证结果为 47 通过、0 失败。
- 近期关键决定和原因：
  - xx管 下同一条连续“有压管道/定向钻/顶管/隧洞*”现在会先识别成整线，再把每个子段自己的起止桩号挂到 group 上，避免整线导入后只能按单段重复维护纵断面。
  - 有压管道窗口改成“上方整线卡 + 下方分段卡”：DXF 导入、清空、平面/纵断面预览只在整线卡处理，分段卡只负责本段 R/D 和结果应用，界面和计算口径保持一致。
  - 整线纵断面改按 `route_key` 存到 manager 的 `routes` 桶里；`set_result` 在调用方漏传 route 信息时也会保留旧绑定，避免一次计算把整线关联写丢。
  - 空间长度和空间总损失不再直接吃整份整线纵断面，而是先按 `segment_start_mc / segment_end_mc` 裁切；覆盖不足时回退到原平面长度，并把原因写进结果备注，避免把中间或开头的隧洞长度误算进有压管段。
  - xx管 导出和中心线高程链路继续按子段 identity 分段，但纵断面来源先走 route；匿名子段优先使用 `pressure_pipe_row_identity`，避免同一流量段多个空名称子段撞键。
