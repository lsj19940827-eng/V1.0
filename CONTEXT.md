# 当前进度

- 当前正在做什么：修复标准纵断面里“中间 `IP0` 竖线穿进顶部两行”和“坡降文字相对建筑物名称偏右”的版式问题，代码与回归已完成，当前停在文档同步与最终复核。
- 上次停在哪个位置：已经完成 `cad_tools.py` 的标准纵断面边界修正与回归测试补齐，当前停在 PRD/CONTEXT 同步和最终结果确认。
- 近期关键决定和原因：
- 标准纵断面顶部两行的 full-height 竖线改为由“真实会切开顶部单元格的边界节点 + 末列右边框”驱动；原因是像暗涵中间 `IP0` 这种图面仍显示为普通 `IP` 的节点，不应再仅凭原始 `进/出` 标记切开顶部两行。
- 坡降段的起点与中点继续保留普通明渠的按坡降变化拆分口径，但特殊建筑边界改为沿用建筑物显示段；原因是要修正 `1/4000` 落到相邻单元格中心的问题，同时不破坏已有多坡降场景。
- `IP点名称` 的用户可见文本本次保持不变，只修竖线和定位逻辑；原因是问题根源在版式边界，不在显示文案，改文案会扩大回归范围。
