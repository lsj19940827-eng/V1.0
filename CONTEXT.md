# 当前上下文

- 2026-09-05：用户已明确确认向上述 GitHub/Gitee 仓库发布本次代码与安装包，并更新指定正式 Gist；此前审批缺少具体地址授权的问题已补齐。现执行标准 `tools/release.py minor --no-bump`，目标固定V1.4.0，沿用已审定五条说明，继续排除本地规范PDF/OCR及恢复码。

- 2026-09-05：V1.4.0 本地完整包与通用补丁已构建、校验通过，尚未正式发布。395项定向回归通过；完整包378896072字节、SHA-256 `5508f6b77983c5ef54cbe77610bd4a2d7dbe518c7ad0ed875565299fdf72e640`，补丁92063029字节、SHA-256 `e09f42f5d143749b0d6617639d05e6643bd9fff6d1c0fa149a7725640a93fe8c`，支持V1.3.0+。两包CRC通过，新增13个关键冻结模块齐全，未含规范PDF；全新解压隐藏启动已确认V1.4.0主窗口句柄及消息响应，验收窗口已正常关闭，未做可见界面逐项操作。
- 本次首次构建在系统内存不足期间加载ezdxf的隔离子进程崩溃；数值库线程限制为1后本地重试成功，不改产品计算代码。后续正式脚本执行时保留 `OPENBLAS_NUM_THREADS=1`、`OMP_NUM_THREADS=1`、`MKL_NUM_THREADS=1`、`NUMEXPR_NUM_THREADS=1`。当前version.py已为1.4.0，正式发布须运行项目虚拟环境 `tools/release.py minor --no-bump -m <已审定说明>`，不要再递增。
- 正式发布命令被自动审批拒绝，未执行提交/tag/推送/上传/Gist更新；审批要求用户明确授权本次本地改动发往 GitHub `lsj19940827-eng/V1.0`、Gitee `pig-farming-pays-off-as-a-dog/canal-update`，并更新Gist `5b065a668e99faddcec02415cb423d96`。当前待用户确认具体地址；说明保存在 `.release-work-140/changelog.txt`，构建及验收日志在同目录（已忽略）。规范PDF/OCR及恢复码按用户要求保留本地，不纳入提交和安装包。

- 2026-09-05：用户已确认 V1.4.0 五条更新说明并要求继续正式发布；随后明确规范资料不纳入发布。已忽略 `docs/规范资料/`、账户恢复码及本轮临时产物，撤回临时 Git LFS 配置，规范文件未上传。正常权限下定向回归 395 项全部通过（管材目录、选型与结果、无压对比及输出、公式和报告、构建补丁与发布快照）；此前沙箱测试临时目录错误不再复现。GitHub/Gitee 凭据验证通过，现准备使用标准 release.py minor 发布，说明采用用户审定稿。

- 2026-09-05：准备 V1.4.0 发版，用户已授权全部改动纳入，但要求先审阅简短更新说明；当前 master、版本1.3.11，拟用 minor。已核对流程及差异，未改版本、未提交、未打包或发布。已在 .gitignore 排除 github-recovery-codes.txt，保留本地文件。首次按 pressure_pipe/build 等宽泛文件名前缀跑回归出现 E/F，约92%时进程退出1，期间水锤原理对话框的 WebEngine 报 Windows 0x80000003；无完整汇总，不能称验证通过，后续须定向分组核对。另有3份未跟踪规范PDF超过100MB，正式推送前需处理大文件纳入方式。下一步先等用户审阅更新说明，再完成验证及正式发版。

- 2026-09-05：按用户要求打开移除无压对比结果页后的源码程序，已核验新启动进程及子进程；桌面主窗口“渠系建筑物水力计算系统 V1.3.11”句柄非零且响应正常，保留供用户试用。本轮仅启动并核验。

- 2026-09-05：按最新要求删除整个“无压输水能力对比”结果页及两个专用前端模块，结果区只保留计算结果/批量日志；保留图2四项CSV、图表PDF、合并PDF、PNG输出与无压输入/后台计算。完成后停留日志并列文件路径，旧项目快照兼容保留、旧第三页索引回落日志。106项定向回归通过，含真实CSV/PDF/PNG/合并输出；实际Qt面板截图确认页签移除和输出项保留。PRD V2.13.22、README及架构同步；当前运行的旧窗口需重启才能看到，未打包发版。

- 2026-09-05：按用户要求打开包含无压选型汇总的新源码程序，桌面主窗口“新项目 * - 渠系建筑物水力计算系统 V1.3.11”已出现、窗口句柄非零且响应正常，保留供用户试用；本轮仅启动并核验。

- 2026-09-05：已采纳并完成无压选型改版：逐底坡最小可用已扫描规格汇总、设计/双工况及底坡筛选、可用管径状态、失败规格保留、交叉表与明细跳转、无结果上限原因和返回批量参数、浏览状态保存恢复。独立快照筛选区分模型可解/所设净空满足/待核查，不放宽条件；默认图表也改按无压可用范围选径。110项定向回归通过；真实PE扫描及正常/150%缩放Qt截图目视通过，交叉表首开定位已验证。PRD V2.13.21、README及架构同步；源码已完成，重启并载入已有结果或重新批量计算可用，未打包发布。

- 2026-09-05：无压对比选型体验建议：现默认管径沿用有压经济/妥协分类，下拉缺少无压可用状态。建议以按底坡汇总最小满足条件的已扫描规格为主入口，配合可用管径筛选及直达明细；区分仅模型可解、满足所设净空、设计/加大工况，未指定底坡时不将任意一坡可用称为整体推荐。仅提出方案，尚未修改选型逻辑。

- 2026-09-05：修复无压对比图底坡刻度重叠、PE长规格标题裁切；按实际画布及字体宽度换行、抽稀刻度并保留首尾，窗口缩放和PDF/PNG导出共用。计算数据不变；36项定向回归通过，700×650真实Qt画布截图目视通过，覆盖窄窗口/150%缩放的边界检查。PRD V2.13.20同步；重启源码后重新显示图表生效，未打包发布。

- 2026-09-05：按截图删除项目净空条件下方“这是项目自定判据，预填值不代表本工程适用规范；”，保留“可留空一项。”；PRD V2.13.19同步，仅修改提示文案。

- 2026-09-05：截图“单侧内衬厚”答疑：核对钢管控件及尺寸计算，指用户指定的径向内衬占用，0仅表示本次不计；SL/T 281-2020第8.1.1条的6 mm为钢板构造最小壁厚，第9.1.1条要求按工况选防腐材料。PCCPE管芯按GB/T 19685-2017第3.1.3条及公称内径口径，不应重复扣内侧混凝土；截图同时显示PCCPE及钢管字段，当前源码限定钢管显示，具体运行界面原因未排查。本轮只答疑，未改代码。

- 2026-09-05：规格卡统一为“造价 / 采购规格”，删除“产品目录口径”“公称外径口径”；帮助、日志、输入校验及Word同步简化术语，尺寸说明保留。PRD V2.13.18同步；本轮仅文案调整，70项定向回归通过；重启源码并重算后可见，未打包发布。

- 2026-09-05：按用户要求打开采用双工况样式的源码程序；正常桌面主窗口“渠系建筑物水力计算系统 V1.3.11”已出现，窗口句柄非零且响应正常，保留供用户试算。本轮仅启动并核验，未修改代码。

- 2026-09-05：已按用户“采纳”将双工况样式接入有压管道真实结果页：设计/加大流量、流速、各项水损并列，类别注明设计流速依据，候选表补加大流速并精简损失列；关闭加大仅列设计工况，兼容零比例、指定规格、球墨铸铁上下限和保存恢复。原选径、排序、计算快照及Word/TXT/CSV不变；189项定向回归通过，真实显示入口HTML完成桌面浏览器视觉核验。PRD V2.13.17及架构、README同步；重启源码并重算可见，未打包发布。

- 2026-09-05：按截图复核有压管道加大流量开关，PCCPE、设计流量2 m3/s、内径1400 mm、管长1000 m、局部比例0.15，关闭/自动加大25%均推荐DN1400；总水损分别1.331785/2.080915 m/km，与截图一致。现逻辑流速及经济区判断采用设计流量，水损采用加大流量；首屏未标明两者工况且未列加大流速1.624030 m/s，易误读为开关无效。本轮仅诊断并更新记录，未修改计算及界面代码。

- 2026-09-05：按用户授权完成无压对比整体改版：仅标准9档/自定义，网格展示、批量编辑/范围生成/撤销清空和输入保留；新增可选项目净空条件。结果页支持管材/流量/管径筛选，设计及加大流量分别对齐有压参照；新增完整明细CSV及三联图PDF/PNG。无压改用有界浅水深解，分开能力不足与求解失败，修复近邻流量图表覆盖。223项定向回归通过；正常/150%缩放Qt截图及新PDF渲染目视通过，PRD V2.13.16、README和架构已同步。源码已完成，重启程序后重新批量计算可用；未打包发布。

- 2026-09-05：按用户要求重新打开精简结果后的源码程序；新主窗口“渠系建筑物水力计算系统 V1.3.11”已建立、句柄非零且响应正常，保留供用户试算。此次仅启动并核对窗口，未修改代码。

- 2026-09-05：有压管道结果精简重复说明：当前规格内径及壁厚只完整换算一次，其他候选独立保留；钢管选径复核不重复扣壁厚，详细过程省去重复尺寸推导、末尾汇总和自动对比。首屏突出推荐/指定DN；输入、流量/流速/水损、双限值、筛选及必要标记保留，原始快照和Word不改，未知历史格式原样展示。118项相关检查通过；钢管、DI、PE及指定钢管已用实际显示入口完成离屏浏览器视觉核验，1000×760首屏均完整显示5条候选。PRD V2.13.15及架构已同步；重启源码重算查看。

- 2026-09-05：有压管道结果重排为摘要、候选管径对比、规格/流量/选径及尺寸说明、详细计算过程；各档换算移出候选表并按原顺序独立展示，计算及排序未改。相关103项回归通过；Qt浏览器离屏软件渲染截图已核验，1000×760视区未滚动即完整显示5条候选（表顶约154px、首行底约235px）。PRD V2.13.14已同步；源码重启后重新计算可见。

- 2026-09-05：新增GB/T 13295-2026两册OCR Markdown已从根目录移入docs/规范资料/Markdown/球墨铸铁管，保留原文件名并在索引与对应两册PDF配对；内容、编码、大小和修改时间保持，SHA-256核验一致。索引与归档清单同步，当前资料库实存19份PDF、18份Markdown，共37份；归档清单保留39条历史记录。

- 2026-09-05：根目录4份球墨铸铁管标准PDF和1份2019版OCR Markdown已归入docs/规范资料对应PDF或Markdown/球墨铸铁管目录，索引与归档清单同步；5份文件的SHA-256、大小和修改时间校验一致，资料库共20份PDF、17份Markdown。两册拆分PDF已解除占用；按用户要求，核对与归档副本SHA-256一致后，已删除根目录两份重复文件，归档副本完整保留。

- 2026-09-05：GB∕T 13295-2026.pdf 已按原始页序拆分为两册，第1册为PDF第1—58页，第2册为第59—116页；两册均小于200 MB，原件保留，页数、页面内容流及原文件哈希校验通过。

## 正在做什么

- 2026-09-05：最终推荐/指定结果改为加粗标题优先显示DN公称规格，球墨铸铁示例为DN 800，并分别列出等级C25、插口外径842 mm、单侧壁厚9.6 mm、内衬6 mm与准确水力内径810.8 mm。PE/PCCP/玻璃钢/钢管及Word结果汇总同步，旧无产品规格结果不虚构DN。计算规则不变；125项定向回归通过，真实计算结果经离屏浏览器截图核验，内径公式完整显示。PRD V2.13.13同步；当前已打开的旧进程需重启并重新计算。

- 2026-09-05：已按用户要求打开包含下标修复的源码主程序；普通Windows桌面主窗口“渠系建筑物水力计算系统 V1.3.11”已建立，窗口句柄非零且响应正常。新启动进程及子进程命令行已核对，窗口保留供用户试算；本轮未改代码。

- 2026-09-05：修复计算过程多字符下标仅首字符下沉，共用公式转换器完整分组nom等字母/数字下标，兼容已有过程中的di/dn/en连写，并调整毫米单位间距；有压管道摘要内径下标、Word管壁符号同步。计算值不变，DN800/C25仍为810.8 mm。相关89项回归通过，实际公式SVG转图及下标字号/基线检查通过；离屏浏览器受访问限制，未完成整页浏览器视觉验收。PRD V2.13.12已同步；重启源码程序刷新结果后生效。

- 2026-09-05 GB/T17457-2019 PDF与OCR已补齐；用户解除占用后，两份资料完成规范目录归档，核对SHA-256一致再清理根目录重复副本。原件PDF第5页（印刷3页）5.1及表1确认DN800内衬公称6.0 mm、最小3.5 mm，六档公称厚度与程序32档DN一致，842/9.6/6/810.8 mm尺寸链依据已齐。程序仅补充内衬来源为第5.1条、表1，不改变算法及数值；PRD V2.13.11、规范索引/归档清单/核验报告同步。目录、集成、面板72项回归通过，资料链接检查通过；当前20 PDF+19 Markdown，41条历史归档记录。

- 2026-09-05新上传GB/T 13295-2026完整PDF与两册已补全《水及燃气用球墨铸铁管、管件和附件》名称，三份哈希/大小/修改时间一致，页数116/58/58。按新原件PDF15页（印刷9页）核对内径定义及公式、32页（印刷26页）表16核对DN800/DE842/C25/壁厚9.6；19页明确内衬按GB/T17457，6 mm仍需配套原件核验。810.8 mm仅为该壁厚及6 mm内衬组合的理论值。索引、归档清单、规范核验报告已同步；旧低清PDF/OCR检查时已缺失，当前实存19 PDF+16 Markdown。未改代码。

- 2026-09-05采购与过流能力答疑：截图采购标识为DN800/C25球墨铸铁管并配套内衬要求，810.8 mm为理论计算净内径。复核设计流量0.5 m3/s流速0.9684 m/s，加大流量0.65 m3/s流速1.2589 m/s；截图总水损2.5858～3.0393 m/km已采用加大流量，均满足当前程序筛选条件，实际运行仍以可用水头和承压校核为条件。本轮未改代码。

- 2026-09-05球墨铸铁管内径答疑：核对本地GB/T 13295-2026 PDF第14页（印刷9页）4.2.2.2.1及表5、表6，公称内径与水力设计理论内径是不同取值；DN800/C25按外径842、管壁9.6、内衬6 mm换算为810.8 mm，大于公称尺寸本身不构成错误。程序换算逻辑一致，外径及壁厚另经厂家公开尺寸表交叉核对；内衬6 mm仍为现有目录取值，本轮未补齐GB/T 17457原件。仅答疑并更新记录，未改代码。

- 2026-09-05按用户要求启动更新后的源码主程序：普通Windows桌面主窗口“渠系建筑物水力计算系统 V1.3.11”已建立且响应正常；已核对新启动进程的父子关系和main.py命令行。窗口保留供用户试算钢管外径及查看选径过程，本轮仅启动并核对窗口状态，未改代码。

- 2026-09-05展示确认：已再次核对当前结果页调用，钢管“自动选径依据：从最小水力内径到公称外径”直接位于规格卡片下方，逐项展示水力反算、补厚、整百上取及净内径复核公式，不依赖“输出详细计算过程”勾选；Word也调用同一快照的公式说明。旧结果需重算后生成该过程，本轮未改代码。

- 2026-09-05钢管最新决定：统一输入/推荐公称外径；先反算满足流速和水损上限的最小净内径，补内衬及SL/T 281-2020构造最小壁厚，外径按用户指定的100mm整数倍上取并复核。取消界面内外径切换及候选编辑，旧内径等效转外径保留原净空，旧结果待重算；其他管材保持规范目录。PRD V2.13.10、README与架构已同步。
- 当前实现和13个相关测试文件273项回归已完成，含48项钢管检查；最终展示调整后69项定向复测通过。真实输入控件及自动、手动、低流量参考结果页已完成视觉核验，Word生成、内容和可编辑公式已验证（未逐页渲染）；源码已更新，重启后可试用。规范资料仍为16份PDF和16份Markdown，钢管来源原件保持；本功能为构造最小壁厚水力预选，结构验算另行完成。

- 2026-09-05壁厚答疑：直接复核新增PE原件PDF第11页（印刷6页）表3，DN800/SDR17表列公称壁厚47.4 mm；同时查看球墨铸铁表16、PCCP表2和FRPM第6.2.3条原件。明确PCCP规范已有最小管芯、保护层及钢筒厚度，当前程序仅接入公称内径，不能把未接入说成规范未规定；FRPM管壁设计厚度由生产企业设计并在技术文件中给出。球墨铸铁内衬仍保留GB/T 17457-2019原文缺档、公开标准转引的证据边界。本轮只核对答疑，未改代码。

- 2026-09-05：有压管道结果新增“公称直径、壁厚与水力内径”说明，推荐/指定规格和每档候选均展示尺寸取值、公式及数值代入，Word同步可编辑公式和逐档换算表。PE按查表壁厚扣两侧管壁，球墨铸铁同时扣内衬，PCCP/玻璃钢内径系列明确直接取公称内径。保留旧结果快照及简洁输入区；PRD更新至V2.13.8。结果页、项目恢复和报告相关51项检查通过，四类管材真实结果HTML已完成浏览器视觉核验；待用户重启源码主程序后试用。

- 2026-09-05：按用户要求打开DN统一和“规范推荐”改动后的源码主程序。普通Windows桌面已确认“渠系建筑物水力计算系统 V1.3.11”主窗口句柄非零且响应正常，保留窗口供用户查看；本轮仅启动程序。

- 2026-09-05补充：按用户最新要求，公称管径在输入、提示、结果、Word和批量CSV中统一显示DN；PE仍采用公称外径数值，PCCP仍采用公称内径数值，内部字段及既有规格ID保持不变。球墨铸铁管等级由“程序推荐”改为“规范推荐”，占位提示改为“留空按规范推荐”。PRD同步至V2.13.7；相关回归156项通过。

- 2026-09-05：有压管道选径界面已按用户反馈简化。PE 输入标签改用中文“公称外径”，单次/批量说明只保留材料、压力、SDR自动匹配和选径操作；帮助页改为“PE 管怎么选管径”，删除原公式框、符号列表、两段黄色说明和相关工程提醒。球墨铸铁管等级改为“程序推荐”，输入提示与批量日志改用中文管材名。PRD同步至V2.13.6。
- 单次和批量的规范选径勾选框已移除，新工况和界面批量固定按规范选径；旧工况保留原内径，仅显示“按规范重新选径”迁移按钮，点击前先保存当前编辑值。相关回归82项通过，补充旧内径编辑场景并修正批量说明后，面板35项重跑通过。真实输入控件的离屏截图已核验；帮助页文案检查通过，浏览器因本地文件URL策略拒绝预览，未完成该页的浏览器视觉检查。本轮未打包、提交或发布。

- 2026-09-05：按用户要求启动当前源码主程序；普通桌面环境已确认“渠系建筑物水力计算系统 V1.3.11”主窗口句柄非零、Responding=true，保留窗口供用户试用。清理了本轮首次受限启动的无窗口进程；未修改程序代码。

- 2026-09-05补充：用户新复制的PE第1、2、3、5部分PDF已归档至`docs/规范资料/PDF/PE管/`，与既有Markdown逐份配对，4份移动前后哈希一致。资料总数现为PDF15份、Markdown15份；PE原件缺档已解除，GB/T 17457-2019全文仍缺档。本次仅归类和更新索引，未重做PE逐页规范核验。

- 2026-09-05：完成有压管道规格与规范配套核验；新增独立原表快照，PE/DI/PCCP/FRPM共675次全规格指定计算，目录、面板、恢复、批量、报告和加大流量回归242项通过，另验混合CSV141行。修复非有限流量、管长、局部比例等仍能生成规格结果的漏洞，单次与批量入口均明确拦截。
- 规范资料已归档至`docs/规范资料/`，PDF11份、Markdown15份按6类专业分开存放，26份哈希一致；索引与原路径清单齐全。详见`docs/有压管道规范配套核验.md`，PRD更新到V2.13.5。PE原件PDF、GB/T 17457-2019全文仍缺档；结论限于尺寸目录与水力链，未声称完成承压、结构及完整采购选型。本轮未打包、提交或发布。

- 2026-09-04：有压管道独立设计在既有 PE 目录之外，新增球墨铸铁管、PCCP 管和玻璃钢夹砂管产品规格目录；新工况默认启用规范目录，普通钢筋混凝土管不纳入本模块，钢管继续保持既有水力内径入口。
- 2026-09-05：按用户明确指定，球墨铸铁管目录切换为本地 GB/T 13295-2026 表16、表C.1，采用 DN40～DN3000 的32档及 C20/C25/C30/C40/C50/C64/C100 非空单元格；2027-03-01 是实施日元信息，不作为本项目采用开关。配套内衬按 GB/T 17457-2019 的4/5/6/9/12/15 mm分段，仍按 $d_i=DE-2(e_{\mathrm{nom}}+e_c)$ 换算名义内径。水利导则仅补充工程适用边界，不再截断新版产品系列。
- 新版已删除 K 壁厚分级；旧 K 工况保持原等级并提示主动重选，新计算规格ID含2026版本，旧结果和报告保留快照标准，不自动改写成新版。PRD已同步至V2.13.4；本轮目录、计算、项目恢复和报告定向回归 `192 passed`，DN3000新版输入区已完成带系统字体的离屏截图核验；未打包、提交或发布。
- PCCP 按现行 SL 702-2015 与 GB/T 19685-2017 区分 PCCPL、PCCPE，新增工况默认 PCCPE；产品型式与历史 `n=0.013/0.014/0.015` 摩阻预设相互独立，公称内径 $D_0$ 作为名义水力内径。
- 玻璃钢夹砂管按现行 GB/T 21238-2016，仅以内径基准系列自动推荐；外径基准系列保留为查阅信息，不在缺少厂家壁厚时反推水力内径。旧项目若没有目录版本标记，继续按原水力内径解释，不静默改成公称尺寸。
- 本轮收尾修正了旧 DI/PCCP/玻璃钢夹砂管重新启用目录时的安全迁移、PE 与通用产品壁厚 CSV 字段分离、FRPM 管端内径范围和设计值偏差的一级成果展示，以及非 PE 报告固定引用 PE 标准的问题；目录、内核、面板、项目恢复、批量和报告定向回归共 `173 passed`。
- 2026-09-04：PE 指定公称外径新增实时规范校验。合法 $d_n$ 立即显示对应 $e_n$、$d_i$；非规范输入显示附近四个有效外径、相邻上下级，并只提供需用户主动点击的“采用上邻规格”按钮，禁止静默替换和默认向下取整。PE100、PN 1.0 MPa 输入 600 mm 时提示 500/560/630/710 mm，建议从 630 mm 重新做水力校核；计算错误路径同步给出相同附近规格。
- 2026-09-04：PE 单次输入区新增可见的动态“规范学习提示”和 PE 等级、PN、公称外径悬停说明；随 PE80/PE100、PN/SDR 及手动 $d_n$ 切换同步解释 MRS、20 ℃ 与 $C=1.25$ 条件、$d_n/e_n/d_i$ 分工，以及“留空自动推荐 / 输入标准公称外径直接计算水损”两种模式。批量 PE 设置区同步显示当前 MRS、PN、SDR 和压力复核边界。
- 默认 PE100、PN 1.0 MPa 组合有 31 个有效规范规格；本机连续执行 2000 次自动推荐平均约 `0.212 ms/次`，规范规格逐项评价对交互速度无可感知影响。新增规范学习和邻近规格提示后，PE 目录、内核、面板、批量、项目恢复、报告和加大流量相关完整回归为 `152 passed`。
- 2026-09-04：有压管道独立设计已补齐 PE 规范产品规格链：PE100/PE80 按 PN 唯一映射 SDR，公称外径 $d_n$ 用于采购造价，GB/T 13663.2-2018 表 3 公称壁厚 $e_n$ 与名义计算内径 $d_i=d_n-2e_n$ 同步进入界面、计算书和批量 CSV。
- PE 自动选型只扫描当前组合的标准离散规格并推荐最小合规 $d_n$；手动指定只接受该组合合法 $d_n$。非 PE 管材、旧工程和显式旧批量直径序列继续保留原水力内径语义。
- 规格目录包含表 2 的 36 个离散公称外径和表 3 的 215 个有效壁厚单元格；对 $d_n=1000/1200\,\mathrm{mm}$ 附近三个与理论商值不同的表列值已再次查看标准 PDF 原表影像，程序保持逐格表值，不用 $d_n/SDR$ 覆盖。
- 旧 PE 项目会先逐工况保留历史水力内径，再安全上取到 $d_i$ 不小于旧值的首个标准规格，并在界面和计算书提示迁移；多工况恢复、复制到全部和从上一工况复制均已覆盖。界面统一显示“聚乙烯（PE）管”，批量 CSV 继续保留稳定值 `HDPE管`。
- Word 仅在当前成果确实包含 PE 产品规格元数据时加入 GB/T 13663.1/13663.2，已去除重复引用；非 PE 成果和仅恢复的旧任意内径成果不声称已经完成 PE 产品规格选型。
- 本轮 PE 规格目录、内核推荐、批量 CSV、旧项目恢复、面板、计算书元数据、既有有压管道与加大流量相关定向回归共 `147 passed`；四个运行模块编译检查和差异格式检查通过。
- 当前 PE 压力等级映射边界为 20 ℃、设计系数 $C=1.25$；实际温度折减、水击压力和制造公差仍须另行复核，本期不自动联动既有水锤允许压力。
- 2026-09-02：按用户要求打开主程序。受限环境首轮启动未成功；改在普通 Windows 桌面用项目虚拟环境 pythonw 启动 main.py 后，已确认 V1.3.11 主窗口持续响应，并切到前台、截图核实界面正常。
- 本轮完成“有压管道设计”三项修复：球墨铸铁管对同一候选管径同时列出 `f=223200 / 189900` 的沿程、局部、总水损及管长折算结果；推荐、类别和既有图表仍按上限值，结果摘要、候选表、详细过程、Word 与批量 CSV 均可供用户自行取用。
- 批量计算日志改为从任务启动起实时追加准备、计算、保存、取消和完成消息，不再等任务结束后一次性回填；相邻重复进度会去重。
- 沿程水损详细公式改为明确分组的 `(Q')^{m} / d^{b}`，已验证 `m / b` 可由公式渲染器正常显示为上标；相关 PRD 更新到 V2.12.40。
- 本轮定向回归 `93 passed`；另做球墨铸铁管批量 CSV 真实写入校验，上限/下限总水损同一工况下分别约为 `3.2444 / 2.7603 m/km`。仅有批量图表既有 SimHei 缺少 `³` 字形警告，不影响通过。
- 本轮改动已提交并推送至 GitHub `master`，功能提交为 `7d16a24`；推送前再次运行有压管道内核、批量和界面反馈回归，结果为 `52 passed`，工作区内倒虹吸自动保存数据、个人设置、临时目录及规范资料均未纳入提交。
- 本轮继续修复批量图表的 SimHei `³` 缺字问题：流量单位统一改由 Matplotlib 数学公式渲染为 `m$^3$/s`，PNG/PDF 不再出现上角标方框或空白；PRD 更新到 V2.12.41，相关回归 `52 passed` 且警告清零。
- 最新：V1.3.11 双击无界面链路已完成修复，覆盖 PyInstaller 标准钩子过早触发 WMI、Qt WebEngine IPC `0x5` 拒绝访问与输出管道拖住、八个面板首次显示前同步创建，以及启动期临时窗口遗留退出事件导致主窗口刚显示就消失。
- 最终启动编排包含：最早期 WMI 运行钩子、隔离宿主原生依赖、WebEngine 子进程输出临时文件、父进程延后 WebEngine 导入、IPC 拒绝访问自动单进程兼容、仅创建“明渠设计”首屏、主窗口显式退出控制。相关定向回归为 `39 passed`，另有 2 个依赖受限临时目录权限的测试未在本轮重跑。
- 已排除同期未提交的有压管道改动后重建最终急用包：`output/CanalHydraulicCalc-V1.3.11-急用.zip` 为 378,716,549 字节，SHA-256 `9279EC34CA78492EA674E0E08FCA3CE69A0EAD4390589DEB9D1E88A3EC8ED99D`；ZIP 共 5256 项、CRC 正常且无 Codex/Poppler/UCRT 污染。全新解压后在普通 Windows 桌面连续两次确认标题 `渠系建筑物水力计算系统 V1.3.11`、窗口 1415×937、进程响应正常。GitHub Release 已直连替换完整包和补丁包，补丁为 91,872,858 字节、SHA-256 `48947B1FB7A16AA24577B0AE14D7B1824DF67C256D5B9F64D6DC23F6A038F2C3`；正式 Gist 与本地发布快照已回读同步，覆盖下限仍为 V1.3.0。
- V1.3.11 修正版完整解压后双击仍无窗口，已确认每次启动都会留下约 37MB、无主窗口句柄的卡死进程；模块清单显示 Qt/PySide6 尚未加载，但 Python `_wmi.pyd` 和 WMI COM 已进入进程，根因收口为 PyInstaller 标准运行钩子在项目内 WMI 防卡逻辑之前触发 `platform` WMI 查询。
- 构建脚本现向主程序和更新助手注入 `tools/pyinstaller_runtime_wmi_guard.py`，在 PyInstaller 标准钩子之前替换 `platform._wmi_query`；本轮启动验收改为必须检测到非零主窗口句柄和可响应窗口，不再以“进程持续存在”作为成功依据。
- V1.3.11 同事电脑启动时报 `QtCore` DLL “找不到指定的程序”，已确认不是授权故障；根因是 Codex 自带 Poppler/libheif 原生目录进入构建 `PATH`，导致 ICU 78、旧 `ucrtbase.dll` 及 44 个关联 API DLL 被误打包并覆盖目标电脑系统运行库。
- 早期仅清除原生 DLL 污染的急用包及其哈希已被本轮最终启动修复包替代，不再作为可交付基线；构建脚本继续对主程序和更新助手统一隔离 Codex 原生依赖路径。
- GitHub V1.3.11 曾替换过一轮中间修正版，但该资产已由本轮最终包原子替换；Release 资产 ID 为完整包 `539335403`、补丁包 `539337511`，远端大小、状态与 GitHub digest 均已回读匹配。
- 后续 `pyi_rth__tkinter` 提示 `_tcl_data` 不存在的截图路径含 `Temp\360zip$Temp`，确认是从 360 压缩预览窗口临时运行；最终 ZIP 内 `_tcl_data` 有 832 个文件且完整解压启动已通过，要求用户必须整包解压后运行。
- 本轮启动 V1.3.11 补丁发布：因“张蕾新电脑”和“皇甫新台式电脑”是两台不同实体电脑但显示完全相同的旧机器码，确认根因边界为克隆 Windows `MachineGuid` 无法区分设备；本地与 GitHub Gist 均核实该旧码当前归属皇甫，未直接覆盖台账。
- 新版机器码改为组合 `MachineGuid + SMBIOS 系统 UUID + 硬盘序列号` 的 SHA-256 指纹，SMBIOS UUID 通过 Windows 固件表直接读取；同时保留 V1.3.10 `MachineGuid` 单字段码与更早期硬件码作为兼容候选，启动校验和粘贴激活均接受候选集合，现有授权无需重签。
- V1.3.11 发版范围按用户确认包含机器码修复以及现有倒虹吸平面转弯半径连续输入修复；排除 `.claude/settings.local.json` 删除状态和两份倒虹吸运行数据变化。发布说明采用通用稳定性口径。
- V1.3.11 计划内授权、倒虹吸、构建、发布快照和更新安装回归共 `105 passed`；真实 Windows 冒烟确认可从 SMBIOS 固件表读取 36 位系统 UUID，生成 3 个去重候选且均为 64 位十六进制，新主机器码与旧 `MachineGuid` 单字段码不同。
- 宽泛关键词回归另发现批量面板既有测试桩 `3 failed`，均因测试对象缺少 `input_table`，与本轮 `license_checker.py` 和倒虹吸 `panel.py` 修改链路无关，本次不扩大修复范围。
- 本轮已为“皇甫新台式电脑”登记新机器永久授权：本地授权台账与 GitHub Gist 云端台账均已同步为 43 条，新机器码仅有 1 条有效记录；旧“皇甫秉辉笔记本”授权保持有效，未执行吊销。
- 本轮修复倒虹吸设计面板“平面转弯半径 R (m)”连续输入偶发失败：根因除实时联动回写外，还有刚修改 `Q / v` 后遗留的 200ms 防抖刷新会在输入途中覆盖文本；现以完整用户编辑会话锁住输入框，任何联动刷新都只更新计算状态，Enter/失焦后才统一显示两位小数，Excel 显式半径覆盖规则不变。
- 本轮按用户确认把核心引擎加载修复作为紧急补丁发布为 V1.3.10，只纳入修复代码、诊断、构建配置、测试和相关文档，排除 `.claude/settings.local.json` 的既有删除状态。
- V1.3.10 本地全量包已先行构建：ZIP 与独立解压目录均为 `5256` 个文件，全量包约 `361.2 MB`；正式发布脚本二次构建后的最终 SHA-256 为 `89436468C4FD53219B0548FE347A3ABB167F0ECB0FF9DB3AD6ED17297B356EC5`。从 ZIP 解压的 EXE 可持续运行，包内存在完整包名和兼容入口模块，且未产生新的核心故障日志。
- 本轮修复 V1.3.9 点击“插入渐变段”提示“核心计算引擎未加载”：先确认用户最初从压缩包临时目录直接运行，随后在完整解压的官方包中再次复现，证明解压不完整不是唯一根因。
- 诊断版捕获到真实异常 `ModuleNotFoundError: No module named 'core.spillway_steep_chute_adapter'`；根因是 PyInstaller 冻结环境同时存在同名顶层 `core`，`推求水面线.core` 内部短导入被错误解析，并非渐变段参数问题。
- 水面线面板改用 `推求水面线.core/models` 完整包名，核心包内部在正式命名空间下改用相对导入，并在 `core/__init__.py`、`models/__init__.py` 保留旧顶层导入兼容别名；构建脚本显式收集整个 `推求水面线` 包及诊断模块。
- 已新增 `core_engine_diagnostics.py`：未来若核心导入仍失败，会在相关入口显示真实异常和可复制详情，并把完整诊断写入 `%LOCALAPPDATA%/CanalHydraulicCalc/logs/water_profile_core_engine.log`；日志不可写不会二次阻断。
- 最终冻结诊断版已由用户在真实界面点击验证“插入成功”，且未生成新的核心故障日志；V1.3.10 发版聚焦回归 `178 passed`。完整 `tests/` 运行结果为 `2207 passed, 36 failed`，剩余失败可在恢复本轮无关文件后独立复现，属于仓库既有基线问题。
- 本轮启动 `v1.3.9` 正式发版：当前版本为 `1.3.8`，目标按 `patch` 递增到 `1.3.9`；发版范围只纳入产品代码、测试、PRD、README、ARCHITECTURE 和官方 Excel 模板。
- 发版前已恢复不应删除的 3 份 DWG 样例和 1 份 Word 导出样例，撤销 `data/siphon_autosave.json` 的本地时间戳变化；本地规范 PDF/OCR 和西阳山工程 DWG 保留原位并通过 `.git/info/exclude` 排除，不进入提交。
- `v1.3.9` 发版定向回归已完成：批量 Excel、七一/五排真实样例、泄水渠与陡坡专项、表3连续链、水面线损失、承压回写、公式详情和 DXF 导出共 `223 passed`，仅有 4 条第三方 `pyparsing` 弃用警告。
- 本轮把表3普通明流弯道附加损失从“当前节点完整弯道一次计入”改为按本行 `MC_{i-1}→MC_i` 区间分配：上一弯道后半段与当前弯道前半段分别采用各自参数计算后相加；承压弯头及外部回写口径不变。
- 本轮已采纳沿程水头损失“做法一”：普通沿程摩阻包含完整 MC—MC 水流长度，仅扣除已单列计损的渐变段；弯道公式统一定义为二次流附加损失。
- 本轮已按用户更正重新读取 `D:\项目\引大济岷\非均匀流水面线计算\莲花山隧洞水面线.dwg`，并重写专项理论稿；本轮仍未制作 Excel。
- 正式计算范围更正为总干 93+522.403～132+941.000，全长 39,418.597 m；实际水流沿桩号递增，后台从 132+941 向 93+522.403 反推。
- 总干 93+522.403～115+000.000 已从 CAD 读取为 11 个连续位置区间、12 个边界桩号；区间交替采用 `FB-C01(R3.20/bf2.50)` 和 `FB-C02(R3.40/bf3.20)`。
- 总干 115+000～115+010 为 10 m 专用渐变段，局部损失暂按约 0.07 m；115+010～132+941 采用 `FB-C03(R3.30/bf2.50)`，中间不再改变断面。
- 专项理论稿已改为通用可迁移模板：桩号及方向、物理上下游端、高程、断面尺寸与数量、区间、边界事件、水位、流量、糙率、净空、损失、系数、步长和容差均为用户可编辑输入；莲花山数据仅作示例。
- 本轮只读核查渡槽模块中 U 形断面直段高度 `f` 与拉杆高度的关系：普通结果字段 `f` 由含拉杆总高 `H_total` 回算，显示口径包含拉杆高度；水深反算使用内部 `hydraulic_f`，该字段不含拉杆高度。
- 本轮继续收口“导出全部 DXF”显示口径：计算仍统一走 `泄水渠与陡坡` 专项内核，但纵断面“建筑物名称”行保留用户在结构形式中填写的 `充水渠 / 泄水渠 / 陡坡 / 泄槽 / 陡槽 / 泄水渠及陡坡 / 泄水渠与陡坡`。
- 批量结果和 `section_params["spillway_steep_chute"]` 中新增/传递 `display_structure_type` 保存用户原始结构名；`-` 只表示空建筑物名称，不参与拼字。
- 本轮修复七一充水渠“导出全部 DXF”中泄水渠与陡坡名称显示问题：批量表里的 `-` 只作为空建筑物名称占位，专项链准备时清成空名称，纵断面 DXF 不再显示 `-泄水渠与陡坡`。
- 本轮同步修正泄水渠与陡坡专项链的首尾角色口径：内部继续用 `section_params["spillway_steep_chute"]["role"]` 记录 `inlet/outlet` 供计算使用，但表3通用 `进出口判断` 保持普通节点，不再显示 `泄陡进 / 泄陡出`。
- 已新增相关回归：空名称/`-` 的建筑物名称显示、专项链不污染通用进出口字段、七一真实 Excel 导出 DXF 不出现 `-泄水渠与陡坡`、`泄陡进`、`泄陡出`。
- 本轮按用户更正重新收口七一充水渠口径：`充水渠`、`泄水渠`、`陡坡`、`泄槽`、`陡槽`、`泄水渠及陡坡`、`泄水渠与陡坡` 在表3水面线中统一走 `泄水渠与陡坡` 专项内核。
- 表3适配层不改专项内核本身，而是把连续专项链按底坡变化拆成固定底坡子段逐段计算；底坡相同的连续 IP 点归为同一子段，七一末尾 4 行形成 `100`、`10`、`24.5` 三个子段。
- 七一 Excel 只补必要缺参：第 1 流量段手工 `Q加大` 保持空白，表示继续采用自动比例和自动加大流量；末尾 4 行不淤/不冲流速补为 `0.1/100`；保留原始坐标、`Q=0.7`、`n=0.014`、`B=1`、`m=0`、底坡倒数和转弯半径等核心工程数据。
- 本轮更新 `tests/test_batch_qiyi_fill_channel_regression.py`、`tests/test_water_profile_spillway_steep_chute_unit.py` 和 `tests/test_batch_import_dimension_preserve_unit.py`，覆盖专项别名、变坡链、同坡 IP 合并、批量占位透传和可打开的“导出全部DXF”；目标回归 `46` 个通过。
- 此前曾按“五排水库充水渠批量计算用表”做真实回归；本轮已按用户更正改回七一水库充水渠作为真实验收目标。
- 本轮同步修正 `泄水渠与陡坡` 表3链路：连续专项链不跨越其他结构或不同流量段；孤立单行专项节点不回退普通明渠，而是提示缺少相邻下游节点。
- 本轮修复批量 Excel 基础信息读取：起始水位等基础信息遇到简单数字公式（如 `=352.44`）时按数字值读取，避免公式缓存缺失后误用默认水位。
- 本轮按用户确认的修订口径继续完善“泄水渠与陡坡”表3接入：连续多个 `泄水渠与陡坡` 之间即使底坡、名称或尺寸不同也不自动插入渐变段；专项参数采用“Excel 预填 + 表3详情补改/查看结果”的混合方式。
- 已新增批量 Excel 的 10 个泄水渠专项可选列，并透传到 `section_params["spillway_steep_chute"]["advanced_params"]`；表3详情入口已改为可编辑参数和查看结果的同一入口，保存后写回同名组隐藏参数；本轮验证为专项内核 `27` 个测试通过、表3/批量大回归 `336` 个测试通过、组合回归 `71` 个测试通过。
- 批量页现在只负责校验、展示“表3专项计算”占位结果并向共享数据注册 `Q/n/B/m/底坡/坐标/转弯半径/专项高级参数`；正式水面线结果以表3计算为准。
- 表3正式链式接入已完成：结构枚举/下拉、表1占位透传、共享结果、表3适配层、主水位递推、详情入口和相关 PRD 均已同步；本轮目标回归 `46` 个测试通过。
- 本轮开始全盘复核“泄水渠与陡坡”PRD、计算原理和程序实现是否与 GB 50288-2018、明渠水力教材理论及熊启钧专著口径一致，重点排查隐藏默认值和来源不清问题。
- 本轮已按“入口过流能力系数修复方案”修正泄水渠与陡坡入口流量系数：入口连接形式按 GB 50288-2018 附录 N 自动计算，专业模式可手动输入流量系数，旧版隐藏默认 `0.42` 不再用于入口校核。
- 本轮已按“泄水渠与陡坡计算原理一致性修复计划”接入 `alpha_profile`：水面线断面比能和逐段距离统一使用 \(\alpha_e\)，专业模式可见，新手模式默认参与计算；计算原理、Word/Excel 和 PRD 已补齐默认系数与入口 \(H_0\) 口径说明。
- 本轮按软著申请口径生成“泄水渠与陡坡水力计算系统”两份 Word 材料：软件源代码 Word 和操作手册 Word；只生成材料，不修改正式软件代码。
- 本轮按用户截图反馈重生成软著源码 Word 为纯代码版：删除每行文件路径/行号前缀和“源码第 X 页 / 共 X 页”页内标题，源码仍为 3830 行、76 页、每页 50-51 行。
- 本轮继续按用户红框反馈重生成软著源码 Word：进一步删除 Python 模块/类/函数文档字符串、`#` 注释和空行，并把多行普通字符串压成单行表示；最终为 3618 行、72 页、每页 50-51 行。
- 本轮已实现“暗涵-矩形”同时指定底宽 B 和高度 H：单项页与批量参数弹窗均支持固定宽高验算，程序只校核水深、流速和净空，不自动改尺寸。
- 本轮追加把“暗涵-矩形”尺寸输入扩展到 Excel 和批量表明列：`底宽B(m)` 后新增 `暗涵高宽比H/B`、`暗涵高度H(m)`，`明渠宽深比` 改为 `宽深比β` 并兼容旧表头。
- 已在 `codex/experimental-spillway-steep-chute` 分支完成“泄水渠与陡坡”第二版正式模块改造，并继续处理用户反馈的界面对齐问题。
- 本轮已修复 3 个用户可见问题：加大流量输入方式对齐明渠/渡槽，计算原理来源不再暴露内部 PRD 口径，纵断面图右侧不再因固定画布宽度被裁切。
- 本轮追加修复 `main.py` 启动时弹出 Qt WebEngine 诊断框的问题：标准 WebEngine 冷启动实际可成功，但原 8 秒预检预算过短，会误判为超时。
- 本轮已把旧公式页改为“计算原理”，并将该页签和 Word/Excel 导出章节放到“结果汇总”之前。
- 本轮追加修复计算原理页源码露出与英文残留：不再因不支持的分段公式回退显示 LaTeX 源码，断面类型、起点控制来源、水面线模式和公式下标均改为中文可见口径；Word/Excel 的计算原理“公式”列改用中文可读公式文本。
- 本轮追加修正计算原理中的湿周符号：参照明渠设计展示口径，湿周统一用 \(\chi\)，水力半径写为 \(R=A/\chi\)，不再用 \(P\) 表示湿周。
- 本轮追加完成泄水渠输入栏组件体系对齐：灰底不是单纯颜色问题，根因是泄水渠仍使用原生输入框和透明容器；现已改为与明渠/渡槽一致的 Fluent 输入框和白色输入卡片。
- 本轮追加修正泄水渠“分级流量”提示：用户不再手填分级流量，程序先按设计流量的 10% 到 100% 初筛，再在控制区间按 1% 设计流量加密，加大流量自动作为额外工况加入。
- 本轮追加修正泄水渠导出和清空入口：补齐“清空”按钮，“导出文档”改为“导出计算书”，计算书和表格导出时自动预填工程/工况相关文件名，仍允许用户自行修改；导出后询问是否直接打开，并修复表格导出缺少 `.xlsx` 后缀的问题。
- 本轮追加完成计算前“计算原理”预览：未点击计算前也展示完整流程、公式、变量、来源和当前输入，正式结果位置统一显示“计算后生成”。
- 本轮追加完成计算原理页顶部 11 步流程总览：按“先算、得到、用于下一步”展示完整顺序，只影响软件页面，不同步扩展到 Word/Excel 导出。
- 本轮追加修正泄水渠首个默认工况名：新建和重置后默认显示“工况1”，未手动重命名的工况按序号自动显示，导出默认文件名同步使用当前工况名。
- 本轮追加完成泄水渠 Word 计算书开头格式对齐：导出计算书复用明渠设计同一套工程计算书固定格式，开头包含产品运行卡、封面、强制性条文校审检查表、目录、计算目的、计算依据、基本资料和计算程序。
- 本轮追加修复纵断面图左侧“高程（米）”被截断问题：新增 Matplotlib 文字安全布局，按标题、坐标轴标题、刻度和图例的真实渲染边界自动留白，不采用固定加大左边距补丁。
- 本轮追加修复计算原理页普通说明文字的工程符号下标显示：变量含义、本次代入、计算结果和原理说明中的 \(h_s\)、\(E_s\)、\(Q_{\text{过流}}\) 等不再裸露为下划线文本。
- 本轮已完成多流量段批量计算官方 Excel 模板说明页改造：保留 `导入模板` 为第一张和活动表，新增先看这里、公共字段、明渠、渡槽、隧洞、暗涵、承压与闸类、字段速查 8 张新手说明页，并给 `结构形式` 列增加下拉提示。
- 本轮已按用户填写心智调整批量输入表和官方 Excel 模板列顺序：`拉杆高度(m)` 移到 `矩形渡槽深宽比` 后，`直墙高度H直(m)` 移到 `圆心角(°)` 后，AE/AF 现在为 `右坡m3`、`平台高差h1(m)`。
- 本轮已统一多流量段官方 Excel 模板视觉格式：`导入模板` 恢复为活动页，`A2:AF500` 统一表头、筛选、边框和整行斑马纹，R/V/Y/Z 等列不再出现背景、边框或字体断层。
- 本轮追加修复旧版无 X/Y 且表头不可识别 Excel 的兜底导入：倒角、圆心角、不淤/不冲流速、转弯半径和管材不再被新增列挤偏。
- 本轮追加修复渡槽加大有效超高校核：`0.097m` 这类真实值低于 `0.10m` 的结果，即使四舍五入显示为 `0.1m`，也必须判定未通过。
- 本轮追加收短渡槽加大有效超高失败提示：界面只展示“加大有效超高不足”和实际数值，不展示四舍五入判定规则说明。
- 本轮按“只合代码”口径已合并到 `master`：保留代码、测试、PRD 和官方 Excel 模板改动，恢复自动保存时间戳和本地导出文件删除；同时补齐官方模板筛选范围。

## 上次停在哪

- V1.3.11 修正版急用 ZIP、GitHub Release 完整包/补丁、正式 Gist 和本地发布快照已全部收口；张蕾新授权仍需等待她完整解压修正版程序并复制新的组合机器码。
- “皇甫新台式电脑”授权码已按 2026-08-27 签发并完成 HMAC 签名、Base64 往返解码及云端回读核验；本轮只更新授权台账和 `CONTEXT.md`，未修改程序代码、PRD、README 或 ARCHITECTURE。
- 倒虹吸 `R` 键盘输入回归已在每轮预置 200ms 延迟刷新的条件下重复 3 轮：依次键入 `3`、`0` 时稳定显示 `3`、`30`，Enter 后显示 `30.00`；相关 4 组测试分开运行共 `32 passed`。
- `v1.3.9` 发版前置条件已核对：当前已切回 `master`，本地 `master` 比 `origin/master` 领先 1 个既有功能提交，`v1.3.9` 本地与 GitHub 标签均不存在，GitHub/Gitee 发布凭据已就绪，正式脚本预演只会暂存产品范围文件。
- 普通明流半弯道分配已接入水位递推、表3弯道附加列和双击详情；详情同时保留两个完整弯道参数、各半段分配值及当前完整弯道核查值；相关扩展回归累计 `229 passed`，4 个改动模块语法编译及目标文件差异检查均通过。
- 沿程有效长度的核心与承压链备用路径均已改为 `max(0, MC里程差-渐变段长度)`；表头、公式弹窗、计算说明、PRD 和回归测试已同步，相关完整回归 `70` 个、界面对齐回归 `82` 个全部通过，语法编译检查通过。
- 正式理论稿已更新为 3010 行、100085 字节、UTF-8 无 BOM，SHA256 为 `CAA8B2DE9F811845A5C2583EE0385F0065DB030A27EC056EE7AD7575200A12BB`。
- 文档已写入通用可迁移数据层、动态强制节点、双向桩号支持和完整反推流程；莲花山 11 个 CAD 区间、三种断面及 14 个示例强制节点均作为可编辑示例保留。
- 原 DWG 只读使用，SHA256 仍为 `AA183FDA137859E6E783699BBF2C6B905A4C0142BF6EC9DF8EC8467C09D2757D`。
- Excel 尚未制作；正式建表时必须采用结构化输入表，全部工程输入可修改、增删或停用，公式中不得硬编码莲花山数值，并须用第二个不同桩号方向和断面数量的项目做迁移验证。
- 当前新增回归已覆盖“批量别名按专项计算但保留原始显示名”和“DXF 建筑物名称行读取用户填写的结构名、不读取占位横杠”；组合回归已确认真实七一导出链路。
- 七一充水渠 DXF 名称问题已收口：专项链仍按 `100`、`10`、`24.5` 三个固定底坡子段联算，但建筑物名称占位 `-` 不进入导出文字，专项首尾不再占用表3通用进出口显示。
- 七一充水渠口径已落地：Excel 末尾 4 行导入后按 `泄水渠与陡坡` 专项参数注册，表3按 `100`、`10`、`24.5` 三个固定底坡子段联算，真实 Excel 批量、表3和“导出全部DXF”回归已通过。
- 五排真实 Excel 和空名称泄水渠分组已完成代码与 PRD 同步；聚焦回归 `3` 个通过、泄水渠/空名称相关回归 `13` 个通过、批量与五排组合回归 `41` 个通过、组合 DXF 回归 `30` 个通过。
- 本轮已修复此前发现的 \(\alpha_e\) 只展示不参与计算问题；旧项目缺少 `alpha_profile` 时按 `1.1` 迁移并给风险提示，梯形/台堰形入口本次只在 PRD 中收窄为后续增强。
- 入口流量系数修复已接入内核、单项界面、计算原理、Word/Excel 导出和旧项目迁移；PRD 已同步记录默认“扭曲面连接”、专业手动覆盖、侧收缩系数 `ε=1.0` 和旧版 `0.42` 废止口径。
- 软著材料输出到 `output/doc/`：源码文档采用专项运行源码全量，操作手册使用临时独立窗口承载真实泄水渠与陡坡面板截图，未出现综合软件其他模块导航。
- 当前最新版源码文档为纯代码排版，不再展示文件路径、行号和页码标题；操作手册未变。
- 当前最新版源码文档不包含空行、`#` 注释行、模块/类/函数三引号文档说明或独立三引号边界；操作手册仍未变。
- 暗涵矩形固定 B+H 已接入内核、单项工况保存恢复、批量明列参数、Excel 模板/导入、复制行、快照和项目保存恢复；PRD 已同步更新到 v1.32。
- 计算内核 `calc_渠系计算算法内核/泄水渠与陡坡设计.py` 已扩展到第二版，支持水面线型识别、起点控制水深、上游缓坡自由衔接、下游水跃、掺气侧墙、消力池初步尺寸、出口整流、多流量控制，并已被表3正式成组结构复用。
- 前端包 `app_渠系计算前端/spillway_steep_chute/` 已按渡槽/隧洞风格重做，包含新手/专业模式、正式输入分组、工况条、结果导航、计算原理页、网页结果页、沿程水面线、纵断面图、规范校核、双表工况对比、项目保存恢复和文档/表格导出。
- 本轮将加大流量输入改为复用 `increase_input_helper`，支持“按比例 / 按Q加大”、比例留空自动查表、旧项目字段恢复和非法输入明确提示。
- 本轮将旧公式页统一升级为审查级“计算原理”：界面、Word 和 Excel 均展示计算流程、公式、变量含义、本次代入、结果、原理说明和来源，并继续清洗内部 `PRD` 字样。
- 本轮补强计算原理安全展示：界面公式渲染失败只显示中文提示，不显示原始源码；计算原理共享数据新增导出用中文可读公式，避免 Word/Excel 展示 `\frac`、`\quad` 等源码格式。
- 本轮将纵断面图接入共享断面图画布布局，切页和窄窗口下会按可视区域刷新画布。
- 本轮将泄水渠输入栏从原生 `QLineEdit` + 透明 `QFrame` 改为 qfluentwidgets `LineEdit` + `QGroupBox` 输入卡片，只对齐左侧输入栏，不改变右侧结果页、计算、保存和导出逻辑。
- 本轮将隐藏的“分级流量列表”改为纯兼容字段：界面不再展示，旧项目中的 `flow_cases_text` 不再参与计算；面板参数组装层自动生成 `0.1Q` 到 `1.0Q` 的初筛流量、传入 `flow_case_refinement`，并去重加入 \(Q_{\text{加大}}\)。
- 本轮将泄水渠“清空”定义为只清除计算结果、结果导航、表格、图形和导出缓存，不恢复默认输入、不删除多工况；导出计算书和导出表格会按当前工程名或当前工况名预填文件名，多工况导出会标注“等N个工况”，用户在保存对话框里改名后仍以用户选择为准；导出计算书固定补 `.docx`，导出表格固定补 `.xlsx`，保存成功后复用明渠已有的打开询问。
- 本轮将泄水渠未重命名工况改为按“工况1、工况2……”自动显示；工况按钮和导出默认文件名不再用“设计工况”或“未命名工程”作为默认工况名。
- 本轮将计算原理页拆成计算前预览态和计算后正式态：预览态只读当前输入、不调用内核；正式态继续复用正式计算结果和导出口径。
- 本轮将计算流程总览限定在计算原理页顶部，作为新手阅读路线图；详细公式卡片、Word 导出和 Excel 导出继续沿用原有审查级计算原理口径。
- 本轮补齐多流量控制的细化口径：前端传入 `flow_case_refinement`，内核先按 10% 初筛，再在控制流量邻近区间按 1% 设计流量加密并重新选择控制工况。
- 本轮将泄水渠 Word 导出从普通 `Document()` 改为复用共享 `create_engineering_report_doc()`，正文仍保持“计算原理 → 结果汇总 → 沿程水面线 → 校核与风险”的原顺序；模板缺少项目符号样式时风险提示回退为普通段落。
- 本轮新增 `app_渠系计算前端/matplotlib_text_layout.py`，泄水渠纵断面图绘制完成后会基于真实渲染边界调整图内边距；空数据状态也走同一套防裁切逻辑。
- 本轮在 `app_渠系计算前端/spillway_steep_chute/panel.py` 新增计算原理段落符号渲染 helper，先做 HTML 转义，再把工程符号转换为下标 HTML；Word/Excel 导出仍保持纯文本口径。
- 已同步更新 `docs/PRD_泄水渠与陡坡水力计算.md`，记录本轮对齐修复、计算书开头固定格式、纵断面图文字安全布局和最新验收结果。
- 已将 Qt WebEngine 标准预检外层超时从 8 秒调整为 20 秒，并补充启动诊断回归测试；`tools/qt_webengine_doctor.py` 当前返回标准模式可用。
- 多流量段批量计算官方 Excel 模板已按结构形式分 sheet 写入填写指南；`导入模板` 第1行手工 `Q加大` 入口不变，新增说明页和列顺序调整均不改变导入与计算逻辑。
- 多流量段批量输入列顺序已从“末尾追加”调整为“按参数关联”：旧 Excel 继续按表头映射导入；旧 `.qxproj` 若没有保存表头顺序，则按调整前 32 列顺序迁移到当前顺序；新项目保存会写入 `input_header_order`。
- 官方 Excel 模板视觉层统一为“第 1 行基础信息、深蓝表头、A3:AF500 整行斑马纹、隐藏网格线、A2:AF500 筛选、说明页 A7 冻结”的口径；这只影响打开和填写体验，不影响导入解析与计算。
- 旧版无 X/Y Excel 的最后兜底导入路径已改为按 `COL_*` 常量映射，旧表没有的 H/B、H、拉杆高度、H直 和复式梯形列保持空白。
- 本轮将渡槽 U 形和矩形的加大流量最终校核收口到内核：最终水深重算后用未格式化原始 `Fb` 严格判断 `≥0.10m`，不再让四舍五入后的显示值影响通过/失败。
- 本轮将加大有效超高不足的内核错误提示改为用户可读短文案，保留 `Fb = 实际值 < 0.10m`，移除“不能按四舍五入后的显示值判定通过”等实现说明。
- 本轮已将 `codex/experimental-spillway-steep-chute` 的有效改动提交并快进合并到 `master`，本地功能分支已删除；当前 `master` 本地领先 `origin/master` 1 个提交。

## 关键决定

- Qt WebEngine 标准模式优先；仅在预检明确识别到 Windows IPC `0x5` 拒绝访问时自动切换单进程兼容模式。主窗口改为首屏先显示、其余面板按首次访问加载，同时保留旧属性和项目恢复契约。
- PyInstaller 冻结包的 WMI 防卡保护必须使用自定义最早期运行钩子，不能只放在 `bootstrap.run()`；最终验收必须观察真实主窗口句柄、标题和响应状态，后台空进程无论存活多久都判定为启动失败。
- 构建环境不得把 Codex、Poppler、libheif 等宿主工具的原生 DLL 带入正式包；PyInstaller 子进程必须使用隔离后的 `PATH`，最终验收必须以“从实际 ZIP 重新解压 + 精简系统 `PATH` 启动”为准，不能只在开发机现有 `dist` 目录启动。
- 可直接编辑的数值输入框从第一次用户文本变化到 Enter/失焦属于完整编辑会话；会话期间包括防抖定时器在内的所有联动刷新都不得强制补齐小数位，实时计算可继续执行，规范化显示必须延后到编辑结束。
- 核心模块的规范入口固定为 `推求水面线.core` 和 `推求水面线.models`；包内优先使用相对导入，旧脚本所需的 `core.* / models.*` 只作为兼容别名，避免 PyInstaller 中同名顶层包抢占。
- 用户直接在压缩包临时目录运行时仍建议先完整解压，但不得再把“重新解压”作为本故障的最终修复结论；本次代码修复必须以完整冻结包真实点击“插入渐变段”为验收门槛。
- 整个 `推求水面线` 包、关键适配器和加载诊断同时进入 PyInstaller 收集清单、打包前导入预检及回归测试，降低自动扫描遗漏和命名空间漂移风险。
- 弧段普通摩阻与弯道二次流附加损失必须分项相加；`arc1_half`、`arc2_half` 继续保留用于核查，但不得参与沿程有效长度扣减。
- 莲花山全线按总干 93+522.403～132+941.000 建模；总长 39,418.597 m，不再把 115+000～132+941 的 17,941 m 误当作全线。
- CAD 中的“11 个断面”按 11 个位置区间理解；它们复用两种上游几何模板，另加下游 `FB-C03`，全线共三种唯一断面几何。
- 强制节点由已启用区间端点、边界事件端点和附加节点动态生成；莲花山示例得到 14 个唯一节点，只用于自检，不构成通用模板限制。
- 总干 115+000～115+010 单独作为已知长度渐变步，约 0.07 m 为全段总水头损失，只计一次。
- 下游控制水位 $Z_{w,d}$ 和同断面底板高程 $Z_{b,d}$ 都是可编辑输入；莲花山初始值分别为 $797.141\,\mathrm{m}$、$792.537\,\mathrm{m}$，控制水深 $h_d$ 由公式重算。
- 所有项目专属参数均属于可编辑输入，包括桩号及方向、高程、断面尺寸与数量、区间、边界事件、$Q$、$Z_{w,d}$、$n$、净空、$g$、动能系数、损失、步长、容差和最大迭代次数；只有派生几何、求根、能量校核和结果公式可保护。
- Markdown 中行内公式使用 `$...$`，独立公式使用 `$$...$$`；聊天回复中的公式改用 `\(...\)` 或 `\[...\]`。
- 泄水渠类结构的计算口径统一为 `泄水渠与陡坡`；DXF 纵断面“建筑物名称”行读取 `display_structure_type`，按用户原始结构形式显示。
- `泄水渠与陡坡` 的入口/出口是专项内部计算角色，不等同于表3通用特殊建筑物进出口；对外显示仍按普通 IP 节点处理。
- 对 `泄水渠与陡坡` 来说，建筑物名称中的 `-` 与空白等价，只表示“不填写名称”，导出时显示结构名本身，不拼接横杠。
- “充水渠”只在结构形式语境下归一为 `泄水渠与陡坡` 专项类型，不改变基础信息里的 `渠道级别=充水渠` 含义。
- 七一末尾 4 行不按普通矩形渠道逐行递推；`m=0`、`B=1` 作为专项内核的矩形断面参数，表3适配层按底坡变化分段调用专项内核。
- 五排水库充水渠样例保留原始 15 条 `明渠-圆形` 数据，只补第 1 行手工 `Q加大=0.2` 表达“加大流量等于设计流量”；不凭空改 `X/Y/Q/底坡/D` 等核心计算参数。
- `泄水渠与陡坡` 空名称不再阻断表3计算，分组依据限定为“相邻连续 + 同结构 + 同 `底坡1/i`”，不跨越其他结构或非相邻行。
- 本轮审查先输出问题清单和修复建议，不直接扩大修改范围；后续若修复，优先处理“公式展示与实际计算不一致”的项目，再处理 PRD 表述和高级参数显隐。
- 水面线动能修正系数正式字段为 `alpha_profile`，兼容读取 `profile_energy_alpha` 和 `alpha_e`；新旧项目统一按 `alpha_profile` 保存。当前入口 \(H_0\) 仍由用户直接输入，不自动由堰前水深和流速水头推导；梯形/台堰形跌口公式保留到后续专项增强。
- 计算原理一致性修复验证结果：`67` 个测试通过；命令为 `.\.venv\Scripts\python.exe -m pytest -p no:hypothesispytest tests/test_spillway_steep_chute_basic_hydraulic_unit.py tests/test_spillway_steep_chute_validation_unit.py tests/test_spillway_steep_chute_panel_unit.py tests/test_spillway_steep_chute_export_unit.py tests/test_spillway_steep_chute_v2_unit.py -q --basetemp=.pytest_tmp\spillway-principle-consistency`。
- 入口过流能力默认不再使用隐藏 `μ=0.42`；新建和旧项目迁移默认采用“扭曲面连接”按规范公式计算流量系数，只有专业模式选择“手动输入流量系数”时才采用用户给定值；侧收缩系数本轮保留 `ε=1.0` 并在计算原理和导出中说明取值含义。
- 入口流量系数修复验证结果：`63` 个测试通过；命令为 `.\.venv\Scripts\python.exe -m pytest -p no:hypothesispytest tests/test_spillway_steep_chute_basic_hydraulic_unit.py tests/test_spillway_steep_chute_validation_unit.py tests/test_spillway_steep_chute_panel_unit.py tests/test_spillway_steep_chute_export_unit.py tests/test_spillway_steep_chute_v2_unit.py -q --basetemp=.pytest_tmp\spillway-inlet-coeff-final-2`。
- 软著源码 Word 只纳入泄水渠与陡坡专项运行源码，不加入测试文件，也不为凑满 4000 行加入综合主程序或共享支撑代码；有效源码统计为 3830 行，因此按“全部代码不足 4000 行则全部提供”处理。
- 软著源码 Word 的最终展示口径按用户要求只保留代码文本本身；路径、行号和页内页码标题只用于内部校验，不进入最终 Word 正文。
- 用户要求“三引号里的说明文字也不要”，因此最终源码 Word 把 Python docstring 视同注释剔除；普通运行字符串仍保留为单行字符串代码，避免出现独立三引号行。
- 矩形暗涵固定高度 H 和高宽比 H/B 现在作为 Excel 与批量表正式明列输入；旧隐藏 `rect_culvert_manual_H` 只作为旧项目兼容迁移来源，恢复后写入可见 `暗涵高度H(m)`。
- 本轮 Excel 明列输入验证结果：`97` 个测试通过；命令为 `.\.venv\Scripts\python.exe -m pytest -p no:hypothesispytest tests/test_culvert_kernel.py tests/test_culvert_arch_panel_batch_unit.py tests/test_batch_import_dimension_preserve_unit.py tests/test_water_profile_batch_sync_unit.py tests/test_transition_reference_culvert_unit.py -q`。
- 本轮暗涵固定 B+H 验证结果：`101` 个测试通过；命令为 `.\.venv\Scripts\python.exe -m pytest -p no:hypothesispytest tests/test_culvert_kernel.py tests/test_culvert_arch_panel_batch_unit.py tests/test_culvert_panel_plot_unit.py tests/test_culvert_comparison_unit.py tests/test_result_summary_unit.py tests/test_batch_import_dimension_preserve_unit.py -q --basetemp=.pytest_tmp\culvert-fixed-bh-elevated`。
- 第二版现采用“独立正式模块 + 表3连续链接入”：独立页面继续负责专项设计，表3按连续专项链分段调用同一专项内核，不把内部沿程采样点展开成正式表3节点。
- 加大流量输入沿用明渠、渡槽等设计面板的共享口径：默认按比例，空值自动查表；按Q加大时反算比例并要求 \(Q_{\text{加大}}>Q\)。
- 消力池多流量控制不再要求用户理解或录入“分级流量列表”；默认每 10% 自动初筛并按 1% 加密控制区间，是为了符合规范“按数值分级”要求，同时减少新手模式下的输入负担。
- 泄水渠输入栏对齐按“组件体系对齐”处理，不只改输入框背景色；右侧结果页本轮保持原结构，避免扩大影响面。
- 计算原理来源里的内部 PRD 表述只在用户界面和导出结果中隐藏，内部 PRD 文档本身保留。
- 计算前原理预览不自动执行正式水力计算；依赖计算的正常水深、临界水深、水面线、消能和校核结论统一显示“计算后生成”，避免误导用户把预览当作结果。
- 计算原理页签固定放在“结果汇总”之前；导出也按“计算原理 → 结果汇总”的顺序组织，避免界面和成果口径不一致。
- 计算原理的“不要出现英文”按用户可见内容处理：工程常用数学符号可保留，内部英文枚举、英文单词和英文公式下标必须中文化。
- 计算原理中的湿周符号采用明渠模块一致的 \(\chi\) 展示口径；内核字段名可继续保留原实现，不影响数值计算。
- 泄水渠 Word 计算书开头格式直接复用明渠工程计算书模板；正文数据和 Excel 导出不变，避免复制一套固定页生成逻辑。
- 纵断面图继续使用现有滚动承载结构，但画布尺寸由共享布局工具按当前可视区域控制，不新增横向滚动条。
- 纵断面图文字截断按“渲染后测量文字边界”处理，图内文字留白和滚动画布尺寸分层负责，避免用固定比例边距掩盖字体或窗口变化问题。
- 计算原理页大公式继续走现有 LaTeX/SVG 渲染；下方普通说明文字单独做安全 HTML 下标渲染，避免中文长段落被公式引擎破坏，也避免 HTML 注入。
- WebEngine 预检仍保留标准模式优先和失败诊断弹窗，但冷启动预算需要覆盖首次创建 `QWebEngineView` 的真实耗时，避免把可用环境误判为不可启动。
- 标准自由陡坡继续按 GB 50288-2018 第 12.3.3 条计算 \(b_2\) 型水面线；非标准工况先识别水面线型并给出衔接与风险提示。
- 熊启钧棱柱体陡坡算例作为教学算例和回归验收口径，当前末端水深容差按 `±0.08m` 控制。
- 当前 `.venv` 的 `hypothesispytest` 插件会让原样 pytest 进程卡住；本轮验证继续使用 `-p no:hypothesispytest`。
- 渡槽加大有效超高校核采用原始计算值判断；显示格式只用于界面和报告，不参与是否通过的判定。
- 渡槽加大有效超高严格校核验证结果：先用截图参数新增回归测试并确认旧逻辑失败，修复和提示收短后相关回归 `58` 个测试通过；命令为 `.\.venv\Scripts\python.exe -m pytest -p no:hypothesispytest tests/test_aqueduct_kernel_unit.py tests/test_result_summary_unit.py tests/test_aqueduct_panel_plot_unit.py -q --basetemp=.pytest_tmp\aqueduct-freeboard-message-final`。
- 本轮验证结果：`101` 个测试通过；命令为 `.\.venv\Scripts\python.exe -m pytest -p no:hypothesispytest tests/test_increase_input_helper_unit.py tests/test_increase_input_mode_panels_unit.py tests/test_spillway_steep_chute_panel_unit.py tests/test_spillway_steep_chute_plotting_unit.py tests/test_spillway_steep_chute_export_unit.py tests/test_spillway_steep_chute_v2_unit.py tests/test_section_plot_layout_unit.py -q --basetemp=.pytest_tmp\spillway-three-fixes`。
- 启动修复验证结果：`15` 个测试通过；命令为 `.\.venv\Scripts\python.exe -m pytest -p no:hypothesispytest tests/test_webengine_diagnostics_unit.py -q --basetemp=.pytest_tmp\webengine-diagnostics-final`；诊断脚本 `.\.venv\Scripts\python.exe tools\qt_webengine_doctor.py` 返回 `ok: True`。
- 计算原理增强阶段验证结果：`40` 个测试通过；命令为 `.\.venv\Scripts\python.exe -m pytest -p no:hypothesispytest tests/test_spillway_steep_chute_panel_unit.py tests/test_spillway_steep_chute_export_unit.py tests/test_spillway_steep_chute_v2_unit.py -q --basetemp=.pytest_tmp\spillway-principles`。
- 输入栏组件体系对齐验证结果：相关回归 `38` 个测试通过，命令为 `.\.venv\Scripts\python.exe -m pytest -p no:hypothesispytest tests/test_spillway_steep_chute_panel_unit.py tests/test_increase_input_mode_panels_unit.py -q --basetemp=.pytest_tmp\spillway-ui-align`；泄水渠专项回归 `56` 个测试通过，命令为 `.\.venv\Scripts\python.exe -m pytest -p no:hypothesispytest tests -q -k spillway_steep_chute --basetemp=.pytest_tmp\spillway-ui-align-full`。
- 自动分级流量与提示修正验证结果：`43` 个测试通过；命令为 `.\.venv\Scripts\python.exe -m pytest -p no:hypothesispytest tests/test_spillway_steep_chute_panel_unit.py tests/test_spillway_steep_chute_v2_unit.py tests/test_spillway_steep_chute_export_unit.py -q --basetemp=.pytest_tmp\spillway-auto-flow-final`。
- 多流量控制区间自动加密验证结果：`50` 个测试通过；命令为 `.\.venv\Scripts\python.exe -m pytest -p no:hypothesispytest tests/test_spillway_steep_chute_panel_unit.py tests/test_spillway_steep_chute_v2_unit.py tests/test_spillway_steep_chute_export_unit.py -q --basetemp=.pytest_tmp\spillway-flow-refine-final`。
- 导出和清空入口修正验证结果：计划内导出/面板回归 `26` 个测试通过；命令为 `.\.venv\Scripts\python.exe -m pytest -p no:hypothesispytest tests/test_spillway_steep_chute_panel_unit.py tests/test_spillway_steep_chute_export_unit.py -q --basetemp=.pytest_tmp\spillway-export-buttons`；泄水渠专项回归 `59` 个测试通过，命令为 `.\.venv\Scripts\python.exe -m pytest -p no:hypothesispytest tests -q -k spillway_steep_chute --basetemp=.pytest_tmp\spillway-export-buttons-full`。
- 计算原理源码与英文残留修复验证结果：`50` 个测试通过；命令为 `.\.venv\Scripts\python.exe -m pytest -p no:hypothesispytest tests/test_spillway_steep_chute_panel_unit.py tests/test_spillway_steep_chute_export_unit.py tests/test_spillway_steep_chute_v2_unit.py -q --basetemp=.pytest_tmp\spillway-principles-cn`。
- 导出默认文件名修正验证结果：计划内面板/导出回归 `30` 个测试通过；命令为 `.\.venv\Scripts\python.exe -m pytest -p no:hypothesispytest tests/test_spillway_steep_chute_panel_unit.py tests/test_spillway_steep_chute_export_unit.py -q --basetemp=.pytest_tmp\spillway-default-name`；泄水渠专项回归 `66` 个测试通过，命令为 `.\.venv\Scripts\python.exe -m pytest -p no:hypothesispytest tests -q -k spillway_steep_chute --basetemp=.pytest_tmp\spillway-default-name-full`。
- 计算前原理预览与多流量细化验证结果：`50` 个测试通过；命令为 `.\.venv\Scripts\python.exe -m pytest -p no:hypothesispytest tests/test_spillway_steep_chute_panel_unit.py tests/test_spillway_steep_chute_v2_unit.py tests/test_spillway_steep_chute_export_unit.py -q --basetemp=.pytest_tmp\spillway-principle-preview`。
- 首个默认工况命名修正验证结果：计划内面板/导出回归 `31` 个测试通过；命令为 `.\.venv\Scripts\python.exe -m pytest -p no:hypothesispytest tests/test_spillway_steep_chute_panel_unit.py tests/test_spillway_steep_chute_export_unit.py -q --basetemp=.pytest_tmp\spillway-case-default-name`；泄水渠专项回归 `67` 个测试通过，命令为 `.\.venv\Scripts\python.exe -m pytest -p no:hypothesispytest tests -q -k spillway_steep_chute --basetemp=.pytest_tmp\spillway-case-default-name-full`。
- 湿周符号修正验证结果：先跑数据与导出回归 `26` 个测试通过；最终面板、导出、计算原理专项回归 `51` 个测试通过，命令为 `.\.venv\Scripts\python.exe -m pytest -p no:hypothesispytest tests/test_spillway_steep_chute_panel_unit.py tests/test_spillway_steep_chute_export_unit.py tests/test_spillway_steep_chute_v2_unit.py -q --basetemp=.pytest_tmp\spillway-chi-panel`。
- 计算书开头固定格式对齐验证结果：导出专项回归 `6` 个测试通过，命令为 `.\.venv\Scripts\python.exe -m pytest -p no:hypothesispytest tests/test_spillway_steep_chute_export_unit.py -q --basetemp=.pytest_tmp\spillway-report-frontmatter`；面板与导出组合回归 `31` 个测试通过，命令为 `.\.venv\Scripts\python.exe -m pytest -p no:hypothesispytest tests/test_spillway_steep_chute_panel_unit.py tests/test_spillway_steep_chute_export_unit.py -q --basetemp=.pytest_tmp\spillway-report-frontmatter-panel`。
- 纵断面图文字安全布局验证结果：先按 TDD 跑出旧逻辑失败，再完成修复；相关回归 `60` 个测试通过，命令为 `.\.venv\Scripts\python.exe -m pytest -p no:hypothesispytest tests/test_spillway_steep_chute_plotting_unit.py tests/test_spillway_steep_chute_panel_unit.py tests/test_section_plot_layout_unit.py -q --basetemp=.pytest_tmp\spillway-profile-text-layout`。
- 计算原理段落符号下标修复验证结果：新增 helper 与页面渲染专项 `2` 个测试通过；计划内三组回归在当前沙箱中已有 `44` 个测试通过，剩余 `8` 个均因 pytest `tmp_path` 临时目录权限报错，未出现业务断言失败；提升权限重跑审批超时，未能完成导出临时文件类用例。
- 多流量段 Excel 模板说明页改造验证结果：计划内回归 `84` 个测试通过；openpyxl 手工核对通过，确认 `导入模板` 仍是活动表、32 个表头未变、8 张说明页齐全、23 个 `SECTION_TYPES` 均被说明页覆盖，`结构形式` 列 `D3:D500` 已有下拉提示。
- 批量输入列顺序优化验证结果：计划内批量/导入/隧洞/暗涵回归 `86` 个测试通过，水面线同步回归 `11` 个测试通过；openpyxl 手工核对通过，确认 `导入模板` 为活动表，`Q:V` 为渡槽和圆拱直墙关联参数，`AE/AF` 为复式梯形末尾字段，下拉和说明页覆盖均保留。
- 旧无 X/Y Excel 兜底映射修复验证结果：新增用例先确认旧逻辑失败，修复后聚焦导入回归 `3` 个测试通过；后续模板格式修复后，`tests/test_batch_import_dimension_preserve_unit.py` 全文件 `36` 个测试通过。
- 多流量段官方 Excel 模板格式修复验证结果：新增格式测试先确认活动页和说明页样式失败，修复后批量模板全文件回归 `36` 个测试通过；openpyxl 手工核对通过，确认活动页、sheet 顺序、表头、冻结、筛选、D列下拉、R/V/Y/Z 样式、A3:AF500 整行斑马纹和字段速查顺序均正常。
- 合并前总体验证结果：`193` 个测试通过；命令为 `.\.venv\Scripts\python.exe -m pytest -p no:hypothesispytest tests/test_aqueduct_kernel_unit.py tests/test_batch_import_dimension_preserve_unit.py tests/test_culvert_arch_panel_batch_unit.py tests/test_culvert_kernel.py tests/test_spillway_steep_chute_export_unit.py tests/test_spillway_steep_chute_panel_unit.py tests/test_spillway_steep_chute_plotting_unit.py tests/test_spillway_steep_chute_v2_unit.py tests/test_section_plot_layout_unit.py tests/test_tunnel_arch_wall_height_panel_batch_unit.py tests/test_tunnel_flat_bottom_circular_panel_batch_unit.py -q --basetemp=.pytest_tmp\merge-master`。
