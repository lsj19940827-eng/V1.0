# 当前上下文

## 正在做什么

- 已完成多工况结果导航的“按工况过期”和“删除工况失效提示”修复，并将有效改动收口到 `master`。

## 上次停在哪

- 已统一明渠、渡槽、暗涵、隧洞、有压管道 5 个面板的按工况过期记录。
- 已补充共享导航、5 个真实面板、复制到所有、新增工况、删除工况的回归测试。
- 已同步相关 PRD，并完成合并前回归验证。

## 关键决定

- 未计算或目标工况结果过期时，点击左侧工况标签不再弹“先重新计算”提示，用户可以继续修改其他工况。
- 只有目标工况结果仍有效时，点击左侧工况标签才会自动定位到右侧对应工况结果。
- 右侧结果快捷导航按被点击的目标工况判断：目标工况参数修改后才提示过期，未修改工况仍可跳到旧结果。
- 右侧目标工况参数过期提示为“因当前工况参数被修改，致使计算结果过期。请执行计算后再查看计算结果。”
- 新增工况不影响已有结果跳转；复制到所有只标记被覆盖工况过期；删除工况后旧结果导航整体失效，提示工况已删除且旧结果可能与当前工况序号不一致。
- 本次只改交互判断、提示文案、测试和 PRD，不改计算逻辑、结果数据结构和导出规则。
- 当前验证已通过：148 个相关测试通过，0 个失败。
- `data/siphon_autosave.json` 仅为本地保存时间变化，不纳入本次代码合并。
