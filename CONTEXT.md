# 当前进度

- 当前正在做什么：已把主工作区、`anon-pressure-loss` worktree、`unnamed-pipe-transition-fix` worktree 的未提交改动并入 `master`，并完成合并后的验证收口。
- 上次停在哪个位置：三处草稿均已落到 `master`；为兼容精简测试桩，已给 `water_profile/panel.py` 增加新工具函数导入的兜底处理。当前相关回归结果为 111 通过、5 失败。
- 近期关键决定和原因：
  - 匿名普通有压管道行按独立承压管段进入有压管道计算窗口、结果回写和静默重算，避免空名称行沿用相邻整组结果而导致损失与水位递推不一致。
  - 空名称有压管道与隧洞、闸等相邻时，只在插段判断阶段按当前位置临时识别进口/出口，不改预处理分组规则，避免误伤现有有压管道提取链路。
  - 紧邻有压同类结构时，不再跨过去借远处渠道流速，而是明确记为“该侧无渐变段、对应损失为 0”，同时在详情和备注里保留原因。
  - 这次先把三处草稿都落成提交再并入 `master`，冲突集中在少数重叠文件，便于逐个核对并保留两边能力。
  - 当前剩余 5 个失败都落在“渐变段双侧插入”相关断言，和本次合并后新增的导入兼容问题无关；后者已经通过兜底处理清掉。
