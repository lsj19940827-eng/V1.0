# 当前进度

- 当前正在做什么：本轮已完成 `xx渠` 末尾连续承压命名有压段的逐行正式计损改造，范围只覆盖 `有压管道 / 定向钻 / 顶管`，并同步补齐表3回写、静默重算、说明弹窗和项目文档；相关 7 个测试文件已通过，当前处于待提交并合入 `master` 状态。
- 上次停在哪个位置：已把提取器拆分、窗口计算、结果回写和静默重算主链打通；本次已补齐 README / ARCHITECTURE / PRD / CONTEXT 记录，并把有压损失说明弹窗文案对齐到“逐段承压成员”口径。
- 近期关键决定和原因：
- `xx渠` 末尾连续承压中的命名 `有压管道 / 定向钻 / 顶管`，改成按“上一承压/普通行 -> 当前行”逐段正式计损；原因是只补列38显示不够，必须让总损失、累计损失和水位也一起按逐段值递推。
- 命名整组结果仍保留在有压管道计算窗口和汇总里，但表3不再把整组总损失写到出口行；原因是要避免同一条尾段同时存在“逐段口径”和“整组出口口径”两套结果。
- 静默重算现在对命名逐段成员也优先使用窗口覆盖结果，并主动清空旧 `head_loss_siphon / external_head_loss`；原因是要彻底切掉旧整组损失对水位递推的干扰。
