# 当前进度

- 当前正在做什么：整理《水击计算_水力计算手册(第二版) [李炜]》OCR Markdown 的本地图引用。
- 上次停在哪个位置：用户已删除 3 张重复图，正文重复位置已改为复用前面 3 张高清本地图。
- 近期关键决定和原因：
- 水锤整线验算按“连续有压线末端阀门启闭”解释；`Ts` 是末端阀门启闭时间，沿线采样只做安全校核，不表示每个采样点都有阀门。
- 多成员整线相时按 `2 × Σ(Li / ai)` 累加；每个成员继续使用自己的 `D / Q / v0 / 管材 / E / a`。
- 水击波速当前采用均质薄壁圆管简化式；该式适用于可近似为均质薄壁圆管的管材，暂未按 PCCP、钢筋混凝土衬砌管、埋藏式钢管等复杂 `K` 类型分别计算。
- OCR 图片原始外链仅保留在本地资源目录的 `README.md` 记录中，正文不再依赖外部图片网址；本地图已从原始 PDF 重新裁剪，清晰度高于 PaddleOCR 外链小图；重复图 1-3-2、图 1-3-3、图 1-3-4 不再单独存副本。
- PCCP 管材显示采用简称方案：`PCCP管0.013 / 0.014 / 0.015` 仅在纵断面 `管材（管径）` 展示层改为 `PCCP管(n=...)`，不改 Excel 原始值和计算逻辑。
