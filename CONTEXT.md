# 当前进度

- 当前正在做什么：正在准备 `1.2.3` 发版，已合并水面线稳定性修正和补丁安装保护改动，待完成验证与正式发布。
- 上次停在哪个位置：停在两组改动都已提交完成、正在合并进 `master` 的阶段，下一步就是跑回归并执行发版。
- 近期关键决定和原因：
  - 对“已存在渐变段/连接段”的节点序列，后续计算不再直接整段跑普通几何重算，而是先只刷新真实节点，再按辅助行现有位置回补内部桩号；这样不会把金花寨这类下游真实节点整体推后。
  - 自动连接段和渐变段在表格往返时必须保留结构类型、坐标和规则上下文；这样旧项目重开或插段后再计算时，不会因为辅助行信息丢失而漂桩号。
  - `0` 长度渐变段不再写回表格；因为这类行没有展示和计算价值，只会让拓扑和桩号链路变乱。
  - 保留内部 `ip_number`，新增显示用 `display_ip_number`；这样特殊建筑进出口可以不占显示编号，同时不影响现有计算链和专项结果。
  - 隧洞/渡槽/倒虹吸/有压管道/定向钻/顶管的进出口只显示“名称 + 简称 + 进/出”，不占显示 IP；矩形暗涵保持原规则，避免误伤旧表。
  - 主表 IP 单元格同时写入 raw/display 元数据，表格回读优先取元数据；这样“黄角坝隧进”这类没有数字的文本也不会被误判成 `0`。
  - 纵断面导出里，坡降行边界遇到“右侧本来就有特殊建筑进/出整高竖线”时，边界改跟随后一个整高竖线；这样矩形暗涵末端不会再在坡降行里多补一根短线。
  - 通用补丁最低覆盖版本收紧到 `1.1.9`；因为 `1.1.8` 及更老版本与当前 `_internal` 差异太大，补丁校验成本远高于收益。
  - 构建阶段新增“覆盖范围过大就不发补丁”的兜底规则；这样以后即使打包目录再次大变，也不会再把高风险补丁发给用户。
  - 补丁安装继续复用 `validate` 阶段，但会显示“正在解压补丁包”“正在校验补丁适用性（x/y）”；这样不改窗口结构，也能让用户知道程序还在工作。
