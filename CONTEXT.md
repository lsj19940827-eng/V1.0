# 当前进度

- 当前正在做什么：已完成“普通渠道末尾有压段在同一 DXF 中上下分表导出”的代码和测试，正在收口文档与相关回归。
- 上次停在哪个位置：末尾有压段此前仍跟着渠道表头一起导出，没有单独画成 5 项有压表，也没有把合并 DXF 下方的断面汇总表和 IP 表一起下移。
- 近期关键决定和原因：
- 末尾分表只在普通渠道项目里启用；纯 `xx管` 和连续承压 `xx渠` 继续走原来的单张 5 项表，避免影响既有整线口径。
- 末尾分表只改 DXF 导出，不改 TXT 导出；同一 DXF 采用“上渠道、下有压”的上下布局，用户更容易核对。
- 末尾有压子表继续沿用严格 `xx管` 纵断面校验；若没导入或覆盖不足，仍然直接给出现有提示，避免导出半对半错的图。
- 合并 DXF 在出现末尾分表时，要把断面汇总表和 IP 表一起下移，不能只挪其中一张，避免重叠。
