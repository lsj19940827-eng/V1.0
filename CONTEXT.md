# 当前进度

- 当前正在做什么：实现“有压段-隧洞-有压段”场景下，下方 `xx管` 纵断面表按连续有压段拆成共享表头的横向多子表。
- 上次停在哪个位置：已在 `codex/xxpipe-split-subtables` worktree 完成测试先行和代码改造；核心 `xxpipe / combined DXF` 回归 `87` 通过、`0` 失败，更大范围的 xxpipe 链路套件两次超时，暂未作为完成依据。
- 近期关键决定和原因：
- 下方 `xx管` 表继续只保留 `有压管道 / 定向钻 / 顶管`；当这些有压段被隧洞分隔成多段连续 span 时，不再压成一整张连续下表，而是保留 1 套共享表头，并把各段画成从左到右排列的独立子表，方便用户在 CAD 里自由拼接。
- 多子表之间使用固定空隙；单段场景保持原样，不新增用户开关。Excel 口径不变；为了让同一套绘图规则保持一致，TXT 绘图命令也同步按多子表布局输出。
- 下方表格横线与管中心线都按子表断开，避免空隙区域继续出现跨段连线；每个子表的首尾桩号同时按整高边界封口，防止 gap 两侧留下边框缺口。整张图占宽同步按“共享表头 + 多子表 + 固定空隙”重新计算，给后续汇总表和 IP 表继续留出正确位置。
- 顺手修复了表3里“有压管道水力计算”按钮的交互 bug：当表1/表3结果失效时，不再把按钮直接禁用成“点了没反应”，而是保持可点击，并在点击后继续通过现有前置校验提示用户先重新执行断面批量计算。
