# 当前上下文

## 正在做什么

- 给 `隧洞-圆拱直墙型` 增加“按加大流量净空比例反推断面尺寸”弹窗助手入口。
- 入口放在隧洞设计面板的圆拱直墙参数组内，只作为当前工况辅助工具，不改变主计算、批量、Excel 或导出流程。

## 上次停在哪

- 已完成内核反推函数、弹窗、面板入口和采用回填逻辑。
- 已补齐新增内核测试、弹窗测试、面板入口测试，并通过隧洞相关回归。
- 正在把入口按钮文案改为完整名称，并把按钮底色改成和“计算”按钮一致的青色主按钮样式。

## 关键决定

- “净空比例”按净空面积比解释；目标比例用于加大流量工况。
- 固定 `H/B` 与 `θ` 后，先在单位断面求目标水深，再用曼宁相似律反推 `B`，最后回到现有水深和输出函数复核。
- 弹窗内临时修改 `Q加大` 或 `θ` 不影响主面板；只有点击“采用到当前工况”才回填 `θ、B、H直`，且保留 3 位小数。
- 校核失败时只提示原因并禁用采用，不自动替用户改参数。
- 入口按钮使用完整文案，不再使用省略号；按钮样式采用 `PrimaryPushButton`。
