# 当前进度

- 当前正在做什么：已确认 `1.2.5` 发版范围并跑完相关回归，正在基于 `master` 执行正式发版。
- 上次停在哪个位置：本地收口后的未提交改动已确认全部纳入本次版本，发布说明采用通用归纳口径。
- 近期关键决定和原因：
- `1.2.5` 按补丁版本发布，沿用项目正式发版脚本统一完成改版本、打包、提交、打 tag、发布 Release 和更新在线版本信息，避免手工漏步骤。
- 压力管道特性表里的“渠首水位 / 渠末水位”默认按各流量段自己的首个/最后一个有效水位输出；但若后一流量段首点正好是前一流量段的连续终点，则两段共用这个切段点水位，避免前段末值和后段首值错开。
- 流量段摘要总长度现在按“当前节点到下一节点”的连续桩号累加；跨流量段时，下一流量段首行桩号也算上一流量段终点，避免把上一段截短到本段最后一个同段节点。
- 连续承压整线导出里，普通有压子段若只剩 1 个纵断面点，继续按“边界占位”处理并回退整线 DXF；隧洞生成段不参与这条回退规则。
- 有压管道弹窗里导入或清空纵断面后，要立即同步到 `PressurePipeManager`，不能再等用户点“开始计算”才写入；否则图2和图1会看到两份不同状态。
- xx管/连续承压整线导出在同桩号合并后，要保留整组节点的 identity 候选，并在代表节点命不中整线纵断面时回退重试。
