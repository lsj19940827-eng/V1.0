# 当前进度

- 当前正在做什么：已在独立分支里把普通有压管道渐变段长度的正式计算口径修正为与倒虹吸一致的 `5h/6h`，并同步修正详情弹窗说明。
- 上次停在哪个位置：代码、测试和项目记录已同步；本次直接相关验证结果为 34 通过、0 失败。
- 近期关键决定和原因：
  - 普通有压管道/定向钻/顶管的渐变段长度正式计算，统一按相邻渠道设计水深的 `5h/6h` 处理，不再让插入阶段和双击详情各用一套口径。
  - 渐变段详情继续保留“公式/规范值、规则目标长度、物理上限、最终采用长度”四层信息，这样 `12.384 -> 6.000` 这类压缩结果能直接看懂原因。
  - 本次不改渐变段插入拓扑、不改补段/合并策略，只修正长度口径一致性与说明展示，避免把既有拓扑判断一起带动。
