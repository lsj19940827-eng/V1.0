# 当前进度

- 当前正在做什么：将有效分支改动合并进 `master`，并清理不再需要的分支。
- 上次停在哪个位置：分支相关回归已通过，准备提交当前改动后合并到主干。
- 近期关键决定和原因：
- 本次合并包含倒虹吸局部系数/纵断面清理、Excel 有压管道 PCCP 管材识别、水锤与表3链路测试补强，以及相关 PRD 更新。
- `PCCP管` 和 `PCCP管0.013` 统一按预应力钢筒混凝土管 n=0.013 的 `f / m / b` 参数计算。
- `PCCP管0.014`、`PCCP管0.015` 分别归一到已有 n=0.014、n=0.015 参数；表3核心也补齐 n=0.015。
- `PCCP管0.012` 等不支持值不阻断 Excel 导入，保留单元格原文，通过 InfoBar 提示并按 n=0.013 继续计算。
- 该规则只作用于 `有压管道 / 定向钻 / 顶管`，不改变倒虹吸的管材糙率逻辑。
