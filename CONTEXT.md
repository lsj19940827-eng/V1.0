# 当前进度

- 当前正在做什么：补齐赛金支渠、苏溪支渠两套批量计算示例入口，并同步测试与 PRD。
- 上次停在哪个位置：已确认 batch 面板和水面线面板各自维护一套示例菜单，现有模式是静态注册 8 套示例。
- 近期关键决定和原因：
- 这次继续沿用现有静态注册方式，只补菜单入口、路径映射和代理方法，避免把小需求扩成配置化重构。
- `赛金支渠`、`苏溪支渠` 统一追加为示例九、示例十，保持两个界面的示例顺序完全一致。
- `苏溪支渠` 文件名统一为 `data/苏溪支渠批量计算用表.xlsx`，去掉旧文件名中的多余空格，避免代码路径、打包清单和本地文件名继续分叉。
- `tools/build.py` 不改；现有打包逻辑已经自动扫描 `data/` 目录，文件名统一后下次打包会自然带上正确文件。
