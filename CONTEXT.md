# 当前进度

- 当前正在做什么：水锤验算 `v0` 修正已提交到 `master`，主干包含本轮有效代码和文档改动。
- 上次停在哪个位置：核心 helper、有压分组提取、单管/整线弹窗、保存展示、Excel 导出和文档已同步；提交后相关测试 36 个通过。
- 近期关键决定和原因：
- 勾选“考虑加大流量”时，水锤 `v0` 采用 `Q加大/A`；未勾选时采用 `Q设计/A`。
- 历史保存的 `v0` 只在缺少有效管径或流量、无法自动计算时兜底，不再优先覆盖当前工况。
- 本次不改 `Hmax/Hmin`、承压、负压和免验算公式，只修正流速来源。
- `data/siphon_autosave.json`、本地快捷方式和临时参考资料不进入本次主干提交，只保留在本地工作区。
- 当前没有额外本地 worktree，也没有可删除的本地功能分支。
