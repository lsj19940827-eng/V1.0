# 当前进度

- 当前正在做什么：已修完赛金支渠连续承压链“前缀段身份撞中段8”问题，并完成真实样表复跑与回归验证。
- 上次停在哪个位置：先前已修过“失败抬头串名”，但真实复跑后发现 `苟家湾（前缀段）` 仍和 `苟家湾（中段8）` 共用了同一个 identity，导致中段8被跳过并借到前缀段失败记录；本轮已把身份冲突修掉。
- 近期关键决定和原因：
- 平底圆形固定采用“直径 D + 平底宽 B”两输入，程序自动推导总高 `H_total`，并由 `app_渠系计算前端/tunnel/geometry.py` 作为唯一几何真源，避免计算、绘图、DXF 和表3复算口径分叉。
- 表3对 `隧洞-平底圆形` 继续只允许“表1同步 / 共享结果导入”两种来源；双击选择、手输、粘贴和批量改写都拦截，避免手工新建和导入链路出现两套口径。
- 第38列普通可编辑行继续使用独立人工采用值 `pressure_pipe_loss_override_m`；有详情时在弹窗里保存/恢复，无详情时直接进入单元格编辑。
- `大石包（前段）/（后段）` 的现有展示和逐段计算口径继续保留，本轮只修失败归属。
- `named_row_segment` 底层小分组只使用未加后缀的基础名称，链成员展示名不再反向污染逐段校验标签。
- `named_row_segment` 失败返回时，`panel.py` 会把失败原因抬头同步改成当前链成员名称；只替换抬头，不改原因正文。
- 链汇总继续只认成员自己的正式 identity；父组只负责窗口汇总，不能把前缀段失败原因复用到中段成员。
- 锁定逐行承压行继续保持单元格只读，手动入口统一收在“双击列38详情弹窗底部”；手动值持久化到 `pressure_pipe_window_override.manual_total_head_loss`，并优先驱动列38、总损失、累计损失和后续水位递推。
- `支渠` 链首前缀段改为优先继承原始行上的 `pressure_pipe_row_identity`，不再按插入渐变段后的当前位置重建 `flow-row`；这样前缀段不会再与中段成员撞 identity。
