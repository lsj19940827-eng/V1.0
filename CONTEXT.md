# 当前进度

- 当前正在做什么：执行 `1.2.6` 正式发版，收口 xx 管纵断面导入提示与标准纵断面重复竖线修复。
- 上次停在哪个位置：已经完成本次相关代码、测试和 PRD/使用说明更新，并跑通发版前回归与 5 秒启动冒烟，正在执行正式发布。
- 近期关键决定和原因：
- 这次发版按 `patch` 从 `1.2.5` 升到 `1.2.6`，沿用 `tools/release.py` 正式流程，不绕过版本、打包、Release 和 Gist 更新链路。
- 发布范围明确排除 `data/siphon_autosave.json` 这类本地自动保存时间戳文件，避免脚本里的 `git add -A` 把无关本地状态带进正式版本。
- xx 管纵断面导入界面新增固定“导入前说明”，并把覆盖不足报错改成“需要到哪、当前到哪、差多少、允许多少、应怎么补”的直接提示，减少用户来回排查。
- 标准纵断面 DXF/TXT 的节点竖线规格统一先按真实导出节点去重，修掉同一桩号重复画竖线的问题，同时保留特殊进出口、末列右边框和 BC/EC 分段规则。
- 已完成本次 3 个相关测试文件回归，结果为 `81` 通过、`0` 失败；`main.py` 5 秒启动冒烟通过，程序可正常拉起并保持存活。
