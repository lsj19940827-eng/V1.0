# 当前进度

- 当前正在做什么：已把 xx管 夹带隧洞整线能力、固定 5 行表头统一，以及 `1.2.1` 启动热修一起收口到 `master`，当前正在做最终验证与分支清理。
- 上次停在哪个位置：停在 `master` 已经合入 xx管 主线，但 `codex/hotfix-1.2.1-startup` 里还有一组未提交的启动修复改动的位置；本次把这组热修验证、提交并合并到了主分支。
- 近期关键决定和原因：
  - xx管 mixed route 继续只导入 1 份整线纵断面 DXF，非隧洞段走 DXF，隧洞段按“进口底高 + 坡降 i + 起终桩号”生成，并统一落到 `profile_segments`；这样不用给每段隧洞单独准备 DXF。
  - xx管 固定 5 行模板里的第 5 行表头统一改为 `管材（管径）`；因为这一行只表达材质、管径或隧洞断面参数，不再混入长度单位，用户理解更直接。
  - 打包清单和打包前校验同时保留 `utils.*` 与 `推求水面线.utils.*` 两条导入路径，并在 `推求水面线/utils/__init__.py` 中显式接管顶层 `utils` 别名；这样正式包启动时不会再被同名包抢占。
