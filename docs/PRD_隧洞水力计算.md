# 隧洞水力计算模块 — 产品需求文档 (PRD)

> **版本**: v1.26
> **创建日期**: 2026-02-22  
> **最后更新**: 2026-05-09
> **状态**: 已实现  

---

## 1. 概述

### 1.1 模块定位

隧洞水力计算模块是「渠系建筑物水力计算系统」的核心子模块之一，负责隧洞断面的水力设计计算。模块集成在主应用侧边导航中，通过 `TunnelPanel` 提供完整的交互界面。

### 1.2 功能摘要

- 支持 **5种断面类型**：圆形、平底圆形、圆拱直墙型、马蹄形标准Ⅰ型、马蹄形标准Ⅱ型
- 基于 **曼宁公式** 进行水力计算
- 对圆形 / 圆拱直墙型 / 马蹄形自动搜索满足约束条件的 **最优断面尺寸**
- 对平底圆形固定采用 **直径 D + 平底宽 B** 两输入，程序自动推导 **总高 H_total**
- 对圆拱直墙型支持可选 **直墙高度 H直**：留空时沿用自动搜索，填写时按 `B + H直 + θ` 固定几何核算
- 对圆拱直墙型提供 **按加大流量净空比例反推断面尺寸** 弹窗助手：按 `Q加大 / 目标净空比例 / H/B / θ` 反推 `B / H_total / H直`
- 计算 **设计流量** 与 **加大流量** 两种工况
- 支持 **详细计算过程** 输出（含公式推导）
- 支持 **工况对比表**，汇总不同流量下的水深、流速和洞身关键尺寸
- 导出 **DXF断面图**、**Word计算书**；多工况 DXF 可追加参数对比表
- 内置 **断面图matplotlib预览**（单工况单图叠加设计水位与加大水位）
- 批量/共享/导入链路统一使用 `隧洞-平底圆形`，表3只允许导入复算，不开放手动新选
- `暗涵-圆拱直墙型` 不属于本模块；当前只复用圆拱直墙几何 helper，净空、表1/表3、断面汇总和导出口径都回到暗涵设计链路处理。

### 1.3 当前实现同步（2026-04-17）

- 加大流量输入已统一为 `按比例` / `按Q加大` 两种模式，保留原“考虑加大流量比例系数”开关，默认仍按比例模式。
- `按比例` 可留空，留空时继续按规范表自动取值；`按Q加大` 输入的是加大后的总流量，必须大于设计流量 `Q`。
- 多工况保存新增 `inc_mode / inc_q_text`；旧工程若只有 `inc_pct`，重开后仍按原比例方式回显。
- 切换模式时只保留一项正式输入，另一项只做灰色换算提示；关闭开关时只隐藏，不清空上次输入。
- 首次进入面板时也必须立即按当前模式收口显示；默认只显示“按比例”输入，不得把“按Q加大”一栏一起显示出来。
- 结果页、TXT、Word、DXF 会统一写出输入方式、用户输入值和系统换算值。
- 加大流量链路的内部计算已统一为高精度直传：比例、倍率和 `Q加大` 不会在进入后续水深 / 流速 / 净空计算前提前 round。
- 加大比例统一显示为 3 位小数，公式代入中的比例小数 / 倍率统一显示为 5 位。

### 1.4 当前实现同步（2026-04-23）

- 点击【清空】只清除计算结果、断面图、结果导航和导出缓存；左侧输入参数、加大流量模式与多工况列表保持用户当前输入，不再恢复程序默认值。

### 1.5 当前实现同步（2026-04-29）

- `隧洞-圆拱直墙型` 新增可选 `直墙高度 H直` 输入。留空时继续由程序搜索 `H_total`，再按 `H直 = H_total - H_arch` 推导；填写时必须同时填写底宽 `B`，并按用户输入固定几何核算。
- 圆拱几何说明统一输出为 `R拱 = (B/2) / sin(θ/2)`、`H拱 = R拱 × (1 - cos(θ/2))`、`H_total = H直 + H拱`。
- 固定 `H直` 后只校核设计流量、加大流量、流速和净空；若固定断面不满足要求，直接返回失败提示，不自动回退到寻优。
- 单项面板、批量表1、Excel 模板、共享结果、表3导入复算、DXF 和详细报告均保留 `H直` 来源说明。

### 1.6 当前实现同步（2026-04-30）

- `隧洞-圆拱直墙型` 新增“按加大流量净空比例反推断面尺寸”弹窗入口，入口只在圆拱直墙型参数组内显示，并使用青色主按钮样式。
- 弹窗只服务当前工况，读取主面板解析后的 `Q / Q加大 / n / 坡降 / 流速范围 / θ` 作为摘要；弹窗内临时修改 `Q加大` 或 `θ` 不会立即影响主面板。
- 用户输入 `Q加大 / 目标净空比例 / H/B / θ` 后，系统按净空面积比反推 `B / H_total / H直`，再用现有圆拱直墙型水深和输出函数复核；目标净空比例默认 `15%`。
- 弹窗会跨重启记忆用户上次成功计算时的 `目标净空比例 / H/B / θ`；`Q加大` 仅在主流程设计流量和加大流量来源未变化时恢复，否则使用当前主流程解析值。
- 反推成功结果必须按三位小数回填后的 `θ / B / H直` 重新复算并真实达到目标净空比例；若边界工况仍不足，优先以 `0.001m` 微增 `H直`，避免采用后主流程失败。
- 只有通过最小宽度、最小高度、设计流速、加大流速、加大净空高度 `0.4m` 和净空面积比校核后，才允许点击“采用到当前工况”。
- 采用结果只回填 `θ / B / H直`，保留 3 位小数，并标记已有结果过期；不自动触发主计算，也不进入批量、Excel 或导出计算书的单独入口。

### 1.7 当前实现同步（2026-04-30）

- “按加大流量净空比例反推断面尺寸”弹窗的断面预览会按圆拱直墙真实轮廓绘制浅蓝色水域；当水深进入拱部时，水域下部贴合直墙宽度，上部沿圆弧边界收窄，不再显示成窄矩形。

### 1.8 当前实现同步（2026-04-30）

- 计算结果详情顶部新增“重点结果汇总”普通卡片，放在每个工况结果块的最前面，不做滚动悬停。
- 汇总内容包含设计/加大流量下的水深、流速、净空高度、净空比例，以及圆形直径、平底圆形平底宽和总高、圆拱直墙型底宽/总高/高宽比 H/B/直墙高度/拱部参数、马蹄形半径或等效直径等关键尺寸；平底圆形和圆拱直墙型同时具备 H 与 B 时显示 `H/B = H_total / B`。
- 未启用加大流量或加大工况无可用结果时，不显示空值指标组，只显示短提示。
- Word 报告在每个工况计算过程前新增同口径“重点结果汇总”表；计算公式、DXF、批量计算和有压管道相关口径不变。

### 1.9 当前实现同步（2026-04-30）

- 反推弹窗的 `Q加大` 默认跟随当前主流程：按比例留空时自动查表，按比例手填时使用手填比例，按 Q 加大时使用主流程手填 Q加大。
- 主流程未启用加大流量时，反推弹窗仍按设计流量自动查表给出建议 `Q加大`，便于继续做加大流量净空反推。
- 弹窗内手改 `Q加大` 只作为当前试算值；只有主流程设计流量和加大来源未变化时才跨重启恢复，避免设计流量改变后沿用旧值。

### 1.10 当前实现同步（2026-04-30）

- 隧洞设计结果区新增「工况对比」页签，单工况也显示一行，多工况用于横向比较。
- 对比表后续已升级为水力结果表与结构尺寸表两张表；水深和流速仍同时显示设计工况与加大工况，未启用加大流量或无加大结果时，加大列留空。
- 洞身周长和洞身断面积固定采用完整洞身几何口径，不使用过水湿周或过水面积。
- 单工况和多工况 DXF 导出都会在断面图网格下方追加两张同口径对比表。

### 1.11 当前实现同步（2026-04-30）

- DXF 中文文字样式默认宽度因子统一为 `0.7`。
- 隧洞多工况参数对比表在 DXF 中按表头和数据内容自动计算列宽，避免长表头或长工况名称超出单元格。
- DXF 对比表表头采用两行显示：第一行指标名，第二行单位；流量和面积单位使用 CAD MTEXT 上标控制码显示为 `m³/s`、`m²`，与“导出全部 DXF”的既有写法保持一致。
- 界面「工况对比」表和 DXF 输出都保留上标单位语义；DXF 中由 MTEXT 控制码负责兼容 CAD 显示。

### 1.12 当前实现同步（2026-04-30，工况对比统一）

- 隧洞原“工况对比”单表升级为“水力结果对比表”和“结构尺寸对比表”两张表，仍只复用已有 `all_results`，不重新计算。
- 水力表统一显示设计/加大流量、水深、流速和明确净空重点结果；未启用加大流量时，加大列留空。
- 结构表保留隧洞关键尺寸：`底宽 B`、`直径 D`、`半径 r`、`洞总高 H`、`高宽比 H/B`、`直墙高度 H直`、`洞身周长`、`洞身断面积`，其中周长和面积继续使用完整洞身几何口径。
- 单工况和多工况 DXF 都在断面图下方追加两张对比表；Word 报告在逐工况详细过程前先写两张总体对比表。

### 1.12.1 当前实现同步（2026-05-01，工况对比重点结果）

- 工况对比水力表同步重点结果口径，新增设计/加大净空高度和设计/加大净空比例列，未启用加大流量时加大列留空。
- 结构尺寸表补齐 `等效直径 2r`、`高宽比 H/B`、`拱顶圆心角 θ`、`拱半径 R拱`、`拱高 H拱` 等重点结构派生值；平底圆形和圆拱直墙型按 `H_total / B` 输出 H/B，不适用的结构形式对应单元格留空。
- 删除原“控制余量类型 / 控制余量”摘要列，工况对比只保留设计/加大净空高度和净空比例等明确重点结果列。

### 1.13 当前实现同步（2026-05-01，断面图水位线）

- 单工况断面图统一显示一张设计流量图；启用加大流量且 `h_increased` 有效时，在同一坐标轴叠加“加大水位”虚线和 `h加大=x.xxm` 标注。
- 多工况断面图每个成功工况一个小图，按默认 2 列、窄窗口 1 列和断面图区域内部滚动承载；设计水位保留蓝色实线和水体填充，启用加大流量且加大水深有效时，在同图叠加“加大水位”虚线和 `h加大=x.xxm` 竖向双向水深尺寸标注。
- 圆形、平底圆形、圆拱直墙型和马蹄形隧洞均按各自真实断面宽度绘制加大水位线；加大水深缺失、为 0 或无效时不绘制。

### 1.13.1 当前实现同步（2026-05-07，断面图绘制底座统一）

- 隧洞界面断面图继续作为四类建筑物统一视觉参考，但绘制入口已收口到 `section_shapes.py` 与 `section_plotting.py`：圆形、平底圆形、圆拱直墙型和标准马蹄形的轮廓、水域填充、水位线、尺寸标注、标题、网格和视图范围由共享绘图底座完成。
- 隧洞面板继续只负责工况组织、单工况叠加 / 多工况布局和隧洞业务字段取数；计算内核、输入表单、DXF 导出和 Word 导出规则不变。
- 圆拱直墙型和平底圆形仍复用隧洞几何 helper 作为几何真源，暗涵只复用几何，不复用隧洞业务语义。

### 1.13.2 当前实现同步（2026-05-08，单工况单图叠加）

- 隧洞普通单项入口和只有 1 个成功工况的汇总入口均统一为一张断面图，避免同一模块内入口显示不一致。
- 设计水位继续显示水体填充和蓝色实线；启用加大流量且 `h_increased` 有效时，在同图叠加“加大水位”虚线、文字和 `h加大=x.xxm` 标注。
- 界面断面图的水平尺寸文字改用固定点数偏移，保持底宽 `B` / 直径 `D` 等文字与尺寸箭头分离；计算、DXF 和 Word 规则不变。
- 多工况仍保持每个成功工况一个小图；本条说明不改变计算公式、输入表单、DXF 或 Word 导出。

### 1.13.3 当前实现同步（2026-05-08，多工况断面图清晰化）

- 隧洞多工况断面图改为宽度足够时默认 2 列；只有窗口明显窄时退到 1 列，重点解决 3 列场景下横向裁切或尺寸标注拥挤的问题。
- 断面图页内部承载垂直滚动，画布高度随行数增加；页面工具栏、完整尺寸标注、加大水位线和网格继续保留。
- 双击任意多工况小图可打开该工况的大图查看窗口；大图只用于查看，不新增编辑、导出或单独保存入口。
- 本次只优化软件界面里的“断面图”页，不改变 DXF、Word 或 TXT 导出断面图规则。
- 断面图页隐藏或未完成布局时若首次绘图读到临时小宽度，切换到“断面图”页会按真实可视宽度重新布局，避免宽屏多工况误退为单列。

### 1.13.4 当前实现同步（2026-05-09，断面图切页宽度复位）

- 从“工况对比”切回“断面图”时，若已有稳定 2 列布局且工况数、列数未变化，隧洞断面图保持原画布尺寸不重画，避免切页瞬间宽度抖动导致排版变小或横向裁切。
- 首次绘图或列数确需变化时，画布优先按滚动区可视宽度重排，不再取外层窗口最大宽度。
- 断面图重绘后会复位横向滚动位置；断面图页继续以纵向滚动查看更多工况，不改变 2 列 / 1 列规则。
- 本次只修复界面显示，不改变隧洞计算、项目保存格式、DXF、Word 或 TXT 导出规则。

### 1.13.5 当前实现同步（2026-05-09，断面图滚动残影修复）

- 断面图滚动区改为由共享布局显式控制 Matplotlib 画布尺寸，不再让滚动区在拖拽过程中自动拉伸画布。
- 横向滚动保持关闭，纵向滚动保留；拖拽纵向滚动条时同步刷新画布和 viewport，避免复杂隧洞小图滚动过程中短暂残留旧像素。
- 已有稳定布局从“工况对比”切回“断面图”时仍不重新绘制整张图，但会刷新一次当前画布，保持排版、大小和列数稳定。
- 本次只增强界面显示鲁棒性，不改变隧洞计算、项目保存格式、DXF、Word 或 TXT 导出规则。

### 1.13.6 当前实现同步（2026-05-09，断面图项目残留清理）

- 打开 `.qxproj` 项目时，如果隧洞只有输入工况、没有保存的 `all_results`，系统会同步清空断面图画布、旧子图双击映射和断面图滚动布局状态。
- 该处理避免先打开有断面图的项目、再打开无计算结果项目时，上一项目的断面子图残留在“断面图”页。
- 本次只清理界面旧状态，不改变隧洞输入工况、项目保存格式、计算内核、DXF、Word 或 TXT 导出规则。

### 1.13.7 当前实现同步（2026-05-09，隧洞多工况断面图高行高）

- 隧洞多工况断面图保留默认 2 列、窄窗口 1 列和区域内部纵向滚动，但每行画布高度单独提高到 520px，避免圆拱直墙型等高瘦断面在 `set_aspect("equal")` 下被压窄。
- 共享断面图布局仍默认使用 360px 行高；明渠、渡槽、暗涵未传入高行高时保持原显示效果。
- 打开多工况隧洞项目后无需拖动左侧分隔栏，断面图区会按真实比例直接显示可读的大图；本次不改变计算、项目保存格式、DXF、Word 或 TXT 导出规则。

### 1.14 当前实现同步（2026-05-01，工况标签交互）

- 左侧输入区的工况标签只负责切换正在编辑的工况；未计算或目标工况结果过期时，点击标签不弹“先重新计算”提示。
- 只有目标工况结果仍有效时，点击左侧工况标签才会同步定位到右侧对应工况结果。
- 右侧计算结果里的“工况快捷导航”按被点击的目标工况判断是否过期：目标工况参数已修改时提示“结果已过期”，未修改的其他工况仍可定位旧结果。
- 目标工况参数修改时，过期提示为“因当前工况参数被修改，致使计算结果过期。请执行计算后再查看计算结果。”；新增工况不影响已有结果定位。
- 删除工况后旧结果导航整体视为失效，提示为“工况已删除，原计算结果与当前工况序号可能不一致。请执行计算后再查看计算结果。”

### 1.15 当前实现同步（2026-05-08，多工况上限与结果导航）

- 隧洞手动多工况上限从 10 个提升到 30 个；第 31 个工况仍被拦截并提示上限。
- 右侧“工况快捷导航”默认只显示 2 行，超出后提供展开/收起；展开后在导航条内部限高滚动，不再继续挤压详细计算结果。
- 导航标签容器会随结果区可视宽度自动同步，10 个及以上工况仍保持多列排布，避免只显示单列并留下大块空白。
- 结果导航标签压缩为“工况号 + Q”，完整断面类型和说明仍保留在悬浮提示或详细结果中；左侧输入区工况标签保持原显示方式。

### 1.16 当前实现同步（2026-05-09，布局密度优化）

- 左侧输入区工况标签改为紧凑显示：芯片正文优先显示“序号｜Q=流量”，完整工况名、断面类型和设计流量保留在悬浮提示中，约 420px 输入栏下优先保持每行 3 个标签。
- 隧洞多工况断面图继续使用 520px 专用行高，但把等比例坐标轴在网格单元内顶对齐，重点减少圆拱直墙型等断面首屏上方大块空白；计算、项目保存、DXF、Word 和 TXT 规则不变。
- 隧洞多工况断面图改用共享的标题安全边距，顶对齐后第一行子图仍完整显示“工况 + 断面类型”和 `Q/V` 两行标记，避免子图 1、子图 2 的工况标记被画布顶部裁切。
- 用户从“计算结果”页完成多工况计算后，断面图隐藏页签应优先按输出区父容器宽度预渲染，不再相信临时 viewport 宽度；首次切到“断面图”页时，若预渲染画布已追上真实 viewport，只做轻量刷新，不再完整重画。若父容器和 viewport 仍存在时序差，继续保留有限重试兜底；9 个隧洞工况正常目标保持 2 列、5 行、2600px 画布高度，不需要用户先拖拽左侧输入栏。

---

## 2. 文件结构

```
calc_渠系计算算法内核/
  隧洞设计.py              # 计算内核（1154行）

app_渠系计算前端/
  dxf_common.py            # 断面 DXF 共享图层 / 偏移 / 网格布局工具
  dxf_multi_export.py      # 多工况 DXF 导出弹窗与合并导出工具
  section_comparison.py     # 四类设计面板工况对比双表共享框架
  section_shapes.py         # 四类设计面板界面断面图共享几何适配层
  section_plotting.py       # 四类设计面板界面断面图共享 matplotlib 绘图器
  section_plot_layout.py    # 四类设计面板多工况断面图布局、滚动和双击大图工具
  tunnel/
    __init__.py            # 模块声明
    panel.py               # UI面板 TunnelPanel（1069行）
    clearance_sizing_dialog.py # 圆拱直墙型按净空反推尺寸弹窗
    geometry.py            # 断面共享几何辅助（预览 / DXF 共用）
    comparison.py          # 隧洞工况对比字段映射与完整洞身几何指标
    dxf_export.py          # DXF断面图与工况对比表导出

tests/
  test_section_plot_layout_unit.py
  test_section_plotting_unit.py
  test_tunnel_panel_plot_unit.py
  test_tunnel_kernel.py    # 全面测试脚本（1013行，15个测试组）
  test_tunnel_flat_bottom_circular_kernel_unit.py
  test_tunnel_flat_bottom_circular_shared_hydraulic_unit.py
  test_tunnel_flat_bottom_circular_dxf_unit.py
  test_tunnel_flat_bottom_circular_panel_batch_unit.py
  test_tunnel_clearance_sizing_unit.py
  test_tunnel_clearance_sizing_panel_unit.py
  test_tunnel_clearance_sizing_dialog_unit.py
  test_tunnel_comparison_unit.py
```

---

## 3. 断面类型详细规格

### 3.1 圆形断面

| 参数 | 值 | 说明 |
|------|-----|------|
| 最小直径 | 2.0 m | `MIN_DIAMETER_CIRC` |
| 搜索范围 | 2.0 ~ 20.0 m | 步长 0.01 m |
| 几何高度 | D（直径） | |
| 总面积 | A = π × D² / 4 | |

**过水面积公式**（弓形面积）:
```
θ = 2 × arccos((R - h) / R)
A = R² × (θ - sinθ) / 2
```

**湿周公式**:
```
χ = R × θ
```

### 3.2 平底圆形断面

| 参数 | 值 | 说明 |
|------|-----|------|
| 最小直径 | 2.0 m | 复用 `MIN_DIAMETER_CIRC` |
| 用户输入 | `D + B` | `D` 为洞径，`B` 为平底宽 |
| 校验范围 | `D >= 2.0`，`0 < B ≤ D` | 允许 `B = D`，此时退化为半圆 |
| 手工模式 | 仅指定尺寸 | 不提供自动寻优，不提供“削底高度”第三参数 |
| 表3策略 | 仅导入复算 | 不进入表3手动结构选择器 |

**几何定义**:
- 以直径 `D` 的完整圆为母形；
- 用一条水平、居中的弦切去下部圆弓，仅保留上部断面；
- 底边就是这条弦，宽度为 `B`。

记 `R = D / 2`，`y_c = sqrt(R² - (B/2)²)`，则:
```
h_cut = R - y_c
H_total = R + y_c
θ_cut = 2 × arcsin(B / D)
A_total = πR² - R² × (θ_cut - sinθ_cut) / 2
```

**过水面积公式**（复用完整圆弓形面积差）:
```
A(h) = A_circle(h_cut + h) - A_circle(h_cut)
0 ≤ h ≤ H_total
```

**湿周公式**:
```
χ(h) = B + 2R × (arcsin((h - y_c)/R) + arcsin(y_c/R))
```

**水面宽公式**:
```
T(h) = 2 × sqrt(R² - (h - y_c)²)
0 < h < H_total
```

### 3.3 圆拱直墙型断面

> 范围边界：本节仅描述 `隧洞-圆拱直墙型`。`暗涵-圆拱直墙型` 可复用本节几何公式，但不得复用隧洞的最小高度、最小宽度、高宽比、围岩或 `xx管` 隧洞摘要规则。

| 参数 | 值 | 说明 |
|------|-----|------|
| 最小高度 | 2.0 m | `MIN_HEIGHT_HS` |
| 最小宽度 | 1.8 m | `MIN_WIDTH_HS` |
| 高宽比范围 | 1.0 ~ 1.5 | `HB_RATIO_MIN` / `HB_RATIO_MAX` |
| 拱顶圆心角 | 90° ~ 180° | 默认 180°（半圆拱） |
| 直墙高度 H直 | ≥ 0 m | 可选；留空自动推导，填写时必须同时填写 B |
| 按加大流量净空比例反推断面尺寸 | 当前工况辅助弹窗 | 输入 Q加大、目标净空比例、H/B、θ |
| 搜索范围(B) | 1.8 ~ 20.0 m | 粗搜索步长 0.1m，精细搜索步长 0.01m |

**几何结构**:
- 底部矩形：宽 B，高 H_straight
- 顶部圆拱：拱半径 R_arch = (B/2) / sin(θ/2)，拱高 H_arch = R_arch × (1 - cos(θ/2))
- 总高 H_total = H_straight + H_arch
- H_straight 来源：用户未填写时为 `H_total - H_arch`；用户填写时按输入值固定

**过水面积计算**（分段）:
- h ≤ H_straight: A = B × h
- h > H_straight: A = 直墙矩形面积 + 拱部过水面积

**自动搜索优化**（H直 留空）:
1. 粗搜索：B步长0.1m，HB比步长0.05 → 减少90%搜索次数
2. 最优解附近精细搜索：±0.3m范围内，B步长0.01m，HB比步长0.01
3. 以最小总面积 `A_total` 为优化目标

**固定 H直 核算**:
1. 读取用户输入的 `B / θ / H直`
2. 计算 `R_arch / H_arch / H_total`
3. 只进行设计流量、加大流量、流速和净空校核
4. 校核失败时返回失败原因，不自动改写 `B`、`H直` 或 `H_total`

**按加大流量净空比例反推断面尺寸弹窗**:
1. 使用当前工况的 `Q_design / n / slope_inv / v_min / v_max` 作为固定背景。
2. 用户输入 `Q_increased / target_freeboard_pct / hb_ratio / theta_deg`，其中目标净空比例默认 `15%`，`target_freeboard_pct / hb_ratio / theta_deg` 优先恢复上次成功计算保存的弹窗输入。
3. `target_freeboard_pct` 表示加大流量下的净空面积比；不得低于隧洞最小净空面积比 `15%`，且必须小于 `100%`。
4. `Q_increased` 默认取主流程解析后的加大流量；当主流程未启用加大流量时，弹窗按当前设计流量自动查表生成建议值。
5. 弹窗手改 `Q_increased` 只在主流程 `Q_design / base_Q_increased / Q_increased_source` 未变化时恢复；任一项变化则丢弃旧弹窗值。
6. `H/B` 严格限制为 `1.0~1.5`，`θ` 严格限制为 `90°~180°`；若当前设计流量有效，`Q加大` 必须大于设计流量。
7. 固定 `H/B` 与 `θ` 后，先在单位断面中求目标净空比例对应的单位水深，再按曼宁相似关系 `Q ∝ B^(8/3)` 反推 `B`，并给尺寸增加极小保守余量。
8. 内核会先生成前端真实采用的三位值：`θ` 按文本回填精度处理，`B / H直` 向上保留三位。
9. 三位值按固定 `B + H直 + θ` 口径重新复算设计流量与加大流量；若只差目标净空边界，优先每次增加 `H直=0.001m` 后复算，直到满足目标或达到保护上限。
10. 校核失败时只显示失败原因并禁用采用；校核通过后，采用操作只回填 `θ / B / H直`，不自动运行主计算。

### 3.4 马蹄形标准Ⅰ型

| 参数 | 值 | 常量名 |
|------|-----|--------|
| t | 3.0 | `HORSESHOE_T1` |
| θ | 0.294515 rad (16.874°) | `HORSESHOE_THETA1` |
| c | 0.201996 | `HORSESHOE_C1` |
| 底拱半径 | R = t × r = 3r | |
| 最小半径 | 1.0 m | `MIN_RADIUS_HORSESHOE_STD` |
| 搜索范围 | 1.0 ~ 10.0 m | 步长 0.01 m |

适用于地质条件 **较好** 的隧洞。

### 3.5 马蹄形标准Ⅱ型

| 参数 | 值 | 常量名 |
|------|-----|--------|
| t | 2.0 | `HORSESHOE_T2` |
| θ | 0.424031 rad (24.295°) | `HORSESHOE_THETA2` |
| c | 0.436624 | `HORSESHOE_C2` |
| 底拱半径 | R = t × r = 2r | |
| 最小半径 | 1.0 m | `MIN_RADIUS_HORSESHOE_STD` |
| 搜索范围 | 1.0 ~ 10.0 m | 步长 0.01 m |

适用于地质条件 **一般** 的隧洞。

### 3.6 马蹄形断面几何计算（Ⅰ/Ⅱ型通用）

断面分为 **三段**，底拱段高度 e = R_arch × (1 - cosθ):

**① 底拱段** (0 ≤ h ≤ e):
```
β = arccos(1 - h / R_arch)
A = (t×r)² × (β - 0.5×sin(2β))
B = 2×t×r×sin(β)
χ = 2×R_arch×β
```

**② 侧拱段** (e < h ≤ r):
```
α = arcsin((1 - h/r) / t)
A = R_arch² × (c - α - 0.5×sin(2α) + ((2t-2)/t)×sin(α))
B = 2×r×(t×cos(α) - t + 1)
χ = 2×t×r×(2θ - α)
```

**③ 顶拱段** (r < h ≤ 2r):
```
φ/2 = arccos(h/r - 1), φ = 2×(φ/2)
A = r² × (t²×c + 0.5×(π - φ + sin(φ)))
B = 2×r×sin(φ/2)
χ = 4×t×r×θ + r×(π - φ)
```

**分段连续性要求**: A 在 h=e 和 h=r 处必须连续（误差 < 0.01 m²）。

---

## 4. 水力计算核心

### 4.1 曼宁公式

```
Q = (1/n) × A × R^(2/3) × i^(1/2)
R = A / χ       （水力半径）
V = Q / A       （流速）
```

其中:
- Q: 流量 (m³/s)
- n: 糙率
- A: 过水面积 (m²)
- χ: 湿周 (m)
- R: 水力半径 (m)
- i: 水力坡降
- V: 流速 (m/s)

### 4.2 水深求解

采用 **二分法** 反算水深:
- 容差: `SOLVER_TOLERANCE = 0.0001`（相对误差）
- 最大迭代: `MAX_ITERATIONS = 100`
- 搜索范围: `[0.00001, H_total]`
- 先检查上下界是否满足，再进行二分迭代
- 最终校核容差放宽至 1.5 倍

### 4.3 断面尺寸搜索

搜索算法按以下约束逐步筛选:
1. 设计水深 h_design < 断面高度
2. 设计流速 v_min ≤ V_design ≤ v_max
3. 设计净空高度 ≥ 0.4 m
4. 设计净空面积比 ≥ 15%
5. 加大水深 h_increased < 断面高度
6. 加大流速 V_increased ≤ v_max
7. 加大净空高度 ≥ 0.4 m
8. 加大净空面积比 ≥ 15%

找到满足所有约束的 **最小尺寸** 即停止（圆形/马蹄形），或 **最小总面积** 方案（圆拱直墙型）。

### 4.4 净空约束

| 约束项 | 值 | 常量名 |
|--------|-----|--------|
| 最小净空面积比 | 15% | `MIN_FREEBOARD_PCT_TUNNEL` |
| 最小净空高度 | 0.4 m | `MIN_FREEBOARD_HGT_TUNNEL` |

净空计算:
- **净空面积比**: (A_total - A_water) / A_total × 100%
- **净空高度**: H_total - h（圆形为 D-h，马蹄形为 2r-h）

### 4.5 加大流量比例

根据设计流量 Q 查表自动确定（也可指定）:

| 设计流量 Q (m³/s) | 加大比例 |
|-------------------|---------|
| Q < 1 | 30% |
| 1 ≤ Q < 5 | 25% |
| 5 ≤ Q < 20 | 20% |
| 20 ≤ Q < 50 | 15% |
| 50 ≤ Q < 100 | 10% |
| Q ≥ 100 | 5% |

函数: `get_flow_increase_percent(design_Q)`

---

## 5. 输入参数

### 5.1 通用参数（所有断面类型）

| 参数 | 字段 | 默认值 | 必填 | 说明 |
|------|------|--------|------|------|
| 设计流量 Q | `Q_edit` | 10.0 | ✅ | m³/s，必须>0 |
| 糙率 n | `n_edit` | 0.014 | ✅ | 必须>0 |
| 水力坡降 1/ | `slope_edit` | 2000 | ✅ | 倒数输入，必须>0 |
| 不淤流速 | `vmin_edit` | 0.1 | ✅ | m/s |
| 不冲流速 | `vmax_edit` | 100.0 | ✅ | m/s，必须>不淤流速 |
| 流量加大比例 | `inc_edit` | 留空 | ❌ | %，留空自动查表 |

### 5.2 圆形断面专用参数

| 参数 | 字段 | 默认值 | 必填 | 说明 |
|------|------|--------|------|------|
| 指定直径 D | `D_edit` | 留空 | ❌ | m，留空自动搜索 |

### 5.3 平底圆形断面专用参数

| 参数 | 字段 | 默认值 | 必填 | 说明 |
|------|------|--------|------|------|
| 平底圆形直径 D | `flat_bottom_D_edit` | 留空 | ✅ | m，必须 `>= 2.0` |
| 平底宽 B | `flat_bottom_B_edit` | 留空 | ✅ | m，必须 `> 0` 且 `<= D` |

### 5.4 圆拱直墙型专用参数

| 参数 | 字段 | 默认值 | 必填 | 说明 |
|------|------|--------|------|------|
| 拱顶圆心角 | `theta_edit` | 180 | ❌ | 度，90~180 |
| 指定底宽 B | `B_hs_edit` | 留空 | ❌ | m，留空自动搜索 |
| 直墙高度 H直 | `H_straight_hs_edit` | 留空 | ❌ | m，留空自动计算；填写时必须同时填写 B，且 `>= 0` |

### 5.5 马蹄形专用参数

| 参数 | 字段 | 默认值 | 必填 | 说明 |
|------|------|--------|------|------|
| 指定半径 r | `r_edit` | 留空 | ❌ | m，留空自动搜索 |

---

## 6. 输出结果

### 6.1 计算结果字段（result dict）

**通用字段**:

| 字段 | 类型 | 说明 |
|------|------|------|
| `success` | bool | 计算是否成功 |
| `error_message` | str | 失败时的错误信息 |
| `section_type` | str | 断面类型名称 |
| `design_method` | str | 设计方案描述字符串 |
| `A_total` | float | 断面总面积 (m²) |
| `h_design` | float | 设计水深 (m) |
| `V_design` | float | 设计流速 (m/s) |
| `A_design` | float | 设计过水面积 (m²) |
| `P_design` | float | 设计湿周 (m) |
| `R_hyd_design` | float | 设计水力半径 (m) |
| `Q_calc` | float | 计算流量校核 (m³/s) |
| `freeboard_pct_design` | float | 设计净空面积比 (%) |
| `freeboard_hgt_design` | float | 设计净空高度 (m) |
| `increase_percent` | float | 加大比例 (%) |
| `Q_increased` | float | 加大流量 (m³/s) |
| `h_increased` | float | 加大水深 (m) |
| `V_increased` | float | 加大流速 (m/s) |
| `A_increased` | float | 加大过水面积 (m²) |
| `P_increased` | float | 加大湿周 (m) |
| `R_hyd_increased` | float | 加大水力半径 (m) |
| `freeboard_pct_inc` | float | 加大净空面积比 (%) |
| `freeboard_hgt_inc` | float | 加大净空高度 (m) |

**圆形断面专用**:

| 字段 | 类型 | 说明 |
|------|------|------|
| `D` | float | 直径 (m) |

**平底圆形断面专用**:

| 字段 | 类型 | 说明 |
|------|------|------|
| `D` | float | 洞径 (m) |
| `B` | float | 平底宽 (m) |
| `H_total` | float | 程序推导总高 (m) |
| `HB_ratio` | float | 高宽比 H/B，展示时可由 `H_total / B` 推导 |

**圆拱直墙型专用**:

| 字段 | 类型 | 说明 |
|------|------|------|
| `B` | float | 底宽 (m) |
| `H_total` | float | 总高 (m) |
| `H_straight` | float | 直墙高度 (m) |
| `manual_H_straight` | float / None | 用户输入的直墙高度，留空时为 None |
| `used_manual_H_straight` | bool | 是否按用户输入固定直墙高度 |
| `theta_deg` | float | 圆心角 (度) |
| `HB_ratio` | float | 高宽比 H/B |

**马蹄形专用**:

| 字段 | 类型 | 说明 |
|------|------|------|
| `r` | float | 马蹄形半径 (m) |
| `D_equiv` | float | 等效直径 2r (m) |

### 6.2 结果一致性约束

以下一致性必须满足（测试覆盖）:
- `R_hyd_design = A_design / P_design`
- `V_design × A_design ≈ Q`（相对误差 < 2%）
- `h_design < h_increased`（加大流量水深更大）
- `V_increased ≥ V_design`（加大流量流速更大）
- `Q_increased = Q × (1 + increase_percent / 100)`
- `freeboard_hgt_design = H_total - h_design`
- `design_method` 非空字符串

---

## 7. UI面板规格

### 7.1 布局

- **左侧**: 输入参数面板（可滚动，宽度 280~420px）
- **右侧**: 输出区域（QTabWidget，三个Tab）
  - Tab1「计算结果」: QWebEngineView 显示格式化计算过程
  - Tab2「断面图」: matplotlib Figure + NavigationToolbar；多工况默认 2 列、窄窗口 1 列、区域内部滚动，隧洞多工况使用更高行高保证等比例断面可读，双击小图可查看该工况大图
  - Tab3「工况对比」: 两个 QTableWidget 分别显示已计算工况的水力结果和结构尺寸

### 7.2 输入区域

- 断面类型下拉框（ComboBox）切换时动态显示/隐藏对应参数组:
  - 圆形 → `circ_grp`（指定直径 D）
  - 平底圆形 → `flat_bottom_grp`（直径 D + 平底宽 B）
  - 圆拱直墙型 → `hs_grp`（圆心角 θ + 指定底宽 B + 可选直墙高度 H直）
  - 马蹄形标准Ⅰ型/Ⅱ型 → `shoe_grp`（指定半径 r）
- 通用参数始终可见
- 「考虑加大流量比例系数」CheckBox（`inc_cb`，默认勾选）：勾选时显示流量加大比例输入框和动态提示标签，不勾选时隐藏并以 `manual_increase_percent=0` 传入计算内核
- 「输出详细计算过程」复选框（默认勾选）

### 7.3 操作按钮

| 按钮 | 功能 |
|------|------|
| **计算** | PrimaryPushButton，执行水力计算 |
| **清空** | PushButton，清除计算结果并重置为初始帮助页，保留用户输入与多工况 |
| **导出DXF** | PushButton；单工况直接选择比例尺并导出，多工况弹出共享 DXF 范围对话框并追加参数对比表 |
| **导出Word** | PushButton，生成Word计算书 |

### 7.4 初始帮助页

通过 `HelpPageBuilder` 构建，包含:
- 支持断面类型列表（5种，含各自特点说明）
- 曼宁公式
- 净空约束条件
- 加大流量比例规范表

### 7.5 计算结果显示

**简要模式**（detail_cb 未勾选）:
- 输入参数 → 断面尺寸 → 设计流量工况 → 加大流量工况（仅勾选时） → 验证结果

**加大流量条件化显示**：当 `use_increase=False`（不勾选“考虑加大流量比例系数”）时，简要/详细模式均跳过加大流量工况段落，验证结果仅使用设计流量工况数据（净空验证仅检查设计工况）。

**详细模式**（detail_cb 勾选，默认）:
- 输入参数（一）
- 断面尺寸（二）: 含面积公式展开
- 设计流量工况计算（三）: 20个步骤
  1. 设计水深
  2-3. 圆心角/过水面积/湿周（按断面类型分支展开公式）
  4. 水力半径 R = A/χ
  5. 设计流速（曼宁公式展开）
  6. 流量校核
  7. 净空面积计算
  8. 净空高度计算
- 加大流量工况计算（四）（仅勾选时）: 步骤 9-17
  9. 加大流量计算
  10. 加大水深
  11-12. 加大过水面积/湿周
  13. 加大水力半径
  14. 加大流速
  15. 加大流量校核
  16. 加大净空面积
  17. 加大净空高度
- 设计验证（五）: 步骤 18-20
  18. 流速验证（v_min ≤ V ≤ v_max）
  19. 净空面积验证（≥ 15%）（勾选时用加大工况，不勾选时用设计工况）
  20. 净空高度验证（≥ 0.4m）（同上）

输出通过 `plain_text_to_formula_html()` 转换后加载到 `QWebEngineView`。

### 7.6 断面图预览

matplotlib 绘制:
- **单工况**: 默认显示设计流量工况；启用加大流量且结果有效时，在同图叠加加大水位
- **多工况**: 每个成功工况一个小图，按默认 2 列、窄窗口 1 列和断面图区域内部滚动承载；隧洞每行使用 520px 画布高度，保证高瘦断面在等比例坐标下不被压窄；图内叠加设计水位和加大水位，双击小图可打开该工况大图查看窗口
- 叠加加大水位时，同步显示 `h加大=x.xxm` 竖向双向尺寸箭头

每幅图包含:
- 断面轮廓（黑色实线）
- 水面线（蓝色填充）
- 尺寸标注（箭头+文字）
- 水深标注（蓝色双向箭头 + 文字 h=x.xxm，平底圆形、圆拱直墙型与圆形/马蹄形保持一致）
- 标题（断面类型 + Q + V）
- 网格线 + 等比例

各断面类型有独立绘制方法:
- `_draw_circular()`: 圆形轮廓 + 弓形水面
- `_draw_flat_bottom_circle()`: 平底圆形轮廓 + 水平底边 + 圆弧顶 + 分段水面填充 + 水深标注
- `_draw_horseshoe()`: 底板 + 直墙 + 拱部 + 分区水面填充 + 水深标注
- `_draw_horseshoe_std()`: 精确轮廓曲线（100点采样）+ 水面填充

### 7.7 工况对比表

- 表格数据来自所有已成功计算的工况；参数变化导致结果过期时，表格提示重新计算。
- 支持圆形、平底圆形、圆拱直墙型、马蹄形标准Ⅰ型和马蹄形标准Ⅱ型。
- 表格拆成“水力结果对比表”和“结构尺寸对比表”；水深和流速分别列出设计工况与加大工况，未启用或无结果的加大列留空。
- `洞身周长` 与 `洞身断面积` 是完整洞身几何指标，不随当前水深变化。
- 表格支持 `Ctrl+A` 全选后 `Ctrl+C` 复制到 Excel；复制内容包含两张表标题和表头，列用制表符分隔，空值保持为空单元格。

---

## 8. DXF 导出规格

### 8.1 文件格式

- DXF版本: R2010
- 单位: mm (`$INSUNITS=4`, `$MEASUREMENT=1`)
- 线型比例: `$LTSCALE = sf × 0.5`
- 中文文字样式宽度因子: `0.7`

### 8.2 图层定义

| 图层名 | 颜色 | 线宽 | 用途 |
|--------|------|------|------|
| `轮廓线` | 7 (白) | 50 | 断面轮廓 |
| `设计水位` | 5 (蓝) | 25 | 设计水位线 |
| `加大水位` | 4 (青) | 25 | 加大水位线（虚线 DASHED） |
| `尺寸标注` | 2 (黄) | 18 | 尺寸标注 |
| `参数文字` | 3 (绿) | 18 | 参数文字块 |

### 8.3 缩放机制

- 缩放因子: `sf = 1000.0 / scale_denom`
- 支持比例尺: 1:20, 1:50, 1:100（默认）, 1:200, 1:500
- 单工况导出前弹出比例尺选择
- 多工况导出前弹出共享 DXF 对话框，固定包含导出范围（当前工况 / 勾选多个工况 / 全部工况）、比例尺和工况勾选列表

### 8.4 各断面绘制内容

### 当前实现同步说明（2026-03-22）

- 圆拱直墙型 DXF 已改为与界面断面图共用同一套几何公式，不再单独手写另一套圆心坐标。
- 平底圆形 DXF 已改为与界面断面图、表3复算共用同一套共享几何 helper；轮廓固定画成 `1 条水平底边 LWPOLYLINE + 1 条原生 ARC`。
- 圆拱直墙型轮廓已改为：`1 条 LWPOLYLINE`（底板+直墙） + `1 条 ARC`（拱顶），导出后拱脚与直墙顶严格重合，不再出现断面不闭合。
- 马蹄形标准Ⅰ型 / Ⅱ型轮廓已改为 `4 条 ARC` 精确拼接，不再使用采样折线逼近。
- 圆拱直墙型水位线宽度已按真实拱部几何计算；进入拱部后不会再按固定底宽 B 直接画满。
- 多工况导出时，“当前工况”固定以左侧输入区当前激活标签 `_current_case_idx` 为准，不再从 `current_result` / `input_params` 推断，也不再落到最后一个工况。
- 多工况 DXF 对话框固定支持 `当前工况`、`勾选多个工况`、`全部工况` 三种范围；切到“勾选多个工况”时默认仅勾选当前工况。
- 当最终有效工况数大于 1 时，导出结果为 1 个合并 DXF，采用自动网格排版：`ncols = ceil(sqrt(n))`、`nrows = ceil(n / ncols)`，按从左到右、从上到下摆放。
- 合并 DXF 中每个工况块都使用独立图层前缀，例如 `工况1_轮廓线`、`工况1_设计水位`，便于在 CAD 中按工况开关显示。
- 无效工况定义为：无计算结果、计算失败、或当前面板 `_results_dirty=True`；批量导出时会自动跳过，并在完成后汇总提示“已导出 M 个工况，跳过 X 个无效工况”。
- 隧洞单工况和多工况 DXF 在断面图网格下方追加“水力结果对比表”和“结构尺寸对比表”，表头带单位，内容与界面「工况对比」页签同源。
- 对比表 DXF 表头单位使用 CAD MTEXT 上标控制码显示 `m³/s`、`m²`；表头按“名称 / 单位”两行显示，列宽按表头和内容自动适配。
- `export_single_case_dxf()` 与 `export_combined_case_dxf()` 通过可选 `draw_summary_table` 回调追加附表；不传该回调时，其他模块的 DXF 行为保持不变。

**圆形** (`_draw_circ`):
- 圆形轮廓（`add_circle`）
- 中心线（虚线）
- 设计/加大水位线 + 水位文字标注
- 直径标注、水深标注、净空标注、半径标注
- 参数文字块（输入参数 + 断面 + 设计/加大工况）

**平底圆形** (`_draw_flat_bottom_circ`):
- 水平底边（`LWPOLYLINE`）
- 上部原生圆弧（`ARC`）
- 设计/加大水位线 + 水位文字标注
- `D / B / H_total / h / 净空` 标注
- 参数文字块（统一显示 `D + B + H_total`）

**圆拱直墙型** (`_draw_arch`):
- 底板 + 左右直墙开放多段线（`LWPOLYLINE`）
- 拱部原生圆弧（`ARC`）
- 水位线 + 标注
- B/H直/h/H/θ 标注
- 参数文字块

**马蹄形** (`_draw_shoe`):
- 底拱 1 段圆弧 + 左右侧拱 2 段圆弧 + 顶拱 1 段圆弧
- 水位线 + 标注
- 2r/h/H/r 标注
- 参数文字块（含标准Ⅰ型/Ⅱ型名称）

### 8.5 公共绘图工具

复用自 `app_渠系计算前端.dxf_common` 与 `app_渠系计算前端.dxf_multi_export`:
- `ensure_section_dxf_layers()` / `setup_section_dxf_document()`: 创建共享图层、字体与标注样式
- `TrackedSectionMsp`: 为单工况绘图提供偏移包装与包络尺寸统计
- `_add_dim_h()` / `_add_dim_v()` / `_add_text_block()`: 通用尺寸标注与参数文字块
- `compute_auto_grid()` / `export_combined_case_dxf()`: 多工况合并 DXF 自动网格布局与保存
- `draw_tunnel_comparison_table()`: 隧洞工况对比双表绘制，复用中文字体与参数文字图层，并按内容自动适配列宽

---

## 9. Word 导出规格

### 9.1 依赖

- `python-docx`
- `latex2mathml`
- `lxml`

如未安装，按钮点击时 InfoBar 提示安装命令。

### 9.2 报告结构

使用 `create_styled_doc()` 创建高端咨询报告风格文档:
- **封面**: 标题「隧洞水力计算书」+ 副标题（断面类型 + 设计方案）
- **一、基础公式**: 曼宁公式 + 水力半径公式（LaTeX渲染）
- **二、计算过程**: 先插入工况对比两张表，再从 `_export_plain_text` 渲染各工况详细过程，跳过标题行
- **三、断面图**: 从 matplotlib Figure 导出 PNG 后插入（宽度14cm, 150dpi）

---

## 10. 计算内核 API

### 10.1 公开函数

文件: `calc_渠系计算算法内核/隧洞设计.py`

| 函数 | 说明 |
|------|------|
| `get_flow_increase_percent(design_Q)` | 查表返回加大比例 (%) |
| `get_required_freeboard_height(H_total)` | 返回最小净空高度 0.4m |
| `calculate_circular_area(D, h)` | 圆形过水面积 |
| `calculate_circular_perimeter(D, h)` | 圆形湿周 |
| `calculate_circular_outputs(D, h, n, slope)` | 圆形全部水力要素 |
| `solve_water_depth_circular(D, n, slope, Q_target)` | 圆形水深反算 |
| `quick_calculate_circular(Q, n, slope_inv, ...)` | **圆形一键计算** |
| `calculate_flat_bottom_circular_area(D, B, h)` | 平底圆形过水面积 |
| `calculate_flat_bottom_circular_perimeter(D, B, h)` | 平底圆形湿周 |
| `calculate_flat_bottom_circular_surface_width(D, B, h)` | 平底圆形水面宽 |
| `calculate_flat_bottom_circular_outputs(D, B, h, n, slope)` | 平底圆形全部水力要素 |
| `solve_water_depth_flat_bottom_circular(D, B, n, slope, Q_target)` | 平底圆形水深反算 |
| `quick_calculate_flat_bottom_circular(Q, n, slope_inv, ...)` | **平底圆形一键计算** |
| `calculate_horseshoe_arch_geometry(B, theta_rad, H_straight=None, H_total=None)` | 圆拱直墙拱半径、拱高、总高与直墙高换算 |
| `calculate_horseshoe_area(B, H_total, theta_rad, h)` | 圆拱直墙过水面积 |
| `calculate_horseshoe_perimeter(B, H_total, theta_rad, h)` | 圆拱直墙湿周 |
| `calculate_horseshoe_total_area(B, H_total, theta_rad)` | 圆拱直墙总面积 |
| `calculate_horseshoe_outputs(B, H_total, theta_rad, h, n, slope)` | 圆拱直墙全部水力要素 |
| `solve_water_depth_horseshoe(B, H_total, theta_rad, n, slope, Q_target)` | 圆拱直墙水深反算 |
| `quick_calculate_horseshoe(Q, n, slope_inv, ...)` | **圆拱直墙一键计算** |
| `calculate_horseshoe_std_elements(section_type, r, h)` | 马蹄形水力要素 (A, B, P) |
| `calculate_horseshoe_std_outputs(section_type, r, h, n, slope)` | 马蹄形全部水力要素 |
| `solve_water_depth_horseshoe_std(section_type, r, n, slope, Q_target)` | 马蹄形水深反算 |
| `quick_calculate_horseshoe_std(Q, n, slope_inv, ...)` | **马蹄形一键计算** |

### 10.2 快速计算函数签名

```python
quick_calculate_circular(
    Q: float, n: float, slope_inv: float,
    v_min: float, v_max: float,
    manual_D: float = None,
    manual_increase_percent: float = None
) -> Dict[str, Any]

quick_calculate_flat_bottom_circular(
    Q: float, n: float, slope_inv: float,
    v_min: float, v_max: float,
    manual_D: float,
    manual_B: float,
    manual_increase_percent: float = None
) -> Dict[str, Any]

quick_calculate_horseshoe(
    Q: float, n: float, slope_inv: float,
    v_min: float, v_max: float,
    theta_deg: float = 180.0,
    manual_B: float = None,
    manual_H_straight: float = None,
    manual_increase_percent: float = None
) -> Dict[str, Any]

quick_calculate_horseshoe_std(
    Q: float, n: float, slope_inv: float,
    v_min: float, v_max: float,
    section_type: int,       # 1=标准Ⅰ型, 2=标准Ⅱ型
    manual_r: float = None,
    manual_increase_percent: float = None
) -> Dict[str, Any]
```

### 10.3 常量

```python
PI = 3.14159265358979
MIN_DIAMETER_CIRC = 2.0           # 圆形最小直径
MIN_HEIGHT_HS = 2.0               # 圆拱直墙最小高度
MIN_WIDTH_HS = 1.8                # 圆拱直墙最小宽度
HB_RATIO_MIN = 1.0               # 高宽比下限
HB_RATIO_MAX = 1.5               # 高宽比上限
MIN_RADIUS_HORSESHOE_STD = 1.0   # 马蹄形最小半径
MIN_FREEBOARD_PCT_TUNNEL = 0.15  # 最小净空面积比 15%
MIN_FREEBOARD_HGT_TUNNEL = 0.4   # 最小净空高度 0.4m
SOLVER_TOLERANCE = 0.0001        # 二分法相对容差
MAX_ITERATIONS = 100             # 最大迭代次数
DIM_INCREMENT = 0.01             # 尺寸搜索步长
```

---

## 11. 错误处理

### 11.1 输入校验

| 条件 | 处理 |
|------|------|
| Q ≤ 0 | 弹出错误提示 + 结果区显示错误 |
| n ≤ 0 | 弹出错误提示 |
| slope_inv ≤ 0 | 弹出错误提示（不触发 ZeroDivisionError） |
| v_min ≥ v_max | 弹出错误提示 |
| 圆拱直墙 θ ∉ [90°, 180°] | 返回 success=False + error_message |
| 圆拱直墙 H直 < 0 | 返回 success=False + error_message |
| 圆拱直墙填写 H直 但未填写 B | 返回 success=False + error_message |
| 马蹄形 section_type ∉ {1, 2} | 返回 success=False + error_message |
| 输入格式错误（非数字） | ValueError 捕获，提示检查必填参数 |

### 11.2 计算失败

当搜索范围内找不到满足约束的尺寸时:
- 指定尺寸: 提示"指定的尺寸无法满足要求" + 建议
- 自动搜索失败: 提示搜索范围 + 建议检查流量/坡降

### 11.3 导出异常

| 异常 | 处理 |
|------|------|
| `ImportError` (ezdxf/docx) | InfoBar 提示安装命令 |
| `PermissionError` | InfoBar 提示关闭同名文件 |
| 其他 Exception | InfoBar 显示错误信息 |

---

## 12. 测试覆盖

### 12.1 测试文件

- `tests/test_tunnel_kernel.py` — 原有圆形 / 圆拱直墙型 / 马蹄形回归
- `tests/test_tunnel_flat_bottom_circular_kernel_unit.py` — 平底圆形内核与边界条件
- `tests/test_tunnel_flat_bottom_circular_shared_hydraulic_unit.py` — 共享结果、表3导入复算与展示兜底
- `tests/test_tunnel_flat_bottom_circular_dxf_unit.py` — 平底圆形 DXF 轮廓连通性、总高一致性与极小水深边界
- `tests/test_tunnel_flat_bottom_circular_panel_batch_unit.py` — 单断面页、批量页、参数回填与 Word/TXT 标题链路
- `tests/test_tunnel_flat_bottom_circular_table3_xxpipe_unit.py` — 表3手工限制与 `xx管` 隧洞摘要链路
- `tests/test_tunnel_flat_bottom_circle_section_summary_unit.py` — 断面汇总独立分组、标题与配置入口
- `tests/test_tunnel_arch_manual_wall_height_unit.py` — 圆拱直墙型固定 H直 的内核几何、失败边界和自动模式回归
- `tests/test_tunnel_arch_wall_height_panel_batch_unit.py` — 单项面板、批量列、参数弹窗和 H直 透传回填
- `tests/test_tunnel_clearance_sizing_unit.py` — 按加大流量净空比例反推断面尺寸内核、输入边界和校核失败
- `tests/test_tunnel_clearance_sizing_panel_unit.py` — 隧洞面板入口显示、上下文读取和采用回填
- `tests/test_tunnel_clearance_sizing_dialog_unit.py` — 弹窗计算成功/失败状态与采用按钮启用规则
- `tests/test_tunnel_comparison_unit.py` — 隧洞工况对比双表、完整洞身几何指标和 DXF 回调链路
- `tests/test_multi_case_dxf_export_unit.py` — 多工况 DXF 可选附表回调不影响默认导出行为
- `tests/test_tunnel_dxf_export_unit.py` — 隧洞 DXF 轮廓、对比表表头单位、列宽自动适配和默认文字宽度因子
- `tests/test_section_plot_layout_unit.py` — 四类设计面板界面断面图共享布局、可配置行高、滚动、切页刷新和双击大图回归
- `tests/test_tunnel_panel_plot_unit.py` — 隧洞界面断面图标题、2 列多工况、高行高可读性、加大水位叠加和圆拱直墙型绘图回归
- `tests/test_section_plotting_unit.py` — 四类设计面板界面断面图共享 matplotlib 绘图底座回归

### 12.2 测试组清单

| # | 测试组 | 测试内容 | 用例数量级 |
|---|--------|----------|-----------|
| 1 | `test_circular_geometry` | 圆形面积/湿周 vs 独立验算 | ~30 |
| 2 | `test_circular_outputs` | 圆形水力要素 (R_hyd, V, Q, 净空) | ~64 |
| 3 | `test_circular_solver` | 圆形水深反算精度 (<1%) | ~24 |
| 4 | `test_circular_design` | 圆形完整设计（18组参数） | ~200+ |
| 5 | `test_horseshoe_geometry` | 圆拱直墙面积/湿周/总面积/单调性 | ~100+ |
| 6 | `test_horseshoe_solver` | 圆拱直墙水深反算 | ~16 |
| 7 | `test_horseshoe_design` | 圆拱直墙完整设计（14组参数） | ~200+ |
| 8 | `test_horseshoe_std_geometry` | 马蹄形Ⅰ/Ⅱ型几何 + 分段连续性 + 单调性 | ~200+ |
| 9 | `test_horseshoe_std_solver` | 马蹄形水深反算 | ~20 |
| 10 | `test_horseshoe_std_design` | 马蹄形完整设计（15组参数） | ~200+ |
| 11 | `test_flow_increase` | 加大流量查表 (19个Q值 + 边界) | ~21 |
| 12 | `test_boundary_conditions` | 异常输入 (Q=0, n=0, slope=0, θ越界, type=3, h>2r等) | ~15 |
| 13 | `test_outputs_field_validation` | 圆拱/马蹄形 outputs 字段独立验证 | ~60+ |
| 14 | `test_manual_params` | manual_increase_percent + manual_B/manual_H_straight/manual_D/manual_r | ~40+ |
| 15 | `test_consistency_and_misc` | h_d<h_inc 一致性, design_method非空, R_hyd=A/P, V_inc≥V_d | ~20 |
| 16 | `test_clearance_sizing` | 圆拱直墙按加大流量净空比例反推断面尺寸、弹窗入口和采用回填 | ~14 |
| 17 | `test_tunnel_comparison` | 五类隧洞对比双表、完整洞身周长/面积、DXF 对比表标题和列名 | ~8 |
| 18 | `test_section_plot_layout` | 多工况断面图 2 列/1 列布局、显式画布尺寸、可配置行高、切页刷新、纵向滚动刷新和双击大图 | ~22 |
| 19 | `test_tunnel_panel_plot` | 隧洞面板断面图多工况布局、高行高可读性、加大水位叠加、单图叠加和圆拱直墙型图元 | ~26 |

### 12.3 测试方法

- **独立验算函数**: 测试文件内有 `_circ_area`, `_circ_perim`, `_manning`, `_hs_area`, `_hs_perim`, `_hs_total_area`, `_shoe_elems` 等独立实现，与被测模块交叉验证
- **精度要求**: 面积/湿周绝对误差 < 0.001, 流量相对误差 < 2%, 水深反算相对误差 < 1%
- **运行方式**: `python tests/test_tunnel_kernel.py`；反推助手补充运行 `pytest tests/test_tunnel_clearance_sizing_unit.py tests/test_tunnel_clearance_sizing_panel_unit.py tests/test_tunnel_clearance_sizing_dialog_unit.py`；工况对比补充运行 `pytest tests/test_tunnel_comparison_unit.py tests/test_tunnel_dxf_export_unit.py tests/test_multi_case_dxf_export_unit.py`；界面断面图补充运行 `pytest tests/test_section_plot_layout_unit.py tests/test_tunnel_panel_plot_unit.py tests/test_section_plotting_unit.py`

---

## 13. 技术栈与依赖

| 组件 | 用途 |
|------|------|
| **PySide6** | UI框架 |
| **qfluentwidgets** | Fluent风格控件 (ComboBox, PushButton, LineEdit, CheckBox, InfoBar) |
| **QWebEngineView** | 计算结果HTML渲染 |
| **matplotlib** | 断面图绘制 + NavigationToolbar |
| **numpy** | 绘图数据生成 |
| **ezdxf** | DXF文件导出（可选依赖） |
| **python-docx** | Word文档导出（可选依赖） |
| **latex2mathml + lxml** | Word公式渲染（可选依赖） |

---

## 14. 集成方式

### 14.1 主应用注册

文件: `app_渠系计算前端/app.py`

```python
from app_渠系计算前端.tunnel.panel import TunnelPanel
self.tunnel_panel = TunnelPanel()
self.stack.addWidget(self.tunnel_panel)
```

导航索引: **第3个**（明渠→渡槽→隧洞→矩形暗涵→...）

### 14.2 内核导入路径

`panel.py` 通过 `sys.path` 注入 `calc_渠系计算算法内核/` 目录后导入:
```python
from 隧洞设计 import (
    quick_calculate_circular,
    quick_calculate_horseshoe,
    quick_calculate_horseshoe_std,
    PI, MIN_FREEBOARD_PCT_TUNNEL, MIN_FREEBOARD_HGT_TUNNEL,
)
```

### 14.3 样式复用

从 `app_渠系计算前端.styles` 导入共享样式常量:
`P, S, W, E, BG, CARD, BD, T1, T2, INPUT_LABEL_STYLE, INPUT_SECTION_STYLE, INPUT_HINT_STYLE`

从 `app_渠系计算前端.export_utils` 导入共享导出工具。

从 `app_渠系计算前端.formula_renderer` 导入公式渲染工具:
`plain_text_to_formula_html, load_formula_page, make_plain_html, HelpPageBuilder`

---

## 15. 变更日志

| 日期 | 版本 | 变更内容 |
|------|------|----------|
| 2026-02-22 | v1.0 | 初始版本，完整记录已实现功能 |
| 2026-02-25 | v1.1 | 新增“考虑加大流量比例系数”CheckBox控件（`inc_cb`，默认勾选）：不勾选时`manual_increase_percent=0`传入计算内核，简要/详细模式均跳过加大流量工况段落，验证结果仅使用设计流量数据。涉及文件：tunnel/panel.py |
| 2026-04-13 | v1.3 | 完成 `平底圆形` 隧洞断面全链路收口：表1参数弹窗回填与提示文案、单断面多工况与 Word/TXT 标题、DXF 总高一致性、表3手工输入拦截、`xx管` 隧洞摘要、断面汇总独立分组和对应测试全部补齐；表3继续只允许来源带入、不开放手动新选。 |
| 2026-04-14 | v1.4 | 文档口径同步：明确 `暗涵-圆拱直墙型` 不属于隧洞模块，只能复用圆拱直墙几何，不能复用隧洞约束、`xx管` 隧洞摘要或隧洞导出标题。 |
| 2026-04-29 | v1.5 | `隧洞-圆拱直墙型` 支持可选 `直墙高度 H直`：留空自动推导，填写时按用户输入固定几何；单项、批量、Excel 模板、共享结果、表3和 DXF/报告同步保留来源说明。 |
| 2026-04-30 | v1.6 | `隧洞-圆拱直墙型` 新增按加大流量目标净空比例反推尺寸弹窗；弹窗只回填当前工况 `θ / B / H直`，不改变主计算、批量、Excel 或导出链路。 |
| 2026-04-30 | v1.7 | 净空反推弹窗默认目标比例改为 `15%`，跨重启记忆上次成功计算输入；反推结果必须真实达到目标比例，避免回填后三位尺寸在主流程低于 `15%`。 |
| 2026-04-30 | v1.9 | 反推弹窗 `Q加大` 默认跟随当前主流程解析值；只有设计流量和加大来源未变化时才恢复弹窗手改值，避免主流程改流量后沿用旧 Q加大。 |
| 2026-04-30 | v1.10 | 隧洞设计面板新增「工况对比」页签，并在多工况 DXF 断面图网格后追加同口径参数对比表；周长和断面积统一按完整洞身几何计算。 |
| 2026-04-30 | v1.11 | 修复隧洞多工况 DXF 对比表文字溢出和上标单位显示：DXF 文字宽度因子统一为 `0.7`，对比表列宽自动适配，表头单位使用 CAD MTEXT 上标控制码显示 `m³/s`、`m²`。 |
| 2026-04-30 | v1.12 | 隧洞设计面板「工况对比」表支持全选复制到 Excel，复制文本包含表头并保留空单元格。 |
| 2026-04-30 | v1.13 | 断面图预览补齐平底圆形和圆拱直墙型的蓝色水深 `h` 标注，与圆形和马蹄形显示口径一致。 |
| 2026-04-30 | v1.14 | 净空反推成功结果改为按三位回填后的 `θ / B / H直` 固定断面复算，边界不足时优先微增 `H直`，确保点击采用后主流程仍能通过。 |
| 2026-04-30 | v1.15 | 隧洞“工况对比”从单表升级为水力结果表 + 结构尺寸表，并接入共享对比框架；单工况和多工况 DXF、Word 报告均输出两张同口径对比表，完整洞身周长和断面积口径保持不变。 |
| 2026-05-01 | v1.16 | 工况对比水力表同步重点结果，补齐设计/加大净空高度和净空比例；结构表补齐等效直径、拱顶圆心角、拱半径和拱高。 |
| 2026-05-01 | v1.17 | 工况对比水力表删除“控制余量类型 / 控制余量”摘要列，界面、DXF 和 Word 统一以明确重点结果列展示安全余量。 |
| 2026-05-07 | v1.18 | 隧洞重点结果汇总和结构尺寸对比表补齐 `高宽比 H/B`：平底圆形和圆拱直墙型按 `H_total / B` 显示，界面、Word 汇总、Word 对比表和 DXF 对比表同源。 |
| 2026-05-08 | v1.19 | 单工况界面断面图统一为单图叠加：普通单项入口和工况汇总入口都只显示一张设计流量图，加大水位在同图叠加虚线、文字和 `h加大` 标注。 |
| 2026-05-08 | v1.20 | 手动多工况上限提升到 30 个；结果区“工况快捷导航”改为默认 2 行、展开后内部滚动，标签压缩为“工况号 + Q”，避免挤压详细计算内容。 |
| 2026-05-08 | v1.21 | 多工况界面断面图新增自动列数、断面图页内部滚动和双击小图查看大图；完整尺寸标注和导出规则保持不变。 |
| 2026-05-08 | v1.22 | 修复断面图页隐藏状态下首次绘图读到临时小宽度，导致宽屏多工况误退为单列的问题；进入断面图页时按真实宽度重排。 |
| 2026-05-08 | v1.23 | 多工况界面断面图列数规则调整为宽屏默认 2 列、明显窄窗口 1 列，取消小数量工况使用三列的旧策略。 |
| 2026-05-09 | v1.24 | 修复从“工况对比”切回“断面图”后画布宽度抖动导致排版变化的问题；已有稳定布局时保持原画布尺寸，首次绘图或列数变化时优先使用断面图可视宽度，并复位横向滚动位置；同时滚动区显式控制画布尺寸并在纵向滚动时刷新，避免隧洞复杂小图拖拽残影。 |
| 2026-05-09 | v1.25 | 打开无隧洞计算结果的 `.qxproj` 项目时，清空旧断面图画布、双击映射和滚动布局状态，避免上一项目断面子图残留。 |
| 2026-05-09 | v1.26 | 隧洞多工况断面图单独使用 520px 行高，解决圆拱直墙型等高瘦断面在等比例坐标下被压窄、右侧留出大片空白的问题；明渠、渡槽、暗涵仍沿用共享默认行高。 |
| 2026-05-09 | v1.27 | 左侧工况标签改为紧凑显示，完整信息移入悬浮提示；隧洞多工况断面图保留 520px 专用行高并支持坐标轴顶对齐，减少圆拱直墙型首屏上方空白。 |
| 2026-05-09 | v1.28 | 修复在“计算结果”页完成 9 个隧洞工况计算后，首次打开“断面图”仍沿用隐藏页签临时布局的问题；隐藏页签绘图后标记一次可见刷新，切页时延迟强制按真实 viewport 宽度重排，避免必须拖拽左侧栏才恢复正常。 |
| 2026-05-09 | v1.29 | 二次修复断面图首次可见刷新过早的问题：如果第一次延迟刷新仍停在旧画布宽度，会保留待刷新标记并继续有限重试；6 个圆形工况和 9 个圆拱直墙工况都应在首次打开断面图时得到与轻拖分栏后一致的排版。 |
| 2026-05-09 | v1.30 | 优化断面图首次显示的底层宽度策略：隐藏页签绘图时优先用输出区父容器宽度预渲染，切到断面图页时若画布已稳定则只轻量刷新，减少切页后等待完整重画的短暂延迟。 |
| 2026-05-09 | v1.31 | 保留隧洞多工况断面图专用顶对齐和 520px 行高，同时将标题安全边距行距调紧，避免 9 个圆拱直墙型工况的小图宽度低于可读性门槛。 |
