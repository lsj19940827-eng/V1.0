# 架构说明

## 主要文件/模块职责

- `main.py`：桌面程序入口，负责启动主界面。
- `app_渠系计算前端/`：界面层，负责表格编辑、按钮动作、结果展示和导出。
- `app_渠系计算前端/webengine_diagnostics.py`：启动预检与诊断层，负责收集运行环境摘要、探测 Qt WebEngine 是否可用，并在失败时输出固定格式的诊断信息。
- `app_渠系计算前端/pressure_pipe/panel.py`：有压管道设计页，负责单次计算、批量扫描、结果预览和导出；其右侧初始帮助页改为首次真正显示页面时再渲染，避免拖慢主窗口启动。
- `app_渠系计算前端/open_channel/panel.py`：明渠设计页主控，负责梯形、复式梯形、矩形、圆形、U形的参数输入、多工况保存、结果展示、断面图以及 Word/TXT/DXF 导出。
- `app_渠系计算前端/open_channel/dxf_export.py`：明渠断面 DXF 导出，负责把不同断面的轮廓、水位线、尺寸标注和参数文字块画成 CAD 图。
- `app_渠系计算前端/tunnel/panel.py`：隧洞设计页主控，负责圆形、平底圆形、圆拱直墙型、马蹄形的参数输入、多工况保存、结果说明、断面图以及 Word/TXT/DXF 导出；平底圆形标题分支不再依赖结果回退。
- `app_渠系计算前端/tunnel/geometry.py`：隧洞共享几何真源，负责圆拱直墙型与平底圆形的几何 helper，供预览绘图、DXF 和表3复算共用。
- `app_渠系计算前端/tunnel/dxf_export.py`：隧洞断面 DXF 导出，负责把不同隧洞断面的轮廓、水位线、尺寸标注和参数文字块画成 CAD 图；平底圆形使用“水平底边 + 原生圆弧”导出，并统一以 `D / B` 推导几何作为总高真值。
- `app_渠系计算前端/batch/panel.py`：表1批量计算页面主控，负责 Excel 导入、断面批量计算、参数弹窗、结果导出、导入后重名提示，以及把“支渠连续承压链中的同名普通有压段”和“被闸类节点打断的同名隧洞”从真正风险重名里单独放开；当前已补齐 `明渠-复式梯形` 的主表列、弹窗和 Excel 链路。
- `app_渠系计算前端/batch/panel.py`：表1批量计算页面主控，负责 Excel 导入、断面批量计算、参数弹窗、结果导出、导入后重名提示，以及把“支渠连续承压链中的同名普通有压段”和“被闸类节点打断的同名隧洞”从真正风险重名里单独放开；当前已补齐 `明渠-复式梯形` 与 `隧洞-平底圆形` 的主表列、弹窗、参数回填、提示文案和 Excel 链路。
- `app_渠系计算前端/water_profile/panel.py`：表3水面线页面主控，负责把节点数据和界面单元格互相同步，管理顶部“转弯半径”统一应用入口、普通模式/xx管 的导出精度默认值，以及辅助行往返所需的结构类型/坐标上下文、IP 单元格 raw/display 元数据、承压行稳定身份落盘、跨流量段边界水位统一、单点子段纵断面的整线回退、连续承压链的“前缀段/锚点/可计算子段/未完成汇总”口径、`xx渠` 末尾命名承压尾段与所有 `xx管` 连续承压链命名承压成员的逐行正式计损、真实写回成员检查、split 父组 summary-only 判定、row override 正式损失同步、锁定逐行承压行的“弹窗手动采用值”保存/恢复、route 级纵断面别名 identity 映射、窗口 override 的 `route_key / route_display_name / targets` 同步，以及 `route / segment` 正式持久化载荷的生成与保存；当前也负责把 `明渠-复式梯形` 与 `隧洞-平底圆形` 的隐藏参数透传给下游。表3会继续识别 `隧洞-平底圆形` 的来源数据，但对该类型的双击选择、手输、粘贴和批量改写都会拦截，只允许表1/共享结果带入。第38列“倒虹吸/有压管道水头损失”的双击入口也在这里统一分流：有详情走详情弹窗并可保存/恢复人工采用值，无详情直接进入单元格编辑；其中普通可编辑行继续走 `pressure_pipe_loss_override_m`，锁定逐行承压行走 `pressure_pipe_window_override.manual_total_head_loss`。这里还负责把链汇总严格收口到“只认成员自己的正式 identity”，锚点缺记录时补成功说明，普通成员缺记录时给出“未匹配到本成员计算记录”的独立诊断。表1/表3结果失效时，“有压管道水力计算”按钮继续保持可点击，由入口前置校验统一弹提示，不再直接禁用成无反馈状态；压力管道流量段摘要的节点来源统一按“显式传入节点 > 当前表格快照 > `nodes/calculated_nodes`”取值，避免正式导出继续吃到过期快照。
- `app_渠系计算前端/water_profile/water_profile_dialogs.py`：有压管道配置窗口，负责普通模式的链卡/分段卡，以及连续承压整线的整线卡、隧洞只读摘要、DXF 导入锚点选择和非隧洞覆盖校验；`xx管` 夹带隧洞参数统一从表1/Excel 读取，弹窗只做只读摘要和缺项提示，不再新增弹窗手填入口。整线卡导入纵断面前，会先按解析器返回的候选结果决定是否需要确认，再继续导入，并支持在同一次弹窗里连续补导入多份 DXF；只有覆盖完整后才把 route 级数据同步到持久层。
- `app_渠系计算前端/water_profile/cad_tools.py`：纵断面导出与 xx管 中心线高程采样，负责普通模式与 xx管 的导出桩号格式化、特殊建筑进出口的显示 IP 文本、按结构拆成“上方普通渠道表 + 下方 xx管 表”的导出组织、mixed route 的隧洞段取底高程、连续承压 `xx渠` 的固定 5 项宽松导出、空白中心高程提示、单点纵断面过滤、同桩号 identity 候选回退、普通导出默认使用完整 lookup 节点、统一覆盖报错和结构分表规划器 `TailPressureSplitPlan`。当前也提供共享的纵断面覆盖提示格式化，统一输出 `平底圆形隧洞 D=...m B=...m` 这类隧洞摘要文案，并把平底圆形隧洞放进断面汇总独立分组，不再混入普通圆形隧洞；隧洞节点在导出阶段只退出 `identity_mismatch / coverage` 强校验，但如果本身已有可用隧洞 profile，仍照常参与高程取数。下方 `xx管` 表若被隧洞切成多段连续有压 span，会保留 1 套共享表头，并把每段画成横向独立子表，中间留固定空隙；每个子表的首尾桩号同时按整高边界绘制，保证各自表格边框闭合。
- `倒虹吸水力计算系统/dxf_parser.py`：DXF 解析与平纵断面几何重建，负责纵断面候选自动优选、反向归正，以及在候选非常接近时向上层返回“需要确认”的标记。
- `app_渠系计算前端/water_profile/formula_dialog.py`：表头说明和双击公式弹窗，负责把计算详情解释给用户看；第38列详情弹窗底部支持直接保存人工采用值或恢复自动计算，其中锁定逐行承压行会额外按“本行采用值”口径写回正式承压损失。
- `updater.py`：自动更新核心，负责版本比较、补丁/全量包选择、安装会话、旧 `_update_sessions` 残留清理、安装前校验、补丁适用性校验和回滚。
- `update_helper.py`：独立安装窗口，负责展示安装阶段、`validate` 细分状态、补丁校验进度、旧残留清理失败提示和日志入口。
- `tools/build.py`：构建脚本，负责按分组校验关键依赖、单独拦截 Word 导出依赖缺失、生成 manifest、通用补丁包，并按补丁覆盖范围决定是否发布补丁。
- `推求水面线/core/calculator.py`：整表计算总调度，串起预处理、内部 `ip_number` 与显示 `display_ip_number` 分配、渐变段、水力计算、累计损失和高程递推；当表内已存在渐变段/连接段时，会改走“真实节点重算 + 辅助行保位”的安全几何刷新路径。当前也负责在静默重算里优先展开 `pressure_pipe_window_override`，把逐行承压覆盖结果正式落回总损失、累计损失和水位递推。
- `推求水面线/core/hydraulic_calc.py`：水头损失和水位递推核心，负责沿程、弯道、局部损失及水位更新，并消费窗口回写后的承压覆盖结果；连续承压链逐行 override 现在也会在这里同步写回表3显示损失，避免“总损失已更新、承压损失列还是空白”的口径分裂；若逐行承压 override 带有 `manual_total_head_loss`，这里也按手动总值优先重建正式总损失和水位递推；开放渠道几何口径当前已补齐 `明渠-复式梯形`，隧洞几何口径已补齐 `隧洞-平底圆形`。
- `推求水面线/shared/shared_data_manager.py`：表1/单断面结果进入表3前的共享结果整理层，负责结构类型归一化、断面参数提取和 `section_params` 落盘；当前会稳定保留 `隧洞-平底圆形` 的 `D / B / H_total`。
- `app_渠系计算前端/structure_type_selector.py`：结构类型选择器，负责分类展示、搜索和高亮；当前支持按入口排除指定结构，用于表3手动选择时隐藏 `隧洞-平底圆形`。
- `calc_渠系计算算法内核/生成断面汇总表.py`：断面汇总计算底层，负责各类渠道/隧洞/有压结构的默认分段、汇总计算与导出数据组织；当前新增平底圆形隧洞独立分组与标题。
- `推求水面线/core/pressure_pipe_calc.py`：有压管道专项公式库，提供 FMB 沿程损失和承压弯头局部损失计算。
- `推求水面线/managers/pressure_pipe_manager.py`：有压管道结果与纵断面持久化，负责 `pipes / routes / segments` 三层存储，并兼容保存 mixed route 的 `profile_segments`、隧洞水力核算参数缓存（含糙率与模式标记），以及整线纵断面的即时写入；连续承压保存按当前整线快照重建，会清掉活动范围内的旧 route / segment / pipe 残留。
- `推求水面线/models/data_models.py`：节点和项目设置的数据结构定义，统一封装 IP 显示文本与 raw/display 双编号规则，并保存第38列人工采用值等项目恢复所需元数据。
- `推求水面线/models/enums.py`：结构类型、进出口标记和相关判断规则。
- `推求水面线/utils/pressure_pipe_extractor.py`：保留命名组提取入口，同时补齐连续承压链提取；命名有压段现按“连续出现的同名段”分组，并同时保留新连续段身份与旧兼容 identity；`xx管` 保持原有整线口径，`xx渠` 只有在形成连续承压线时才补整线路由、整线平面点和子段范围；其中 `支渠` 连续承压链会从首个真正有压段起链，前置隧洞不再进入 route 上下文，链首单点命名普通有压会优先识别为“起点前缀段”，仅在拿不到有效长度时才回退为起点锚点；凡是真正进入连续承压链的命名 `有压管道 / 定向钻 / 顶管`，现在都统一按逐行成员正式计损，父组继续保留 `rows...` 身份和 `split_to_row_members / split_row_member_identities` 做窗口汇总，不再和子成员共用 identity。拆分成员会同时保留原父组 identity、legacy identity 和当前 row identity 的桥接别名，让 fresh dialog 分组和执行前门禁继续认得同一条尾段，并且展示名只从基础名称生成，避免双后缀污染。文件内现在正式输出 `PressureRoute / PressureSegment / PressureResult / ProfileCoverageState`，作为计算、保存、导出的统一领域模型。
- `推求水面线/utils/pressure_pipe_longitudinal_utils.py`：纵断面裁切与按桩号采样工具，负责把整线纵断面切成子段可用数据。
- `推求水面线/utils/pressure_pipe_result_helpers.py`：有压管道结果归一化、批量文本拼装和连续承压链总览格式化，并负责把“已完成 / 未完成 / 前缀段 / 起点锚点”翻译成用户看到的结果文案。
- `推求水面线/utils/__init__.py`：打包兼容入口，负责把顶层 `utils` 显式指回项目自带工具包，避免正式包运行时被同名包抢占。
- `tests/`：回归测试与规则测试，覆盖计算口径、界面说明和范围边界。

## 模块之间的调用关系

- `panel.py` 负责收集表格数据并生成 `ChannelNode`、`ProjectSettings`。
- `panel.py` 导入表1/Excel 到表3后，会先保留每一行的转弯半径，再按真实导入行是否“统一正数 / 混合值 / 全空或0”决定顶部栏位是否显示统一值。
- `open_channel/panel.py` 调用 `calc_渠系计算算法内核/明渠设计.py` 的统一入口完成断面计算，再把结果交给自身结果页、断面图和 `open_channel/dxf_export.py`。
- `tunnel/panel.py` 调用 `calc_渠系计算算法内核/隧洞设计.py` 的不同断面入口完成计算；其中平底圆形额外复用 `tunnel/geometry.py` 的共享几何来统一结果说明、断面图和 DXF。
- `batch/panel.py` 对 `明渠-复式梯形` 直接走新的明渠内核分支，并把 6 个参数随结果一起注册到共享数据，供表3继续复用；对 `隧洞-平底圆形` 则固定读取 `D / B` 两列并回写 `H_total`。
- `panel.py` 调用 `calculator.py` 进入整表计算流程。
- `shared_data_manager.py` 在接收隧洞结果时，会把单断面页的 `平底圆形` 和批量页的 `隧洞-平底圆形` 统一归一到同一结构类型，并把 `D / B / H_total` 一起写入节点参数。
- `water_profile/panel.py` 打开手动结构选择器时，会通过 `StructureTypeSelector(excluded_items={"隧洞-平底圆形"})` 隐藏该类型；但工程恢复、表1导入和导出读取仍继续识别它。
- `panel.py` 在插入渐变段后写回表格时，会把自动连接段的原始结构类型、辅助行坐标和渐变段规则上下文一起落到行级载荷；后续再从表格重建节点时，会优先用这些上下文恢复辅助行。
- `panel.py` 在 `插入渐变段` 成功后，除了刷新表3，还要把 `prepared_nodes` 同步回 `self.nodes`，并清空已过期的 `self.calculated_nodes`；这样“按钮入口 / 导出入口 / 自动保存”读取的都是同一套最新拓扑，不会再出现表格已是 71 行、内存仍停在 68 行的分叉。
- `panel.py` 现在会把 IP 列的原始编号和显示编号一并写进单元格 `UserRole`；表格回读优先取元数据，避免“黄角坝隧进”这类无数字文本被误判成 `0`。
- `calculator.py` 调用 `hydraulic_calc.py` 计算水头损失和水位，并在需要时结合渐变段处理。
- `calculator.py` 在检测到节点序列里已有渐变段/连接段时，不再让辅助行参与整套几何重算，而是先只刷新真实节点的转角与桩号基线，再按辅助行现有位置回补连接段桩号。
- `hydraulic_calc.py` 在逐段承压成员已经写回窗口覆盖结果后，会继续优先识别这份覆盖结果，并复用 `pressure_pipe_calc.py` 的 FMB 和承压弯头公式；旧整组损失不会再叠回同一行。
- `pressure_pipe_extractor.py` 会先提取命名组，再按连续承压链决定是否补 `route_key`、整线范围和子段范围；这样 `xx管` 保持原行为，`xx渠` 也能在连续承压场景下进入整线底座。
- `pressure_pipe_extractor.py` 在 `xx渠` 末尾命名承压尾段拆分成逐行成员后，会继续把原父组 identity / storage、legacy identity / storage 和当前 row identity 一起挂到 source aliases；因此 fresh dialog 里的父组虽然保持原命名段身份，仍能被稳定识别成 split parent。
- `pressure_pipe_extractor.py` 在链构建完成后，还会统一补链成员角色和展示后缀；如果 `支渠` 链首是单点命名普通有压、且同链后面还有同名普通有压段，就先判断它到下一段特殊承压建筑进口之间是否存在有效长度；有长度时按前缀段参与计损，无有效长度时才回退为锚点。
- `shared_data_manager.py` 在接收 batch 或明渠结果时，会把 `复式梯形` 统一映射为 `明渠-复式梯形`，并额外保留 `m1 / B1 / m2 / B2 / m3 / h1` 原始参数，供几何复算使用。
- `water_profile_dialogs.py` 把整线纵断面按 `route_key` 收集后返回给 `panel.py`；在连续承压整线模式下，导入时会先读取 `dxf_parser.py` 的候选排序结果，若前两名过于接近就先弹确认，再自动对齐到当前应补的非隧洞段起点，并直接按非隧洞导出节点校验覆盖范围。部分补导入只保留在当前弹窗内并继续提示详细缺口，整线覆盖完整后才同步到 `PressurePipeManager`，让主页面导出读到的是最终整线结果。导入侧覆盖不足和缓存失效说明，都通过 `cad_tools.py` 的共享提示函数统一格式。
- `dxf_parser.py` 负责把纵断面 DXF 中的多段线先做候选排序，再把选中的同一条几何同时供“起点 X 计算”和“纵断面节点解析”复用，避免偏移计算和节点解析命中不同多段线。
- `pressure_pipe_extractor.py` 在 `支渠` 模式下，会先收紧连续承压链的起点，再用收紧后的行范围生成 route 上下文；因此整线卡、导入锚点和 route 平面点不会再被前置隧洞拉到错误位置。
- `pressure_pipe_extractor.py` 在连续承压链拆成逐行成员后，会把 split 父组和正式子成员明确分成两层：父组继续保留 `rows...` 身份供 fresh dialog 和窗口汇总使用，子成员统一用 `flow-row` 身份参与链内统计、正式写回和后续导出。
- `panel.py` 在计算和导出前调用 `pressure_pipe_longitudinal_utils.py`，纯整线继续按 `route_key` 找整线纵断面再裁切子段；mixed route 则优先使用 route 级 `profile_segments`，并会先从 `PressurePipeManager` 把隧洞缓存参数补回新提取的分组对象。若普通子段命中的分段纵断面只有 1 个点，或虽然有多点但覆盖不到当前导出行自己的 `station_mc`，则改回整线纵断面；route 级导出映射还会补齐起点前缀段/锚点、单行成员和旧口径 identity；隧洞生成段不回退。若先命中旧空记录，则继续按 `route_key` 回退整线纵断面，不直接判失败。
- `panel.py` 计算完成后通过 `pressure_pipe_manager.py` 把整线纵断面写入 `routes`，mixed route 额外把分段结果写入 `routes[route_key].profile_segments`；连续承压正式结果则同时写入 `segments`，`pipes` 只保留兼容镜像；保存前会先清掉当前整线活动范围内的旧快照。
- `panel.py` 与 `formula_dialog.py` 配合，把 `ChannelNode` 上保存的详情展示为双击弹窗和表头说明。
- `panel.py` 在第38列保存人工采用值时，不覆盖 `pressure_pipe_window_override` 或命名承压组隐藏结果；`formula_dialog.py` 只展示原始计算值，真正参与表3显示与后续递推的是 `ChannelNode.pressure_pipe_loss_override_m` 的优先口径。
- `panel.py` 会先按渠道级别和已保存的匿名段标识判断是否属于连续承压整线里的匿名普通有压管道，再决定列38显示值、窗口卡片、结果回写和静默重算口径，避免前后端各用一套规则。
- `batch/panel.py` 会先把重名结果拆成“真正风险重名”和“允许放开的连续承压链/隧洞跨闸重名”两桶，再决定是否拦截正式计算或弹黄色提示，避免只是前后同名普通有压段或同一条隧洞跨闸录入也被要求改名。
- `panel.py` 保存 `text_export_settings`，普通模式导出入口、IP 表、合并 DXF 里的 IP 表和 bzzh2 都从这里读取 `station_decimals`；xx管 纵断面继续单独读取 `xxpipe_station_decimals`。
- `cad_tools.py` 在判断到“不是实际 xx管 渠道级别、但已进入连续承压整线”的 `xx渠` 时，会切到宽松导出策略：固定 5 项表头保持不变，第 4 行中心高程允许留空，并在 TXT / DXF / 合并 DXF 成功后统一提示用户回表3补齐纵断面轴线 DXF。
- `cad_tools.py` 在夹带隧洞的导出里，会先按结构拆成“上方普通渠道表 + 下方 xx管 表”：上方普通渠道表继续跟随当前 `7/扩展项` 配置，隧洞回到这张表；下方 `xx管` 表只保留 `有压管道 / 定向钻 / 顶管`。
- `cad_tools.py` 在连续承压相关导出前，会先用整张表 `full_nodes` 生成统一的结构分表规划，再决定上下两张表的内容；这样单独 DXF、TXT 和合并 DXF 使用的是同一套判断结果。
- `panel.py` 现在会把 IP 列的原始编号和显示编号一并写进单元格 `UserRole`；表格回读优先取元数据，避免“黄角坝隧进”这类无数字文本被误判成 `0`。
- `cad_tools.py` 在生成纵断面坡降行补线时，会先判断右侧是否已有特殊建筑进/出的整高竖线；若有，就直接跟随该整高竖线，不再在前一个矩形暗涵节点额外补短线。
- `panel.py` 顶部“转弯半径”栏位不再作为隐式全局兜底，只在用户点击“应用”后批量覆盖真实导入行；行内留空或 `0` 的计算语义始终由该行自己决定。
- `panel.py` 打开有压管道窗口时，会先按“渠道级别 + 连续承压链”决定是否进入连续承压整线模式；纯承压整线和夹带隧洞的 mixed route 都进入同一入口，其中整线卡负责 DXF，隧洞继续只做表1/Excel 的只读摘要与缺项提示。
- 命名有压管道组默认仍由专项模块先算出整组总损失并回写；但 `xx渠` 末尾连续承压中的命名 `有压管道 / 定向钻 / 顶管` 会先拆成逐段成员，表3按逐行结果正式递推，整组总损失只保留在窗口汇总里。
- `panel.py`、`cad_tools.py` 和 `multi_siphon_dialog.py` 在导入 `utils.*` 前，会先加载 `推求水面线.utils`，让正式包运行时的顶层 `utils` 固定落到项目自带实现。
- `bootstrap.py` 在创建主窗口前会先调用 `webengine_diagnostics.py` 做标准预检；预检阶段现在只采集轻量运行时信息，不再顺手调用可能阻塞的系统摘要接口。
- `app.py` 启动时会 eager 创建全部面板，但 `pressure_pipe/panel.py` 的初始帮助页渲染已改为延后执行；这样仍保留“页面对象先建好”的兼容口径，同时避免单个结果页把主窗口首次显示卡住。
- `tools/build.py` 会先按分组校验关键依赖，并复用与 PyInstaller 相同的项目搜索路径；Word 导出依赖缺失时会先直接中止打包，再根据 `UNIVERSAL_PATCH_MIN_VERSION` 选出可覆盖的旧版 manifest 生成通用补丁。如果补丁删除文件过多，或“新增/修改 + 删除”总量过大，会直接跳过补丁，只保留全量包。
- `tools/release.py` 会读取 `patch-info.json` 决定是否把补丁链接写进正式 `version.json`；没有 `patch-info.json` 时，用户端就只会看到全量包。
- `updater.py` 读取 `version.json.min_patch_version` 后，只对满足版本下限的本机提供补丁下载；进入安装后，会在 `validate` 阶段先清理旧 `_update_sessions` 残留，再把“检查写入权限 / 统计安装目录大小 / 解压完整安装包或补丁包 / 校验补丁适用性（x/y）”通过 `stage_callback` 传给 `update_helper.py`。

## 关键设计决定和原因

- 空名称普通有压管道行按“独立承压管段”处理，而不是复制相邻定向钻/顶管整组结果，这样表3红框单元格、总损失和水位递推才一致。
- `复式梯形` 采用“固定几何、只反算水深”的设计决定：用户明确输入 `m1 / B1 / m2 / B2 / m3 / h1`，程序不再复用梯形的附录E寻优、宽深比或自动底宽逻辑，避免公式口径混淆。
- `明渠-复式梯形` 在表3只做最小兼容，不扩展完整编辑界面；这样可以先保证批量结果能顺利进入下游，又不把本轮范围扩散到新的大界面改造。
- `平底圆形` 采用“固定 `D + B`、自动推导 `H_total`”的设计决定：用户不需要也不能再手填削底高度，避免同一断面出现两套输入语义。
- `平底圆形` 的几何真源统一收口到 `tunnel/geometry.py`；内核、断面图、DXF 和表3复算都复用同一套 helper，避免“算出来是一套、画出来是另一套”。
- `隧洞-平底圆形` 只允许通过表1/共享结果进入表3，不在表3手动结构选择器里开放；这样能保留导入复算能力，同时避免在表3上再增加一套手工建模入口。
- 该独立承压口径默认在 xx管 启用；xx渠 只有在末端或跨流量段形成连续承压线时才启用，避免把整线规则扩散到普通渠系项目。
- 连续承压链中的命名 `有压管道 / 定向钻 / 顶管` 只要进入正式计算，就统一按“逐行成员正式计损 + 父命名组只做窗口汇总”执行；这样表3显示列、总损失、累计损失和水位递推始终共用同一口径，也不会再把父组结果重复累计。
- `xx渠` 末尾连续承压尾段的父命名组继续保留原命名段 identity，但它只作为窗口汇总对象；真正决定“是否已完成”、真正回写表3的，只能是拆分后的逐行成员，避免绿色成功后立刻执行仍被误判成未计算。
- 连续承压链的父组和正式子成员必须彻底分开：split 父组保留原 `rows...` 身份和窗口汇总职责，正式结果、失败归因和链汇总只允许落到 `flow-row` 子成员自己身上，避免“前缀段失败文案挂到中段”这类串记录问题。
- `支渠` 链首前缀段的 `flow-row` 身份不能再按当前表格行号现算，而要优先继承原始节点上的 `pressure_pipe_row_identity`；这样即使前面插入了渐变段，前缀段也不会和后续中段成员撞到同一个 identity。
- 成员展示名、父组展示名和逐段小分组展示名职责分开：链成员展示名负责用户看到的 `前缀段 / 中段N / 后段`，父组展示名只服务窗口汇总，`named_row_segment` 底层校验只使用基础名称；若逐段小分组失败，`panel.py` 只替换失败原因抬头为当前成员名，不改原因正文。
- 命名尾段出口行在静默重算和旧工程恢复时，会优先把 `_pressure_pipe_display_loss` 视为“本行正式承压损失”；只有拿不到这笔显示值时，才回退到基础分项。这样列38、总损失、累计损失和水位递推始终共用同一条口径。
- 锁定逐行承压行的人工采用值不再通过直接改单元格处理，而是统一保存在 `pressure_pipe_window_override.manual_total_head_loss`；这样项目保存/重开、静默重算、列38显示、总损失和后续水位都能继续共用同一份正式结果。
- 命名有压段不再按名称全局合并，而是按连续出现的同名段分别建 `identity / storage_key`；旧的 `flow_section::name` 只保留为兼容别名，这样前段普通有压 + 中间顶管/定向钻 + 后段同名普通有压不会再互相覆盖。
- 匿名普通有压管道段在窗口里使用 `pressure_pipe_row_identity` 作为存储键，界面显示名单独放在 `display_name`，这样空名称行不会互相覆盖。
- 表1导入到表3时，承压类行也会主动落 `pressure_pipe_row_identity`；后续导出和结果回写优先按真实行身份命中，旧工程再回退旧口径，避免同名段串结果。
- 匿名普通有压管道段一旦应用窗口结果，就把该结果持久化到节点覆盖载荷里，并由 `hydraulic_calc.py` 优先识别，保证静默重算和详情展示继续沿用窗口结果。
- 连续承压链只负责把连续的有压类结构和隧洞串成一条逻辑链，不改变各成员原有公式；链总损失只是成员损失求和。
- 纵断面 DXF 不能再按“文件里的第一条多段线”解释，因为真实工程图里常混有辅助线、闭合框线和工程坐标线；因此改为“图层关键词 + 局部坐标 + X 向展开 + xspan/总长/顶点数”的候选优选规则，并在头两名过于接近时交给界面层再次确认。
- 整线卡不是所有有压管道组的默认入口，而是“连续承压整线”专用入口；因此 `route_key` 这类整线元数据只在 `xx管` 或满足连续承压条件的 `xx渠` 场景下生成。
- 连续承压整线需要导入几次纵断面 DXF，统一按“有压连续段数”计算；前置/尾置无压隧洞不单独生成 `route_key`，只有夹在中间的无压隧洞才会把前后 `有压管道 / 定向钻 / 顶管` 切成多条 route，这样弹窗卡片、缺失提示、覆盖校验和持久化都能共用同一口径。
- `支渠` 的连续承压链不能再从前置隧洞起算；只有进入 `有压管道 / 定向钻 / 顶管` 后，这条链才算真正开始。这样既能保留后续隧洞，又不会把前置隧洞误挂到整线卡、导入锚点和 route 统计里；`xx管` 旧口径保持不动。
- 连续承压链的链首成员不再一律按锚点处理；只有拿不到有效前缀长度时，才把它当成纯定位锚点，避免把真实存在的起点前缀距离漏算。
- `支渠` 连续承压链里的链首命名普通有压若只是单点进口边界，且后面还有同名普通有压段，就继续往下一段特殊承压建筑进口找前缀长度；有有效长度时按“起点前缀段”只计沿程损失，并把结果写回下一段特殊承压建筑的进口行，无有效长度时才回退为锚点。
- 结果保存继续按成员颗粒度落盘，链总览只作为批量计算展示数据存在，避免表格回写和持久化时重复计损。
- 连续承压链只要还有真实子段失败，就把整线状态标为“未完成”并隐藏整线总损失；成功子段继续保留各自回写，避免把部分和误当最终整线值。
- 表3列38对连续承压整线里的匿名普通有压管道只是展示值，因此总损失、水位和累计说明里不能再把它当作单独一项重复相加。
- `pressure_pipe_window_override` 驱动的逐行承压成员，列38展示值、总损失、累计总损失和水位递推必须统一以同一份 override 重建；split 父组 summary-only 结果和命名隐藏整组结果只保留窗口汇总，不再重复参与正式累计。
- 表3第38列的人工采用值必须独立持久化，优先参与列38显示、总损失、累计总损失和水位递推；但它不能覆盖 `pressure_pipe_window_override` 或命名承压组隐藏结果，因为详情弹窗还需要保留原始计算依据并支持“恢复计算值”。
- `calculator.py` 现在会把相邻的 `有压管道 / 定向钻 / 顶管` 统一视为同类承压结构，不再额外插入渐变段或连接段；但遇到隧洞时仍保留渐变段，保证表3插段结果和有压管道窗口里的“无渐变段”说明一致。
- `calculator.py` 对矩形暗涵 ↔ 隧洞这类场景，若某一侧渐变段最终解析长度为 `0`，就直接跳过该侧渐变段行；这样不会再出现“多一条灰色渐变段、后续还把桩号链带偏”的情况。
- `hydraulic_calc.py` 里的普通有压管道渐变段正式计算，和 `calculator.py` 插入阶段统一按 `5h/6h` 处理；这样表3长度详情、拓扑插入和最终采用值使用同一口径，不再出现“公式值 0/2.5、最终却沿用 10”的错位。
- `calculator.py` 的插入阶段估算现在也优先取相邻明渠/同流量段参考明渠的设计水深，而不是在有压管道自身无水深时直接退回默认 `2.0m`；这样“隧洞 ↔ 有压管道”这类 xx管 场景不会先估出过大的 10m。
- `panel.py` 在展示渐变段长度详情时，会按当前公式重新刷新公式来源的旧长度；只有手动覆盖或物理压缩得到的更短采用值才继续保留，避免旧项目缓存把过期长度反复带回界面。
- 隧洞沿程损失继续保留原有“底坡 × 有效长度”口径，不混用承压管道公式。
- 连续承压整线的导出口径改为“整线 DXF + route 级分段结果 + 按结构拆成上下两张表”并行口径：普通有压段继续按 DXF 裁切，隧洞段参与 route 级分段结果，但导出时回到上方普通渠道表；下方 `xx管` 表只保留 `有压管道 / 定向钻 / 顶管`。若这些有压段被隧洞切成 2 段及以上连续 span，下方表会改成“共享表头 + 多个横向子表”的布局，而不是首尾压成一整张连续表；这些子表的首尾边界都按整高竖线处理。
- 连续承压整线弹窗现在只保留整线卡与隧洞只读摘要，不再在该弹窗里编辑子段 `R / D` 或新增隧洞参数录入；子段参数继续沿用表1/Excel 已有值，避免把“整线几何导入”和“子段参数真源”混在同一个入口里。
- mixed route 的 DXF 只覆盖非隧洞段；若整线起点是隧洞，则第一份导入自动以第一段非隧洞桩号作为 DXF 对齐锚点，后续补导入则按当前第一个未覆盖的非隧洞段起点自动切换锚点。导入校验与导出校验共用同一套非隧洞覆盖规则。
- 共享导出链路里的 lookup rows 会保留 `station_mc / route_key / route_display_name / node_label / is_tunnel`；正式导出先尝试子段纵断面，只有当子段真的覆盖当前行自己的桩号时才继续使用，否则直接退回整线 `routes[route_key].longitudinal_nodes`。隧洞行则跳过 `identity_mismatch / coverage` 强校验，只在已有可用隧洞 profile 时参与取高程。
- 隧洞参数真源统一留在表1/Excel；弹窗只读取当前隧洞的断面、坡降和糙率做摘要展示，其中 `坡降 i / 糙率 n` 仍需落在主表已有列里，确保 mixed route 重算沿用同一套数据。
- xx管 图2“管中心线高程（米）”继续只在导出时现算，不回写主表；桩号优先使用 `station_MC`，只有个别节点缺失时才允许按平面累计距离回退。
- xx管 夹带隧洞时，隧洞不再留在下方 `xx管` 表里单独展示参数或底线；需要改值一律回表1或 Excel，文档口径也不再承诺旧项目迁移保证。圆拱直墙型在这条链路里只认 `B`，不再使用 `H`。被隧洞隔开的下方有压段会各自保留原桩号、IP、材料和管径文本，但在版式上拆成多个独立子表。
- 连续承压 `xx渠` 复用 `xx管` 固定 5 项表头，但只放宽导出时的中心高程缺失处理；这样用户可以先拿到图，再根据软件提示补导或补全纵断面轴线 DXF，而严格 `xx管` 旧口径不受影响。若严格导出仍因覆盖不足失败，也会直接复用导入侧同一套详细缺口提示。
- 普通渠道末尾连续进入有压段时，不直接把整张图切到 `xx管` 模式，而是在同一个 DXF 里上下拆成“渠道表 + 有压表”；这样前半段明渠信息不丢，末尾承压段也能稳定复用 5 项固定表头。若下方有压表内部又被隧洞切成多段，绘图层继续在同一套下表里按固定空隙横向展开多个子表，并同步推开后续汇总表和 IP 表。
- `pressure_pipe_manager.py` 同时保留旧的单段纵断面兼容读取、`routes.longitudinal_nodes`、`routes.profile_segments` 和新的 `segments` 正式桶，避免旧项目打不开，又能让新导出和新提示统一读同一份分段结果。
- `pressure_pipe_manager.py` 不再对连续承压快照做 `setdefault + update` 式叠加；当前整线结果是唯一真源，同一活动范围内的旧 route、旧 segment 和兼容 pipe 镜像都会被覆盖清理。
- DXF 建筑物名称与软件展示名彻底拆开：DXF 只读 `dxf_display_name` 并按整段范围居中，软件里的“前缀段 / 前段 / 后段”继续只停留在 `member_display_name`，避免再次互相污染。
- xx管 导出和中心线高程采样继续按子段 identity 分段，但匿名子段优先使用 `pressure_pipe_row_identity`，避免同一流量段多个空名称子段撞键。
- 普通模式新增 `station_decimals`，默认 2 位；它只作用于导出链路里的桩号文本，不改表3和说明文字，避免把现有非导出显示一起带偏。
- 通用补丁不再追求大跨度兼容，当前默认只覆盖 `1.1.9+`；因为更老版本在 `_internal` 目录上的历史差异太大，会把大量已删除文件带入补丁校验，用户体感就是“卡在校验”。
- 构建阶段新增补丁兜底规则：`deleted_count > 100` 或 `changed_count + deleted_count > 300` 时直接不发布补丁；这样即便后续打包目录再次大变，也不会把高风险补丁放给用户。
- 安装继续沿用原有 `validate` 阶段，不新增窗口阶段枚举；细节通过状态文案显示为“正在清理上次失败残留”“正在检查写入权限”“正在统计安装目录大小”“正在解压完整安装包或补丁包”“正在校验补丁适用性（x/y）”，这样改动小，但用户能看见安装并未卡死。
- 旧 `_update_sessions` 残留被视为纯临时目录：安装开始时会自动清理当前会话以外的旧目录；如果清理失败，会直接把原因写入安装日志，并在窗口里提示用户先关闭软件、仍失败再重启电脑。
- `ProjectSettings.format_station()` 继续保留原有 3 位小数口径；普通模式和 xx管 都改为在 `cad_tools.py` 里按各自设置单独格式化，这样能把影响范围收在导出入口。
- `calculator.py` 继续保留内部 `ip_number` 给算法和专项结果使用，同时额外生成 `display_ip_number` 给主表、纵断面、IP 表和 legacy Excel 共用；这样特殊建筑进出口可以不占显示编号，但原有计算链不需要跟着重排。
- 辅助行的旧项目兼容继续优先依赖表格文本和旧版 `_auto_channel` 载荷恢复；新加的结构类型/坐标字段只是增强，不会让旧项目打不开。
- `cad_tools.py` 里的有压管道断面汇总弹窗，现已拆成“展示模型”和“导出模型”两层：界面只显示“流量段主行 + 顶管/定向钻单独行”；`xx管` 夹带隧洞时，隧洞部分只负责只读摘要和缺项提示，确认后仍回到原始分组与表1/Excel 数据链路，保证压力管道特性表继续按流量段稳定汇总。
- `panel.py` 里的压力管道流量段摘要，主长度会优先用有压管道自身边界的 `segment_start_mc / segment_end_mc` 计算；缺少边界元数据的旧工程才回退到按节点扫描的老口径。隧洞、定向钻、顶管摘要长度则改为按每组“出口里程MC - 进口里程MC”统计，不再把出口后的普通有压管道首段并进建筑物长度。
- `panel.py` 里的压力管道流量段摘要，针对 `xx渠` 会先按连续有压链判断当前隧洞是否被 `有压管道 / 定向钻 / 顶管` 前后夹住；只有这种中间隧洞才会计入 `total_length / tunnel_count / tunnel_length`，前置隧洞和末尾隧洞都不统计。`xx管` 继续沿用整线口径，但在按边界切流量段后，还会把前置无压隧洞以及它到首段有压结构之间的空档补回主长度。
- `cad_tools.py` 导出压力管道特性表时，主长度优先使用 `panel.py` 汇总后的 `total_length`；旧分组范围和缓存长度只作为无摘要旧工程的兜底，不能再把 `xx渠` 已过滤掉的隧洞或 `xx管` 已补回的前缀长度改回旧值，保证 DXF、Excel 和断面汇总弹窗三处口径一致。
- `panel.py` 里的压力管道流量段摘要，主长度会优先用有压管道自身边界的 `segment_start_mc / segment_end_mc` 计算；缺少边界元数据的旧工程才回退到按节点扫描的老口径。隧洞、定向钻、顶管摘要长度则改为按每组“出口里程MC - 进口里程MC”统计，不再把出口后的普通有压管道首段并进建筑物长度。
- `panel.py` 里的压力管道流量段摘要，针对 `xx渠` 会先按连续有压链判断当前隧洞是否被 `有压管道 / 定向钻 / 顶管` 前后夹住；只有这种中间隧洞才会计入 `total_length / tunnel_count / tunnel_length`，前置隧洞和末尾隧洞都不统计。`xx管` 则继续按整线口径统计，并在有压边界长度基础上，把首个真正有压节点之前的前置无压隧洞及其空档补回主长度；这一步不再依赖“前置隧洞和首段有压必须处在同一条连续链里”，中间即使夹了渐变段、自动插入明渠或其他不可归类节点，也要补回。
- `cad_tools.py` 导出压力管道特性表时，会先用 `_resolve_section_summary_source_nodes(...)` 拿到本次正式导出的 `summary_nodes`，再把这份节点显式传给 `panel.py` 摘要链；主长度优先使用 `panel.py` 汇总后的 `total_length`，旧分组范围和缓存长度只作为无摘要旧工程的兜底，不能再把 `xx渠` 已过滤掉的隧洞或 `xx管` 已补回的前缀长度改回旧值，保证 DXF、Excel 和断面汇总弹窗三处口径一致。
- `panel.py` 里的压力管道流量段摘要，渠首/渠末水位会优先按 `upstream_row_index / target_row_index` 指向的有压边界节点读取；只有跨流量段且边界本身连续时，才会把前一段 `end_water_level` 与后一段 `start_water_level` 统一成同一个切段点水位。
- 连续承压整线导出里，只有 1 个点的普通子段纵断面只算边界占位，必须回退整线 DXF；否则会把已经导入整线 DXF 的支管误判成纵断面不足。隧洞相关内容继续回上方普通渠道表处理，不再进入下方 `xx管` 表的单独参数展示链。
- 弹窗里的纵断面导入状态不能只停留在窗口内存；route 级导入和清空必须即时写入 `PressurePipeManager.routes`，否则会出现“弹窗预览有数据、主页面导出仍按旧状态判断”的口径分裂。
- 连续承压整线在同桩号合并节点后，不能只依赖最终代表节点的单一 identity；导出采样必须保留整组节点的稳定 identity 候选并回退重试，否则会把“identity 没对上”误判成“没导入 DXF”。
- `pressure_pipe_extractor.py` 解析匿名普通有压段起点时，若上游参考节点已经属于别的流量段，就直接从当前目标行自身起桩；这样子段范围不会把上一流量段尾点到本段首点的边界距离带进来。
- `cad_tools.py` 处理纵断面坡降短竖线时，边界默认仍按分段切换点取值；但若切换点右侧就是特殊建筑进/出整高竖线，则改跟随后一个整高竖线，避免矩形暗涵末端在坡降行里出现重复短线。
- 水面线相关代码虽然历史上大量使用顶层 `utils.*` 短路径，但正式包运行时实际先通过 `推求水面线.utils` 建立兼容别名；这样既不需要在热修中大范围改旧导入，又能避开打包环境里的同名包冲突。
- 启动预检里的平台摘要改为“Windows 版本字段直拼，非 Windows 再回退 `platform.uname()`”，不再调用 `platform.platform()`；原因是该接口会在部分 Windows 环境阻塞，把 `main.py` 卡在主窗口出现之前。
- 有压管道页的初始帮助内容不再在 `PressurePipePanel.__init__()` 里同步加载到 Web 预览，而是等页面第一次真正显示时再补渲染；原因是主窗口采用 eager 面板装配，若在启动阶段就先做这段 Web 内容加载，会让用户看到“终端在跑但桌面没窗口”。
